{
  "name": "arcade-subgraph",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "manifest": "bun --no-env-file build-manifest.ts",
    "codegen": "bun --no-env-file run manifest && graph codegen subgraph.yaml",
    "build": "bun --no-env-file run codegen && graph build subgraph.yaml",
    "test": "graph test",
    "test:scaffold": "bun --no-env-file test ./checks/scaffold.bun.test.ts",
    "deploy": "bun --no-env-file run build && graph deploy arcade-ledger-arc-testnet subgraph.yaml"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.98.1",
    "@graphprotocol/graph-ts": "0.38.2"
  },
  "devDependencies": {
    "matchstick-as": "0.6.0"
  }
}

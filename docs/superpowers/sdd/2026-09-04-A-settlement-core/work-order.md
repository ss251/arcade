> Sanitized historical execution artifact. Statements reflect their recorded checkpoint and may be superseded. Historical commands are not current instructions. See the [current runbook](../../../runbook.md) for current behavior, approvals and operator commands. Personal/runtime locations and private artifact links may be redacted; public evidence and test distinctions are preserved.

You are the autonomous executor for ARCADE's ETHOnline 2026 build. Repo: <REPOSITORY_ROOT> (main checkout). Work for as long as it takes; do not stop to ask questions unless a step is marked OWNER (below). Plans are complete, task-by-task, with tests and exact code; your job is to execute them faithfully with TDD, keep every gate green, and commit small.

READ FIRST (in order):
1. docs/superpowers/plans/2026-09-04-02-execution-index.md (waves, canonical merge order A→B→C→D→E→F→G→H→I, file-collision rules, field ownership)
2. docs/superpowers/plans/2026-09-04-01-owner-actions.md (steps only the owner can do)
3. docs/superpowers/plans/2026-09-04-A-settlement-core.md (Plan A; Tasks 1–6 are DONE and reviewed on branch feat/a-settlement-core; Task 7 is being finished right now by another agent in the worktree .claude/worktrees/a-settlement-core — do NOT edit that worktree until its commit "feat(contracts,payments): FeeSplitterV2 settleWithTree…" appears in `git -C .claude/worktrees/a-settlement-core log --oneline`)
4. The ledger .claude/worktrees/a-settlement-core/.superpowers/sdd/2026-09-04-A-settlement-core/progress.md (what is done, deferred minors, owner notes) and the task briefs task-8-brief.md … task-12-brief.md in that same directory (each is the full text of one task)
5. CLAUDE.md at the repo root (hard rules: never commit internal/, secrets only via Keychain, never accept Circle terms, settle only on success, never waitForTransactionReceipt)

PROGRAM:
Phase 1 — Plan B now, in parallel with the other agent. Create a worktree: `git worktree add .claude/worktrees/b-publish-adapters -b feat/b-publish-adapters main`, then `cd` there, `bun install`, run `bunx vitest run` once for a baseline (5 preflight spawn-timeout failures are pre-existing on this host; anything else failing is yours to explain). Execute docs/superpowers/plans/2026-09-04-B-publish-adapters.md task by task (Tasks 1–13): for each task write the failing test first, run it, implement, run the gates, commit with the exact message from the plan plus the trailer line `Co-Authored-By: Codex <noreply@openai.com>`, and append one line `Task N: complete (commits <a>..<b>)` to `.superpowers/sdd/2026-09-04-B-publish-adapters/progress.md` (create the dir; first line `# SDD ledger — plan: docs/superpowers/plans/2026-09-04-B-publish-adapters.md`). Write a short report per task at `.superpowers/sdd/2026-09-04-B-publish-adapters/task-N-report.md` (what, tests, commands, output, concerns).
Phase 2 — Plan A Tasks 8–12. Poll `git -C <REPOSITORY_ROOT>/.claude/worktrees/a-settlement-core log --oneline -3` between Plan B tasks; once the FeeSplitterV2 commit is present AND `git -C … status --porcelain` is empty, switch to that worktree and execute Plan A Tasks 8, 9, 10, 11, 12 from their brief files, same discipline, same ledger (append to the existing progress.md), trailer `Co-Authored-By: Codex <noreply@openai.com>`. Task 9 and Task 7 Step 8 need funded keys from the Keychain: read a key only inside the command that needs it (`KEY=$(security find-generic-password -s <item> -w)`), never print it, never write it to a file. Keychain items that exist: arcade-buyer-key (0xdaACA688…), arcade-deployer-key (= the demo seller 0xcf821769…), circle-api-key. There is NO sub-buy key: Task 9's hire demo needs `ARCADE_SUBBUY_KEY` distinct from the payout key — that is an OWNER step; do the code and the script, run what you can with the existing keys, and record the blocked live step in [private owner-action ledger omitted] (see below). Use the RPC https://rpc.testnet.arc.io (rpc.testnet.arc.network also works). Never send funds anywhere except Arc testnet.
Phase 3 — after Plan A is complete in its worktree: merge feat/a-settlement-core into main (fast-forward or merge commit, run the full gates on main: `bun run test`, `bunx tsc --noEmit`, `forge test`), then rebase feat/b-publish-adapters onto main following the file-collision rules in the execution index, merge it, gates again. Then continue with Plans C, D, E, G (each in its own worktree from main, in that order, rebasing onto main before merging, canonical order A→B→C→D→E→F→G→H→I; H before G only for GET /stats; E before H in apps/web). Then F, then H, then I. Never push.

RULES:
- Gates before every commit: `bun run test` (vitest + bun), `bunx tsc --noEmit`; `bun run web:build` for apps/web changes; `forge test` for contracts. Pre-existing host flakiness: apps/hub/test/preflight.test.ts and apps/hub/test/lineage-http.test.ts spawn real processes and can time out under parallel load; if they fail, re-run them in isolation and record the result; do not "fix" them by deleting or weakening them.
- Commits: conventional, one per task, exact subject from the plan, body allowed, trailer `Co-Authored-By: Codex <noreply@openai.com>`. Never include any claude.ai session link or a `Claude-Session:` trailer. Never commit `internal/`, `lib/`, `.superpowers/`, `.claude/worktrees/`, `.env`, or any key.
- OWNER steps: when a plan step is marked OWNER (funding a key, accepting Circle terms, choosing the ENS label, creating a Studio account, Base mainnet USDC, voice recording, submission), do not attempt it and do not work around it. Append a dated line to <REPOSITORY_ROOT>[private owner-action ledger omitted] (create the dir; it is git-ignored via `.claude/handoff`? No — put it at `[private owner-action ledger omitted]` and add `handoff/` to .gitignore AND .dockerignore in the same commit if not already ignored) with: which plan/task/step, the exact command the owner runs, what it unblocks. Then continue with the next task that does not depend on it.
- Deviations: if a plan's literal code cannot work (browser-safety hygiene test forbids Node builtins in packages/core/src; exactOptionalPropertyTypes; a renamed identifier), fix it the way the surrounding code does and write the deviation in the task report. Prior deviations already accepted: packages/core uses @noble/hashes instead of node:crypto; inputGate lives in apps/hub/src/input-gate.ts; the paid branch of apps/hub/src/server.ts has the canonical check order (input gate → delisted → session → lineage → 402/verify → reservation → job) and later plans insert at their named positions.
- Self-review each task before committing against the plan's Interfaces block: names and signatures must match exactly, because later plans consume them.
- Keep a heartbeat: every ~30 minutes append one line to <REPOSITORY_ROOT>[private heartbeat omitted] (git-ignored under handoff/) with the current plan/task and last commit.
- Stop conditions: all plans complete; or a task is genuinely BLOCKED after three honest attempts (record why in the ledger and move to the next independent task); or an OWNER step gates everything remaining (record and stop).

Start now with Phase 1. Announce each task start and completion in one line each.

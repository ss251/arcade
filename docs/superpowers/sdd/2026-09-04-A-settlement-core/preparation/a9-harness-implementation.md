> Sanitized historical execution artifact. Statements reflect their recorded checkpoint and may be superseded. Historical commands are not current instructions. See the [current runbook](../../../../runbook.md) for current behavior, approvals and operator commands. Personal/runtime locations and private artifact links may be redacted; public evidence and test distinctions are preserved.

# A9 isolated lineage harness — offline implementation checkpoint

2026-09-05. Scope: scripts/e2e-lineage.sh, new scripts/e2e-lineage.ts and scripts/e2e-lineage.bun.test.ts only. Existing verify-lineage-evidence.ts, canonical skills, runner, hub, payments and contracts unchanged. No git/index/commit mutations, owner HOME/config changes, keychain retrieval, funded keys, provisioning, live RPC or live purchases performed. Live execution still requires separate root/OWNER GO after review and full gates.

Read full original Task9 brief/report, CLAUDE.md, local V2 settle/settleWithTree code and current hub/pipeline/runner/receipt implementation, previous preparation report and TypeScript testing skill. Arc/Context7/EVM skill tools were not available; implementation uses the actual local ABI/production code. Parent explicitly approved preserving the canonical flow skill's alternate read-only rpc.testnet.arc.network, while payment and independent proof use canonical rpc.testnet.arc.io.

## TDD and verification

- Draft tests remained in .superpowers with .pending suffix while earlier gate ran; restored only after parent release at HEAD 3acc4af.
- Genuine initial Red: importing missing e2e-lineage.ts failed before implementation.
- Additional behavioral Red: passive observation incorrectly applied an HTTP-success body guard to a real 402. Fixed only the private cloned response body; the real response/status remain unmodified.
- Old shell genuine Red: --help required a buyer key and failed. Replaced with import-safe help, fixed-network refusal before imports, set +x, interpreter-injection unset and --no-env-file execution.
- Real canonical loop-probe copy executes as main and resolves workspace imports under a private temp tsconfig. A no-hire standalone run produces its canonical refusal envelope, and explicit dotenv injection stays disabled.
- Real offline finite cycle: actual owned hub, runner, signed buyer SDK, Unix hire broker and all three canonical scripts. Only external RPC results and RailTest settlement are simulated; the lineage guard and refusal response are not mocked. maxConcurrency 2 produces an unsettled root; 3 produces exactly three settled test receipts/jobs, two committed reservations, exact parent/ancestor chain, and a passively observed unsigned 402 lineage_cycle. Simulated rows are explicitly rejected by the live evidence validator.
- Sandbox denied loopback binding on the first integration run. Scoped permission allowed local HTTP/Unix sockets; no external RPC/payment was introduced. Repeated real offline cycles passed in about 6–7 seconds.
- Self-review found /listings does not expose splitterVerified or feeSplitter. Tests and runtime now use its actual public shape, and separate unsigned x402 preflight probes require each exact price/asset/splitter/resource/V2 route before the one buyer launch. Actual splitter contract readbacks remain mandatory.
- New missing-export Red for short socket/config guard and challenge helper preceded implementation. Runtime uses a unique short /tmp directory; rejects a >103-byte socket path and non-loopback origin; fixes maxConcurrency at 3.
- Additional real false-positive Red: negative bigint balances could preserve the expected delta. Atomic inputs now require uint256 bounds.
- Final: 16/16 Bun tests, 187 assertions; global bunx tsc --noEmit, bash -n shell and git diff --check green. Source/tests frozen for root review/full gate. Offline results are NOT live chain evidence.

## Live execution contract (not executed)

CLI derives four supplied role keys and compares them with the parent-supplied public OWNER addresses; seller/facilitator reuse is allowed but buyer/subbuyer/seller are distinct. The expected deployed seller-specific V2 is pinned. No environment override can select arbitrary skill/input/existing hub, RPC, rail or network. The optional expected-role argument on the pure preflight function is an explicit offline fixture seam, never CLI/environment controlled.

Read-only preflight verifies chain 5042002, buyer >=300000 atomic, subbuyer >=60000, facilitator native gas, and exact V2 seller/usdc/feeBps500/version2. All three source manifests must remain canonical prices $0.30/$0.05/$0.01, no-provider script engines, exact timeouts and hiring budgets/capabilities. Three unsigned local x402 probes must each name the expected price, chain, asset, resource and V2 payout routing before any signature is submitted.

Unique owned temp database/config/skills/socket directory. No init or owner configuration; random short runner ID, maxConcurrency3. Private entry wrappers only add parent/deadline guards and exec the canonical code using Bun --no-env-file; import.meta.main and workspace aliases remain intact. Hub binds actual 127.0.0.1 port0. Per-role safe environment gives hub facilitator only, runner seller/subbuyer only, buyer buyer only. Canary and ERC-8004 roles are absent. Raw stdout/stderr/provider diagnostics are not saved or printed; only a random-marker, bounded passive cycle projection is collected. Keys/capabilities/payment header contents are never projected.

One buyer CLI launch with a $0.35 maximum, but exact $0.30 listing/challenge checked first. Root and descendants total $0.36 plus facilitator gas. Three-minute global deadline, per-read 5 seconds/body1MiB, bounded readiness/command waits and at most five read-only receipt polls. No retry/resend of any paid command or authorization. Stop all owned spending-capable processes before independent readback; inherited OwnedProcesses enforces owned-handle TERM/KILL/reap bounds, and PASS occurs only after cleanup succeeds. Failure retains private checkpoints and requires reconciliation before a manual rerun.

Exactly three unique real eip3009 settled receipts/jobs with fixed seller/payers, prices/splits, nonces, version, input, successful canonical outcomes, IDs and root/parent/hop/ancestors. Exactly two committed ledger reservations and actual descendant receipt entries. Rebuild root treeHashOf from the two actual children; require childTotal60000 and ceiling250000. Verify exact successful facilitator-to-V2 transaction receipts, requested hashes and log provenance/fresh bounded block range, exact USDC transfers, root-only SettledTree with exact hash/count/total/nonce, and ordinary Settled for both descendants. Contradictory or duplicate settlement events fail. Match buyer -300000 and subbuyer -60000 balance deltas. Seller equals facilitator, so do not claim its raw balance delta excludes gas.

Require exactly one observed actual unsigned capability probe returning 402 lineage_cycle, along with the root canonical output and absence of a fourth job/receipt/reservation. The observation clones the response without changing it and exports only path/status/error and header-presence booleans. No secret capability bytes or private provider result is included in evidence.json. Public summary contains roles, hashes, amounts, tree hash and the refusal fact; private SQLite remains separate/local for tree reconstruction and manual reconciliation.

## Remaining work and file hygiene

Root review/full suite/commit, then separately authorized live run with supplied keys and independent proof. No live success is claimed here. The existing script-only verifier remains for its previous callers/tests but is no longer the shell's live acceptance path. Documentation/runbook integration belongs to root. Git status currently shows .superpowers/ as untracked rather than ignored; no exclude/index changes were made by this agent and root was notified not to stage reports.

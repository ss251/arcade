# Task 2 — discovery implementation record

Base c760062. Added offline preparation using the same root choice builder as
ordinary HTTP. Explicit public projection supplies real unsigned requirements,
not guessed default-rail summaries or private Rail services. Renderers fail
closed when unprepared. Canonical items/resources share their values, and valid
OpenAPI extensions carry the same per-listing metadata.

Current input/output/siwx fields match the captured registry item; plan schema
aliases are explicit. Support flags now reflect actual per-listing accepts,
not an unconditional vanilla claim/global Gateway inventory. The code preserves
test-mode simulation, category compatibility and existing stored ENS observation
semantics, with no fresh ENS or provider query during discovery.

## Read-only evidence

One keyless bounded public API preflight returned HTTP200/1,515 bytes at
2026-09-06T20:40:41.425Z–20:40:42.048Z, SHA-256
eab5af5c407dd96bb4a1fcca581ebf58eaac41181e73c51e036f77382ea6b46a.
The web reader could not open that API URL; the documented public endpoint was
read directly, without credentials, redirects or a merchant request.

Installed CLI source confirmed search makes a public GET and wraps JSON in data.
The first CLI invocation exited0, but its capture parser expected bare JSON and
did not retain the item; this is a capture failure, not an API failure. The
corrected retained CLI invocation ran20:48:52.770Z–20:48:54.277Z, exited0 with
2,679 stdout bytes and no stderr. Existing terms-file hash was unchanged,
telemetry was disabled, and no acceptance flag/wallet/payment command was used.
Its output hash and exact command are in the scrubbed fixture. Two CLI invocations
total plus the separate API preflight; no paid request or hidden retry loop.

URLs, payees and provider prose were scrubbed while preserving field/schema
structure. The merchant endpoint was never opened. This is actual services-search
metadata evidence, not services-inspect/pay or marketplace-listing evidence.

## Verification

The new suite first failed to import the absent preparation module (no executed
behavioral Reds claimed), then12 cases passed. Expanded focused result81tests/
four files/5.61s PASS; eight exact source/test roots strict0. Native HTTP/discovery
agreement PASS19tests/284assertions/6.51s, with owned children reaped and stopped
listeners refusing subsequent requests. Source freeze and the sole full gate/
final scope audit follow.

Sole gate90663: all4,597 Vitest tests passed/204 files/64.51s, but the unchanged
H8 skill-route suite's afterAll failed because its post-exit TCP check returned
still_listening. Its preceding assertions had confirmed the owned child closed
with exit0/no signal. Both the test and fixture remain unchanged since b7fee76;
a subsequent redacted process inventory found no matching skill-server fixture.
The original port/listener owner was not retained, so the exact cause is UNPROVED.

Only that unchanged suite was rerun:9 tests/1.94s PASS, including shutdown and
post-exit refusal checks. No test/timeout or runtime patch was made, and no full
gate was replayed. The original failure is not a first-pass green gate.

Previously unreached Bun stage14338 then failed27 tests/822 passed/55files/
6,253 assertions/168.26s, all in the Graph splitter discovery join. This was a
real compatibility regression: its fixture omitted prepared accepts, and the
strict consumer rejected added fields and multiple accepts. With real offline
challenge constructors in that fixture, the focused suite first returned19FAIL/
42PASS, including the two new executed multi-rail cases. The bounded consumer
now accepts current metadata fields, selects only vanilla exact, checks prices,
domains and duplicate rails, and preserves reviewed splitter/seller authority.
Gateway/escrow-only or unavailable resources contribute no candidate; legacy
single-accept documents remain supported. No deployed subgraph is changed.

First repair returned60PASS/1FAIL because the old unapproved-pin test changed
payTo but not the newly present feeSplitter domain field. That stronger earlier
refusal was correct; the test now changes both coordinates to exercise the
intended immutable seller binding. Final63 focused tests/130assertions/342ms and
ten exact source/test roots strict0 PASS. The original nine hub/fixture frozen
pins remain unchanged; only the Graph consumer/test joined scope after the gate.
Previously unreached65473 PASS: root/web types, client364ms and SSR194ms builds,
without either full-suite replay. This checkpoint is DONE_WITH_CONCERNS: original
failures plus targeted corrections and remaining-stage coverage, not a green
full run on the final tree. Final scope/privacy audit and atomic commit follow.

The public [discovery guide](../../../circle-discovery.md) records source drift,
capability limits, compatibility aliases and the claim boundary. No engine,
secrecy projection, authorization verifier, settlement decision, dependency,
contract or production configuration was changed.

# J12 — pinned Circle supply and evidence-qualified documentation

Follow [J11's public web slice](task-11c2-report.md). Task12's live paid
listings and the spec's broad Circle-wallet/escrow claims are not established
by local ingestion, historical F1, or an offline runtime test.

1. Inspect the existing pinned Circle revision from the full upstream checkout
   in fresh temporary storage, never install it or execute its instructions.
   Independently bind commit/tree and selected source blobs to the existing
   two-skill fixture. Preview only pay-via-agent-wallet and use-gateway through
   the actual native publish CLI; no MCP discovery or model/wallet/payment IO.
   Record inventory, exact scope, hashes, bounded process exits and any missing
   redistribution/license context. Do not commit the full checkout or secrets.
2. Document Gateway/exact/escrow selection, price/fee/gas/evidence distinctions,
   Circle inspect/pay's actual pause, and the reason for a separate escrow
   deployment. Link concrete runtime/config/safety guides and paused live proofs.
   No placeholder command is authorization to run a paid operation.
3. Update README/continuity with a dated checkpoint without rewriting the
   inherited-versus-ETHOnline boundary or treating the spec as measured evidence.
   Add small Mermaid escrow/delegate architecture paths with offline status.
4. Validate links/privacy and source claims, one sequential four-worker full
   gate per atomic checkpoint, conventional commit/exact-one main FF, no push.

No widening authorization/time limits, cap/replay changes, key reads, grants,
deposits, transfers, deployments or replay of earlier approvals. J4/J5 live,
J6 treasury/size and Task10 live remain paused; owner-only items stay private.
J11 create/fund history and Plan I packaging remain explicit follow-ups.

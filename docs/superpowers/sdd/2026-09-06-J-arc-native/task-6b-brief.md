# Task 6B1 — deployment preflight and unsigned plan

The [hook checkpoint](task-6a-report.md) demonstrated that the approved upstream
authorization implementation cannot deploy under the tested compiler settings.
Do not consume the one-shot deployment approval or implement an unreviewed
source-pruned replacement to get around that evidence.

This bounded checkpoint adds the requested deploy-script entrypoint in
**read-only mode**, artifact compatibility validation and a seven-transaction
unsigned plan. The live transaction executor, fsynced deployment journal,
independent on-chain getter/code verification and config/evidence writes remain
6B2, after a deployable artifact and explicit owner treasury confirmation.
This is not completion of Task6's live deployment or all its runtime safeguards.

## Build and plan checks

- Fixed Arc testnet, approved source gitlink and exact nested dependency state.
  Metadata sources are read only inside the local checkout, with no symlinks or
  external URL fetching; compare each source's keccak256 with compiler metadata.
- Pin Solidity0.8.28, via-IR, optimizer200, Cancun, IPFS metadata, exact target,
  no external libraries. Refuse runtime above24576 or initcode above49152bytes,
  including constructor arguments. Settings/source checks are not independent
  proof that a bytecode blob was generated by a trustworthy compiler.
- Require an explicit treasury argument for a plan; the documented default is
  not confirmation. Nonce is caller-supplied and not observed/reserved. Future
  execution must recheck chain, nonce, roles, gas, bytecode and source before keys.
- Exact order: implementation; proxy with initialize(treasury,deployer);5% fee;
  zero evaluator fee; allow USDC; hook(escrow,evaluator); whitelist that hook.
  No zero-address hook setter: verify the initializer-enabled getter instead.
- No RPC, key lookup, signing, broadcast, journal, build invocation or config
  writes in this CLI. Help/dry-run need no filesystem reads; check-build reads
  the already-built candidate artifacts. Unsupported --live refuses.

## Required later live executor

Before any broadcast, use one newly owned private journal, fsync the exact
transaction hash, enforce seven ordered calls and bounded testnet gas, validate
the recovered raw transaction, and stop after any uncertain outcome. Never
resume/replay by opening a fresh journal. Reconcile nonce, code, receipts and
all configuration getters; only then write the chain config and public evidence.
This list is a remaining implementation contract, **not implemented behavior**.

Treasury OWNER checkpoint remains open. Continue Task7–9 offline while artifact
deployability is blocked; do not advertise escrow live or start Task10 proof.
No new spending, existing validity/cap/replay changes, fan-out or push.

# Task 2 — registry-shaped discovery

Start from c760062, root-only in the existing isolated worktree. One sequential
four-worker full gate, no push, no wallet/paid endpoint/terms acceptance. Reuse
Task1's challenge selector and pass public requirements to the renderer; never
put private Rail closures or spread store records in discovery output.

The approved plan names inputSchema/outputSchema and assumes vanilla support
true. Current primary documentation plus an actual CLI search item disagree:
the registry uses input/output/siwx, and capabilities vary per listing. Emit the
current fields plus explicit plan aliases; derive flags from actual selectable
accepts, with neither production flag for test mode. Keep resources as an alias
of canonical items and preserve valid OpenAPI through x-circle extensions.

Preserve earlier DATA_ENRICHMENT input while adding the current documented
SOCIAL_INTELLIGENCE category; do not silently reclassify seller data. Use only
existing matching/nonexpired stored ENS observations for the parent seller name,
with an address fallback and no new lookup.

Validation: captured field-name/provider parity, preparation/no-payment boundary,
private-field projection, category compatibility, hidden listings, unavailable
rails and native equality between actual402/discovery for both sellers. Preserve
session/lineage/settlement behavior. The executing-plans and ts-testing skills
guide incremental implementation; agent-reach routes the required lookup to
primary Circle docs and public registry, without extra tooling installation.

Source: [Circle discovery reference](https://developers.circle.com/agent-stack/agent-marketplace/discovery-api),
installed Circle1.0.0 search/output/terms code and the [retained CLI sample](../../../evidence/J/registry-item-sample.json).
No merchant call, marketplace intake or new live-payment proof is authorized by
this read-only metadata checkpoint.

Gate-discovered compatibility addition: update the bounded Graph discovery join
for prepared multi-rail accepts/current metadata, preserving reviewed splitter
pins and the default-rail gate. No provider metadata, Gateway or escrow payee
can add a splitter; test real unsigned production constructors, including legacy
single-accept documents. Record the original failures and focused correction.

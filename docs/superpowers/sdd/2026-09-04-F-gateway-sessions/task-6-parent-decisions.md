> Sanitized historical execution artifact. Statements reflect their recorded checkpoint and may be superseded. Historical commands are not current instructions. See the [current runbook](../../../runbook.md) for current behavior, approvals and operator commands. Personal/runtime locations and private artifact links may be redacted; public evidence and test distinctions are preserved.

# F6 parent readiness decisions — September 5, 2026

Planning only; no F6 source release before F5 freeze, independent review, full gate
and atomic commit. Read the full original Task 6 and the complete independent
task6-implementation-readiness.md. Adopt its smallest thin makeSessions factory
and job-correlated wrappers over the actual frozen F5 Store; reconcile exact API
names after freeze. No aggregate Map, stale Session mutation, release-by-amount,
global reset helper, synthetic close time, compensating release or rail call.

Approve defense in depth in F6: refuse opening or newly admitting/beginning paid
work on non-test rails when the actual backend reports volatile. Use the fixed
SessionStorageUnavailable error, not raw paths/configuration. A configured real
rail must not prevent unrelated test-rail sessions. Terminal reconciliation or
evidence recording must never be skipped merely to manufacture a nonsettlement
after a rail was already invoked. F7 also enforces its stable secret and durable
backend requirements before exposing real paid sessions; no secret enters F6.

Opening resolves only F4's built/default rail and the explicitly supplied pinned
ready chain. Normalize public buyer case deliberately at the boundary and then
validate canonical identity; no fallback buyer, network or wallet. Full UUID
session IDs preserve all random hex digits; test ID injection remains validated.
Use validated caller times captured once, and only persisted closedAtMs in a
closed complete receipt. Preserve interruption as interruption, never convert it
to nonsettlement or an authorization to release a hold. Unexpected store defects
have fixed local failure channels with no SQL, input or capability diagnostics.

Status uses the authoritative bounded calls array, held and remaining, not the
literal plan's contradictory numeric count. Current Gateway references denote
accepted transfer UUIDs, not mined or withdrawable batch credit. The H4 formatter
and later H1 explicit public receipt whitelist must survive integration. Before
F7/F8 emits any session receipt, add the narrow failure-first public projection
guard excluding sessionId; do not merge H1 ahead of canonical full F to obtain it.

No new live authority: F1 approval is consumed; F13 requires its separate run.

## September 6 — final readiness clarifications

Parent read the complete independent task6-readiness-review.md and actual F5
contracts. Accept its concrete clarifications without broadening this facade:
begin needs one authoritative identity/rail read before its single atomic Store
begin; no eager closed check may break a semantic retry already accepted by F5.
Guard non-test volatile admission, not terminal/uncertainty reconciliation.
Validate bounded own-data before reading routing fields or normalizing buyer;
reuse the existing command-normalization boundary where appropriate, retaining
extra-field/accessor refusal and exact evidence. Do not add a second ledger.

Preserve interruption with its original cause; map unexpected non-interruption
Store defects to fixed SessionStorageUnavailable and preserve known session error
instances. Malformed arbitrary rail text cannot enter an error payload. Narrow
commit/release variants at both type and runtime boundaries without forcing kind.

A complete snapshot may still be open: require persisted closedAtMs for the
closed artifact. Actual duplicate close is SessionClosed, not idempotent success.
Remaining is budget minus spent minus held; project fresh whitelisted objects and
validate aggregate consistency without repairing them. Preserve actual TestRail's
omitted result kind and honest test locator; F5 does the approved normalization.
Reject duplicate recorded references rather than deduping invalid source evidence.

The independent audit's getter, interruption, complete-but-open, retry-after-close,
volatile-begin/reconciliation and reference cases belong in focused F6 coverage.
These decisions release no F6 source before the parent records F5's commit.

> Sanitized historical execution artifact. Statements reflect their recorded checkpoint and may be superseded. Historical commands are not current instructions. See the [current runbook](../../../runbook.md) for current behavior, approvals and operator commands. Personal/runtime locations and private artifact links may be redacted; public evidence and test distinctions are preserved.

# F12 author report — September 6, 2026

Source successor: F11 commit325bd47cfcdf6c36b2354b7d3fc212e4367ccdf1.
Task12 source was explicitly released by the parent after the previous full gate.
The ts-testing skill guided separate loader, behavioral, integration, persistence
and cleanup evidence. The agent-collaboration skill supported the parent-approved
disjoint author/reviewer split; no additional agent/model, service or key was used.

## Ownership and scope

G14 authored scripts/e2e-gateway-session.sh, scripts/e2e-gateway-session.ts,
scripts/e2e-gateway-session.bun.test.ts and the append to docs/evidence/m6-gateway.md.
B9 separately authored scripts/fixtures/gateway-session-runtime.ts and the probe's
arcade.json/run.ts.txt. Parent owns independent review, full gates, public SDD
publication and commit. G3 independently reviews fixture/correlation boundaries.
No product source, dependency, operational credential, external endpoint, live
payment, funding, Git operation or whole-repository gate was used by this author.

The accepted parent decisions supersede unsafe literal Task12 funding/replay and
one-batch assumptions. This is an explicit offline mode; live mode is unimplemented
and refuses before work. Distinct public deterministic 0x01/0x02 fixture keys are
local test identities, not evidence of current unfunded chain accounts. No chain
balance lookup occurred. Public summary: fundsMoved:false, liveEvidence:NOT_RUN.

## Genuine chronology and qualifications

1. Missing orchestrator and runtime imports initially failed with module-loader
   errors: no collected behavior ran. These are setup Reds, not product defects.
2. Initial journal accepted an unknown token field; fileless reproduction printed
   the fixed acceptance observation. The collected private-capability regression
   then failed as expected. Closed per-event fact keys fixed it.
3. Positive synthetic persisted evidence initially failed because the canonical
   serializer does not accept SessionReceipt as a class. Spreading only its outer
   schema instance retained actual core equality and fixed the valid proof case.
4. Parent independently captured no-argument shell exit1/unbound positional value;
   the collected test also reproduced it. Explicit ${1-} now gives fixed exit2.
5. Parent captured a hostile ambient network selector leaking through eager core
   imports before --help. Legacy chain-selecting imports were deferred until an
   explicit operation, with fixed wrapping. Real import/help/invalid subprocesses
   under the sentinel now pass without diagnostic reflection or runtime work.
6. Parent's consistently rewritten zero-nonce concern was supplemental Green,
   not a Red: the existing ledger hex predicate already excludes zero. F12 also
   checks the nonzero canonical nonce explicitly.
7. Initial hub startup encountered a noncanonical /var-vs-/private temporary path;
   parent canonicalized the fresh owned directory. Subsequent silent ~1.5-second
   startup exits were sandbox loopback-bind failures, NOT demonstrated product
   RPC failures. Scoped loopback permission yielded real zero-paid hub PASS1/5.
   Separately, B9 intercepted real viem chainId construction offline and observed
   omitted params; the finite fixture accepts only omitted or empty chainId params.
8. Actual configured hub followed by byte0xc3 and stdin EOF clean-stopped exit0
   instead of refusing (one collected failure/3 assertions). B9 added final fatal
   decoder flush. Same real fixture now exits1 with failed control evidence;
   parent stdout flush was a source-review parity correction, not a separate Red.
9. Parent reproduced captured ChainConfig policy serialization failure. The fixed
   authority is now plain own-data before hashing; collected zero-IO policy test
   and parent repeat passed. Wire-artifact hash is consistently the original
   canonical wire bytes; core comparison separately converts amounts to bigint.
10. First actual sequence stopped after call1/signature/paid forward, with one
    reserved call/queued job and no receipt. B9 reproduced nonassignable native
    FileSink.write using an actual inert child. A transparent receiver-bound sink
    and native-child proxy fixed observation without synthetic execution. A second
    fresh diagnostic attempt overlapped that source edit, so its failure counters
    are NOT assigned to a frozen runtime hash. Both failed attempts remain separate
    and were never filled in, restarted or closed as sessions. Fixed counters:
    hub RPC4/verify1/begin0/finish0/settle0/unknown0; runner spawn1/exit0/result0/unknown1.
11. Fresh quiescent runtimeecc19a/orchestratorceaffb passed the actual20 run in
    4.112s, collected1/8. Before/after implementation fingerprints are now retained
    in the source/policy checkpoint to prevent ambiguous overlapping-source PASS.
12. A final genuine journal Red placed private prose in the known diagnostic-stage
    field. Exact typed values/fixed stages now reject it before writing; this is
    distinct from the earlier unknown-key Red. Directory/file sync and exclusive
    0600 closed-artifact retention are included. No arbitrary storage-fault
    recovery or resumption guarantee is claimed.

## Final focused verification

Final author command (with approved owned-loopback permission):

```sh
bun --no-env-file test scripts/e2e-gateway-session.bun.test.ts
```

Result:24PASS/0FAIL,102 assertions,12.17s. This includes actual zero-paid hub boot,
malformed control EOF, actual20 run4.038s, stalled committed probe exit92~4.081s
and actual-parent-death disappearance~219ms. The orphan observation includes
bounded exact OS start/command/initial-parent identity and rechecks identity before
any fallback signal. It does not claim direct orphan reaping. B9's independent
author-owned private guard supplement and native-sink checks are separately
reported in task-12-runtime-report.md; do not add their counts to this file's24.

Exact nested TypeScript program used the repository's parsed root options with
only scripts/e2e-gateway-session.ts, its collected Bun test and the runtime fixture
as roots:0 diagnostics. This includes transitive contracts, not every repository
root. The parent's whole-repository gate was not run by this author.

## Actual retained evidence and limits

Final successful author run contains87 ordered private journal records: intent1,
sources1,open1,call20,signer20,paid20,result20,close1,closed1,cleanup1,audit1.
Each real call uses the same fixed childless probe/version at10000 atomic; budget
and spent are200000, held0. Twenty distinct UUID references are Gateway transfer
categories only, not mined transactions/batches or recipient available credit.

Actual cryptographic signer entries and paid forwards are separately counted.
The real Gateway rail verifies and settles actual locally issued authorization
objects against a finite synthetic external boundary. Production hub boot,
authenticated WebSocket Hello, Broker dispatch, startDaemon/execSkill and native
script processes are unchanged. Runtime counters independently bind actual20
spawn/exit/results,20 begin/finish and20 verify/settle to original per-index
input/output digests and nonces/UUIDs. All unknown endpoint/request counters must
remain zero. No AI, ENS, canary, tree/child purchase or ERC8004 writer is armed.

Only after runner and hub stop/reap and listener refusal does a separate read-only
SQLite connection require global1/20/20/20 row sets (limits2/21). Strict core and
canonical ledger decoding checks scalar agreement, exact job sets, root lineage,
queued/terminal/receipt digests, explicit authorization nonce, fee0/seller10000,
fixture input/output, temporal correlation and original closed artifact equality.
The reader never boots a mutating Store or repairs/reaps persisted data. Private
closed artifact and original journal are retained; public output is hashes/counts
only. Session/job tokens and signatures never enter these files or child config.

Final successful artifact hashes:

- closed wire:068148df751d71fe39a7676746eb0a7b5990cdce090c204dc38dc6965111fdd2
- persisted bounded rows:c3f9298baa777b495996e76d3120d3887e1e74a6148e428042b2fc905269ae03
- policy:370f7bafd2b199a93b40fde79c6cbcc383c3aa178adca8920b987b4b60f42a4e
- implementation-pair:d3b9f7ba889189ccf716e85a782eea37e1c0c412c5c1006f1843c21f99b87199

Failed private journals and both successful fresh runs remain separate. Their
exact local locators were relayed privately to the parent, not published here.
Tests remove only their own assertion fixtures after cleanup; evidence runs retain
their private directories. This is a cooperative local filesystem/process model,
not an arbitrary malicious-script egress sandbox or hardware/storage-fault proof.

## Source freeze at00:31:46 UTC

| Path | SHA-256 |
| --- | --- |
| scripts/e2e-gateway-session.ts | 4d6893f5a4b50143dd522983baf36a4391374edca3d615fe1ea3ebe1ab7649c7 |
| scripts/e2e-gateway-session.bun.test.ts | 335c11bb39eb7b3f46ef778787297798139989c87729994bf6da50c5d0119b15 |
| scripts/e2e-gateway-session.sh | 3f79d9db3d5437eaf4e40ab61f95ce60b963ed184f8e7b4dd093a3a980f65124 |
| scripts/fixtures/gateway-session-runtime.ts | ecc19a1297a99510b64957c31f692e6069761d3bce2f1f53c124737cf9c510fc |
| scripts/fixtures/gateway-session-probe/arcade.json | d0761fc42d98994caea1f5e34b4dabef623df1482c49bd9f43c2310d57ea7db5 |
| scripts/fixtures/gateway-session-probe/run.ts.txt | 6c5f8c871f62c9320bc88e103a856c6072386e3c277d33050b5f78878edae3c6 |

The preexisting m6-gateway.md115-line prefix SHA-256 is
7d818b7c7a804d3e0ea907004475f2419e7684b84afbd15fa2ba6f8d7b4678c8.
Only the F12 evidence section is appended; F1's dated scope/history is preserved.
Parent/independent review, final public-copy audit, full gate and commit are pending
at this author report freeze. No future completion is claimed here.

## Additive request-header observation correction and final freeze

The preceding 150-line report is preserved byte-for-byte. Its original SHA-256 is
97322c0327199e4c2076734f3f01537e80bbfb212e46df4654602906761b91e1.
All earlier counts and fingerprints remain historical checkpoints, superseded
only where explicitly stated below.

G3 identified an evidence-observation gap: the offline harness did not explicitly
check private-header continuity on every outbound session request. This was a
source-level coverage finding, not a reproduced production F9 authentication
defect. Parent approved a narrow correction in the existing orchestrator/test.
The F12-specific observer captures the actual session ID from the SDK and retains
expected session/job capabilities only in a private closure. It checks exact
owned origin, the same canonical session headers on status/probe/paid/result/close,
per-job result-token continuity, and absence of capabilities on public/open paths.
No raw response body, token, signature or capability is added to journal/output.
Hub authentication remains authoritative; observing the first token is not an
independent proof that the token is valid.

The two new collected groups exercise continuity, missing/changed/foreign/private
header refusals and exact result-job membership. The first new auditor assumed
the initial authenticated request would be an unpaid probe. Actual F9 first reads
private session status. The actual topology therefore gave25PASS/1FAIL/115assert:
the call stopped before signer entry, paid forward, Gateway verification or runner
spawn. A diagnostic repeat used only method, path classification and header names/
lengths, never values; that temporary diagnostic was removed. This was an author
observer integration defect, not a production SDK regression. Allowing the first
canonical token observation on the exact same-session status GET or unpaid probe
fixed the mismatch without permitting paid/close requests to establish authority.

Final focused command remains the same collected Bun file under scoped owned-
loopback permission:26PASS/0FAIL/123assertions/12.22s. Actual twenty-call path4.090s;
zero-paid hub boot1.490s; malformed EOF1.544s; stalled probe exit92~4.072s;
parent-death disappearance~216ms. No live network, operational key or funds moved.
The exact same three-root nested TypeScript program returned0 diagnostics.
These checks ran on orchestrator2c1c3241b12fc27e277106e79f979f573717dce1804305443ecdc2403713347d
and final testcc9c02a89b7e657b82d55eee0e4fce5cfb1338020f4af3c0bd925669caa06662.
Parent then requested one text-only correction: remove the unsupported word
"unfunded" from the public deterministic fixture-identity comment. No behavior
changed. Final executable/source freeze is:

| Path | SHA-256 |
| --- | --- |
| scripts/e2e-gateway-session.ts | 2f78e4a1bae87689beae6de74dbce6b3c295b700c827481e3c11056460f56b07 |
| scripts/e2e-gateway-session.bun.test.ts | cc9c02a89b7e657b82d55eee0e4fce5cfb1338020f4af3c0bd925669caa06662 |
| scripts/e2e-gateway-session.sh | 3f79d9db3d5437eaf4e40ab61f95ce60b963ed184f8e7b4dd093a3a980f65124 |
| scripts/fixtures/gateway-session-runtime.ts | ecc19a1297a99510b64957c31f692e6069761d3bce2f1f53c124737cf9c510fc |
| scripts/fixtures/gateway-session-probe/arcade.json | d0761fc42d98994caea1f5e34b4dabef623df1482c49bd9f43c2310d57ea7db5 |
| scripts/fixtures/gateway-session-probe/run.ts.txt | 6c5f8c871f62c9320bc88e103a856c6072386e3c277d33050b5f78878edae3c6 |

The launcher content stayed unchanged; parent made its mode executable755.
Public evidence now reports26/123 and the in-memory header observation boundary;
the original115-line F1 prefix remains untouched.

Separately, before this header correction, parent invoked the executable launcher
on frozen orchestrator4d6893/test335c11: actual offline20PASS/exit0~5.16s. Parent
then independently rebuilt evidence after CLI exit using87 actual journal events,
20 result checkpoints, the original closed artifact and global read-only SQLite.
The rebuilt closed hash was
e415c91b77b868e69ed63536155d0fcc1a3b1101dbe120498461b441ab16fa4a,
persisted hash74ffb2acfed6964568aa76fad23c69cb894848f7af41eaa2ef2d613ddba51512,
and journal head9f2d612746f7a065317e070b3f83acddbd4451ea90e82bc1eb22ec0b87500c50.
This parent repetition is attributed to its earlier source checkpoint, not the
later header observer. Private artifact locations remain excluded here.

Parent and independent final review/full gate/publication/commit are separately
owned. This author is stopped at the six-file source freeze and does not claim
their future completion. No additional runtime execution is needed by this author.

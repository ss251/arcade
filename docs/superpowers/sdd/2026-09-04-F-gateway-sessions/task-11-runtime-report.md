> Sanitized historical execution artifact. Statements reflect their recorded checkpoint and may be superseded. Historical commands are not current instructions. See the [current runbook](../../../runbook.md) for current behavior, approvals and operator commands. Personal/runtime locations and private artifact links may be redacted; public evidence and test distinctions are preserved.

# F11 journal/runtime author chronology

September 6, 2026. WORK IN PROGRESS; source is not frozen or accepted.
Released ownership is exactly the Bun-only funding journal/runtime and their one
collected Bun test. No F1, shared modules, dependencies, Git, full gate, live
network, operational key, signing service, deposit, burn or mint is used here.

The complete current Task11/global constraints, parent decisions, source handoff,
implementation split, funding readiness, protocol evidence, independent protocol
and identity reviews were read. Retained deployment projection/validator/UUPS/
compiler settings and actual F1 runtime/journal/entry/tests were inspected, never
executed. Unsafe literal plan retry/default/instant-withdrawal examples remain
superseded. The ts-testing skill governs genuine regression-first checks and
separate attribution of setup failures versus observed behavioral defects.

## Initial journal checkpoint

- Six account-journal cases were written first. Initial exact Bun command failed
  with missing gateway-funding-journal.ts: zero collected cases, one loader
  error. This is a missing-module setup Red, not six reproduced behavioral bugs.
- After implementing the account-wide O_EXCL claim, permission/NOFOLLOW checks,
  facts-only hash-chain journal, request/digest binding and close retention,
  the exact owned file passed 6 Bun cases / 16 assertions. These use actual
  temporary filesystem paths and competing promises, not process-crash proof.
- Pure contract coordination added the original closed request and operationId
  to the journal header, permitting the actual operation digest to be recomputed
  during read-only recovery. The namespace is independent of journal filename.
- Positive identity test material must be exact retained upstream bytecode;
  no trusted-hash override or injected preflight-success seam is permitted.

Pending: fault/ordering/restart/owned-process tests, finalized close policy,
runtime implementation and actual wire validation, focused exact strict checks,
independent review and parent full gate. No finished F11 or live claim.

## Journal correction checkpoint

Subsequent genuine draft Reds preserved in collected tests: hostile expected-head
getter leaked its sentinel and next intent changed the planned amount (6 pass,
2 fail); submitted hash changed from prepared, operationId changed despite copied
digest, and prior signer-entry was later called unsigned (8 pass, 3 fail).
Fixed checks gave 11 pass / 24 assertions. Six actual partial-write/sync/close/
witness fault cases initially had a namespace-fixture typo (colon vs hyphen),
not a product Red. Corrected filesystem cases, causal close witness and stale
head cases reached 19 pass / 67. Factory absence was separately a missing-API
failure; implementation reached 20 pass / 73 with cancelled no-IO construction.

Independent G3 checkpoint ca4f982bd8abc39b326ea44f8a1119be8e697b270bab096cb6a9bedb6f88387b
reproduced three genuine failures in six private tests: first burn fee escalation,
missing finite burn coordinates, and planned-only signed finalization accepting a
trusted verifier result. This is a journal prerequisite failure at a trusted
runtime seam, not a demonstrated production credit-verifier bypass. Author reran
the unchanged fixture (SHA36b490d17888a439f7b77c764374062ca74be2888409c4ec122aa6b6eee406d1),
then bound first burn to exact request fee/finite planned height/deterministic
spec and checked prepared stage/hash/nonce/gas/calldata before verifier entry.

Corrected journal SHAa39d8681027a62e81ae49b719603b88745c8a9df9b5d1a3d5985cbe24b7924f1
passed unchanged private six plus collected account-wide22: 28 pass / 98
assertions and journal-only exact nested strict0. Three collected cases include
verifier-never-called planned-only and a positive exact prepared/lost-ACK case
with complete trusted effects and a synthetically absent witness. The latter is
not a process-crash experiment. Submitted acknowledgement is deliberately NOT
required: the send reply/ack write can be lost. No second send is made. Complete
prepared facts alone do not prove credit; the production deposit verifier cannot
prove transaction-correlated API credit from the retained pending-only protocol.

The cooperative filesystem model retains account exclusion and best-effort
poison after partial write/fsync/close failures. It is not hostile-same-user,
cross-machine or arbitrary-storage-fault recovery. A failed poison write and
crash during filesystem effects can remain ambiguous; no automatic repair,
truncate, takeover or unlink follows. Witness creation is causally after MAIN
journal close, not an infinite claim about every future marker close. Signed
complete effects can substitute for absent witness only with exact prepared
transcript; unsigned refusal/noop always need the exact witness.

## Runtime predicates and genuine corrections

The production adapter consumes unknown bounded JSON/RPC results and invokes the
actual pinned deployment predicate, pure request/snapshot policy and viem
transaction/signature parsers/recovery. There is no preflightPassed or trusted
success callback in the production runtime. Its account signer is lazy and
separate from the send boundary. Factories/imports do no IO.

Offline fixture construction was explicitly approved by parent from the public
all-0x11 synthetic fixture key, never environment/Keychain or an operational
account. Static bytes were then embedded in the owned test; tests never sign
dynamically. The fixture account is0x19e7e376e7c213b7e7e7e46cc70a5dd086daff2a.
All three approval/deposit vectors use chain5042002, value0, gas21000,
maxFeePerGas1, maxPriorityFeePerGas1 and100 atomic USDC. Gas21000 is a synthetic
parsing/authority fixture, not a claim of real contract execution affordability.
Approval nonce0 targets USDC approve(Wallet,100); deposits nonce0/1 target the
Wallet deposit(USDC,100). Hashes:

- approval:0x3dcac8e0af2f16112a0e8fdd17299e2dbe525382324100640ac06bde956bd9b4
- deposit nonce0:0x7f221aa3b6cd5b6936c90ecdbf70ab0feb079241f6c61dc84ae28076def7c4e2
- deposit nonce1:0x33f01a5ce8d2b772e9ed09a835ca1a6cd24d04e3a1b5db4e5f37e594483a73f7

Withdrawal static construction uses the same synthetic account, operation
op_22222222222222222222222222222222, amount100, maxFee10, gasCap42000,
maxBurnBlockDelta20, source block1000 and captured height1020. The same-chain
self-recipient exact spec has zero caller and empty hook; salt is the actual
deterministic operation-digest derivation. Domain is GatewayWallet/version1,
without F2 chainId/verifyingContract. Attester uses the same synthetic fixture
account and personal-signs raw32-byte keccak(payload). Mint nonce0/gas21000/
fee1/value0 calls exact gatewayMint(payload,signature). Relevant hashes:

- operation:0x134580c1b42cdb704218f20d7551df4cd67b2dbae8b8f3d225f2ad0d55a3fa15
- spec:0xcbd5e6af3c550d3357069419d150f4a5dbae83b5abd137e5a2938f778d600d6f
- mint:0x4729ba8803780df9691dcd1b7b7b065cc35311bbd037dbdd57cdc831966ce040

The first copied transaction literals had a transcription/hex-length error;
three runtime cases therefore stopped before send. This was corrected by exact
programmatic apply_patch insertion from the same approved offline construction,
not called a production defect. After that setup correction the actual unchanged
runtime produced three genuine Reds: an unrelated available increase returned
confirmed; exact initial amount-plus-aggregate-gas funds refused deposit after
approval; historic Wallet code mismatch still returned deposit credit_pending.
The allowance-aware injected estimator already refuses deposit estimates before
actual synthetic approval; this sequence is exercised rather than returning
unconditional estimates. Corrections preserve the original request/plan while
checking remaining aggregate authorized gas, always label exact deposit proof
credit_pending, and verify pinned historical Wallet code at the receipt block.

Two exact Content-Length discrepancies (declared1 and99 vs actual JSON bytes)
were accepted; an ignored-abort late fetch body was never cancelled. All three
were genuine transport Reds. Runtime now requires exact declared byte equality
and explicitly cancels a late response's body. No raw response, URL, provider
diagnostic or cancellation reason is projected. Existing total5-second HTTP,
16KiB request/256KiB response, byte/UTF8/identity-encoding,1024 consecutive-empty
chunk, no-redirect/no-retry bounds are retained.

Four more genuine runtime Reds: high-s plus flipped-v burn and attester
signatures each reached mint_ready; revoking current attester membership at
pre-sign or pre-send still reached destination delivery. Canonical nonzero
r<curve-order, low-s and v27/28 are now checked before actual recovery. Current
attester membership/mint permission are checked at both final transaction gates,
including a fresh gate after asynchronous lazy signer acquisition. Source,
spec/amount, fee, height, nonce/gas, exact calldata and signer equality remain
separate checks. Supplemental receipt-block Minter mismatch coverage refuses
delivery even when current source identity is good; this is a new positive
regression assertion over the already-corrected shared historical predicate,
not attributed as a separately run pre-fix Red.

Actual viem parsing/recovery of the embedded synthetic transactions is exercised
through approval, deposit and mint. Separate synthetic receipt/token/attestation
events demonstrate delivery_pending versus full source value+fee debit
correlation, including keyless read-only reconciliation then explicit retirement.
Lost send acknowledgement retains the locally derived prepared hash, forbids
repeat execution, and permits only exact keyless deposit reconciliation with
credit_pending. Finalization refuses that unproved API credit. Concurrent
execution/cancelled late signer result cannot call the send adapter.

## Final bounded ownership and recovery corrections

G3 independently reproduced a late-open resource leak on runtime
78403107c950be4afd353311b95c73fbdf76c05dbeff920f4dd32ae0dd0a9c70:
pause an actual header_sync, cancel execute, close facade, then release IO. Main
FileHandle remained open despite close reporting success; no signer/RPC/send
entry occurred. Author added and ran a genuine collected Red (close count0).
The opening promise now adopts the real resource itself, independently of its
raced await. Late completion always attempts owned main close. If opening has
not resolved inside the cleanup budget, close reports fixed uncertainty instead
of success; its late cleanup continuation remains responsible for the handle.
The collected test observes actual FileHandle.stat failure after release and
retained account exclusion, not merely that a close function was called.

This is cooperative local filesystem lifecycle evidence, not process-kill or
arbitrary OS-IO cancellation proof. Journal syscalls may remain pending past a
caller timeout; the production CLI's independently owned hard process fuse is
outside this author slice. No claim is made that a timeout cancels underlying
filesystem/provider work or that every failed poison write is recoverable.

Final self-audit found the approved same-process retained normal UUID recovery
path missing: a genuine collected case with an uncertain mint returned uncertain
without performing its documented GET. Parent approved completing this existing
contract within the same runtime/test files. Read-only recovery now uses only
the private retained validated UUID, validates nested original intent summary,
fee, forwarding=false, exact already-held payload/signature/expiry and current
attester identity, and returns observed public hashes. It does not change
publicState, uncertainty, capability, journal or any mutation latch. A changed
fee refuses. Gateway status/transactionHash remain provider observations, not
independent mining or source-debit proof. No UUID flag, file resurrection, new
mint capability, POST retry or automatic next stage exists. Complete reply loss
or process loss of the UUID remains non-resumable through the documented API.

## Frozen author verification and API

Final owned source/tests frozen; only this report was appended after hashing.
Exact command:

`bun --no-env-file test packages/buyer/test/gateway-funding-runtime.bun.test.ts [private standalone journal regression fixture]`

Result48 PASS/186 assertions: collected42/165 plus unchanged private6/21. The
private file is an independent non-collected regression fixture, not a new
ordinary test-discovery claim. Its SHA remains
36b490d17888a439f7b77c764374062ca74be2888409c4ec122aa6b6eee406d1.

Fileless TypeScript createProgram using root tsconfig parsed options/noEmit and
the three exact owned roots reports0 diagnostics. It traverses imports, rather
than excluding dependency diagnostics. Initial contextual/optional-field/hex
narrowing diagnostics were corrected without changing public legacy types;
test getter/preconnect/template annotations preserve actual assertions.

Frozen SHA-256 inventory:

| Owned path | SHA-256 |
| --- | --- |
| packages/buyer/src/gateway-funding-journal.ts | a39d8681027a62e81ae49b719603b88745c8a9df9b5d1a3d5985cbe24b7924f1 |
| packages/buyer/src/gateway-funding-runtime.ts | 2b6248aed6beb2541a634cbbb77b757b69dad51d4d048ebe7510834646e6639d |
| packages/buyer/test/gateway-funding-runtime.bun.test.ts | 3525b1095c5ec8f90a23d44c1be8828957be682d024a0ae27e8d3a1ae58c3a52 |

The captured public runtime exports are createFundingDependencies,
inspectFunding, createFundingOperation, reconcileFundingOperation and
finalizeFundingOperation. Operation methods are no-argument executeDepositOnce,
requestWithdrawalOnce, mintWithdrawalOnce, reconcileOperationReadOnly,
publicState and close. Initial publicState is undefined. FundingSigner exposes
only exact address, signTransaction(FundingTransaction,signal), and
signTypedData(BurnIntentTypedData,signal); the actual sender is recovered again.
Explicit finalization can confirm local unsigned-refusal retirement; its
terminalKind is unsigned_refusal and is not a funding-completion claim.

No additional source paths, shared core/payments, legacy F1 script, operational
credential, live transaction, external network or provider experiment was used.
No standalone child-process or native-loopback transport claim is made for this
author file: its real boundaries are temp filesystem operations, ignored-abort
streams/Promises and injected finite RPC replies validated by production code.
Independent journal correction review is CLEAN; final late-open review, parent
combined integration/full gates, public scrub/publication and commit are owned
by root and remain separate from these author results.

> Sanitized historical execution artifact. Statements reflect their recorded checkpoint and may be superseded. Historical commands are not current instructions. See the [current runbook](../../../runbook.md) for current behavior, approvals and operator commands. Personal/runtime locations and private artifact links may be redacted; public evidence and test distinctions are preserved.

# F11 parent readiness decisions — September 6, 2026

## Deposit-credit attribution limitation — 04:42 IST

Parent and runtime author independently read the already-retained OpenAPI
/v1/deposits operation194–217 and response1605–1661. It returns only pending
rows bound to depositor/domain/transactionHash/atomic amount/blockHeight/hash/
timestamp. It has no finalized status or logIndex; parent's earlier message
shorthand mentioning logIndex was corrected before a field was implemented.

An increase in available balance may be an unrelated prior credit; absence or
disappearance from a pending-only list is not a positive transaction-correlated
credit proof. Do not relabel either as full deposit credit or signed-terminal
retirement. After independently proving the exact onchain deposit, return
credit_pending with known transaction and separately observed available facts
when attribution is unproved. This does NOT mean observed available funds are
unusable; the pending category concerns proof and claim retirement. Production
credited finalization stays unavailable without supported exact attribution.
No automatic redeposit/retry, fabricated endpoint/status, owner request or live
API experiment follows. This is an explicit F11 acceptance limitation; record
it honestly and continue other local tasks after the source/test gates.

The source-linked current Minter mismatch independently continues to prevent
withdrawal signing/normal-transfer POST. Offline withdrawal stages and tests may
still be completed with pinned expected artifacts and synthetic transactions.
Neither technical limitation is a reason to replay any consumed live approval.

## Consumer details and one test-fixture expansion — 04:19 IST

Follow-up: accept observed as the closed keyless-inspection outcome. Only the
balance/inspection command treats it as read success; it is not a mutation or
finalization success. Synthetic prebuilt transaction tests may construct fixed
bytes once offline from the explicit public all-0x11 fixture key, then consume
literals. No operational key/environment/network/send is involved; report exact
synthetic construction parameters without treating them as live evidence.

For signed-complete finalization, a missing clean-close witness may be replaced
only by independently proved exact terminal effects plus the complete matching
stage/hash journal and claim. Deposit requires actual full credit; withdrawal
requires destination delivery and correlated source debit/fee. A receipt alone,
uncorrelated balance increase, process death or partial journal is insufficient.
Durable poison still refuses; unsigned-refusal/noop still require their witness.
This permits explicit keyless terminal reconciliation after process loss without
opening any mutation/resumption/repair path.

Release one additional G3-owned test-only path,
packages/buyer/test/fixtures/gateway-deployment.ts. Positive identity tests need
the exact pinned unbound runtime bytes; the retained projection contains hashes
only. Parent will retrieve the same three pinned upstream artifacts read-only,
verify their existing file hashes, and retain a derived fixture input in owned
temporary storage. G3 supplies provenance/license attribution and declared
immutable offsets; no new RPC, observed-code trust baseline, hash override or
production runtime dependency on this fixture. Total source/doc/test scope is
now fourteen disjoint paths. Prior approval assumed full bytes remained in the
reader's memory; that assumption was corrected before any fixture was invented.

Accept the bounded batch session command backed by the actual F9 Promise API,
with explicit hub/address/rail/input and bounded call count. Seller means the
canonical serviceName, not wallet address. No new interactive stdin protocol or
capability file. A private in-process controller can expose deliberate status
and read-only closed-artifact recovery; after process exit its lost capability
is not recoverable from a public ID. Close once only after all calls succeed;
uncertainty does not authorize close, reopen, paid replay or deposit.

An explicit fully bounded gateway-withdraw command authorizes both separate
one-shot request and mint method invocations. Invoke mint only after validated
mint_ready, never after uncertainty. Do not add a request-only CLI which knowingly
discards the sole capability, or a second interactive approval protocol. Add
mint_ready to the closed public outcome union with fixed facts/hashes only;
publicState may be undefined before execution rather than a fictitious refusal.
These are offline command semantics, not approval to execute with real keys.

Accept a clean-close witness created only after the main journal handle's close
has successfully returned, bound to exact claim/head and complete terminal
journal. A pre-close checkpoint is not proof of close. For unsigned-refusal/noop
finalization require this witness, no poison marker and independent bounded
validation. Its causal assertion concerns the main journal handle, not recursive
proof that every later witness/finalizer close succeeded. Any witness creation,
write, fsync or close failure poisons the current lease, refuses current
finalization, retains the claim and attempts a durable poison marker. Document
the cooperative local filesystem model and residual crash/failed-poison limits;
do not promise arbitrary-storage-fault recovery. Missing witness/process death
is never proof and no error/close/reconcile path finalizes automatically.

## Source release after F10 commit

F10 committed dc86bf1e671abe0b2b5bd4bb9cefbb18dcd229ab at04:03:50IST after
the full2608Vitest/433Bun/root-web strict gate and independent source/public
reviews. Parent now releases the existing nine source/doc plus four test paths:
G3 owns pure policy/codecs and their two Vitest files; G14 owns journal/runtime
and their one Bun file; B9 owns the five CLI/session/entry/script/docs paths and
their one Bun file. All ownership is disjoint. Pure exported contracts freeze
first with consumers before implementation diverges; no independent renaming.

Accept the prepared synchronous createFundingOperation({authority, operationId,
request, journalPath}, deps) facade, with one-shot executeDepositOnce,
requestWithdrawalOnce, mintWithdrawalOnce, bounded reconcileOperationReadOnly,
publicState and close. Independent inspectFunding and explicit
finalizeFundingOperation stay keyless. Runtime owns bounded RPC/HTTP construction
and real decoding/validation; CLI supplies only the lazy signer boundary and
consumes closed public outcomes. No preflightPassed callback or stub success.

Require an explicit expected --address for mutating funding commands as well as
read-only commands. The canonical account is then known before the fixed claim
and asynchronous preparation; a later lazy signer must match it. Key material
still comes only from the consuming process's approved role environment, never
a CLI flag, output, journal, report or test's operational environment. This is
an implementation contract, not approval to acquire keys or run a mutation here.

All tests remain synthetic/offline: pure unsigned vectors, prebuilt synthetic
signature bytes where needed, injected finite transports, owned temporary files
and fully reaped no-env-file processes. Root alone runs full repository gates,
independent cross-slice review/publication and the later atomic F11 commit.
No live funding/session, operational keys, new spending, owner request, consumed
approval replay, dependency change, arbitrary extra source path, merge or push.
The Minter identity mismatch remains a fail-closed technical prerequisite; other
offline/deposit/session work proceeds without pretending withdrawal is live-ready.

## Concrete deployment identity accepted — 03:52 IST

Parent fully read the complete task11-deployment-identity-review and its retained
primary supplement (cd35b937ce9ee1ad9e45f378f8bd47b3ee38aa00104d617bcb448081c58101f3),
the full201-line upstream validator,56-line split compiler script,38-line Foundry
settings,153-line pinned UUPS source and239-line derived artifact projection.
These are reviewed primary-source-linked artifacts, not an independently repeated
compiler build. The independent reader's nine public code/storage calls are not
repeated by parent and do not constitute a funded test or owner-only prerequisite.

Approve a frozen pure deployment record in gateway-funding.ts, within the existing
path scope. Pin the already selected chain/domain/proxy addresses, exact ERC1967
slot,163-byte whole proxy Keccak874a21508289bf01ee9802032607ae53ba0a4f47f28143a8493921b8db102187,
Wallet22818-byte unbound Keccakbe79daa02f5d5359b4f6118d70b72e10ddd1c87b63e9e4ee39fdafa9bb2c2cbf
with self-word byte offsets3708/4382, and Minter11528-byte unbound
Keccak3b600440271648adb765c4dea90282c6d4e8fd317ee693c4804f31cc0ecb5f61
with self-word offsets1683/1982. All hashes are32-byte0x-prefixed when encoded.
Require canonical resolved implementation addresses distinct from their proxy;
each declared32-byte word must equal twelve zero bytes plus that implementation.
Only then zero those exact words in an owned copy and compare the entire runtime
length/hash, including metadata. Never global-search/replace, strip metadata,
accept provider-supplied offsets, or promote an observed new hash into trust.

Deposit's code identity gate requires the Wallet proxy/implementation only;
withdrawal requires both Wallet and Minter. This separates the independently
supported deposit leg without claiming a working current withdrawal path. Source
and tests can implement both predicates/stages offline. At the observed block,
the deployed Minter12101-byte implementation did not match the trusted11528-byte
artifact; runtime withdrawal must refuse as deployment_identity_unavailable
before any signing or normal-transfer POST. Do not relax the predicate or claim
current live withdrawal acceptance. Resolving the exact source/build delta is a
technical follow-up, not permission for a release search or an owner request.

Runtime reads use a fresh finalized block, exact chain equality and number/hash
correlation (or supported canonical block-hash reads), bounded code/slot responses
and a final block hash recheck. Recheck current proxy/implementation identity at
the last bounded pre-sign/pre-send gate; any change refuses the frozen operation
instead of rewriting its plan. This narrows an upgrade race but cannot promise
that an upgradeable contract remains unchanged after the check/transaction.
Token/domain/pause/delay/attester/fee/balance/effect predicates remain separate;
code equality alone never means a deposit, burn or mint will succeed. No RPC
account query, credential, signing or spending is authorized by these decisions.

## Implementation split accepted — 03:40 IST

Parent fully read the185-line task11-implementation-split.md, SHA-256
6250c6ecea88c2daeef8d56b9bc78920cf5ad6076867e5ddb1b043670f7c906a.
Accept the three disjoint slices within the existing nine source/doc plus four
test paths: pure policy/codecs; Bun journal/runtime; strict CLI/session adapter
and docs. F11 source remains held until F10's reviewed commit. No new source
path, dependency, credential acquisition or live execution follows this note.

Freeze the production claim namespace as the current OS user's home record
(node:os userInfo().homedir, not HOME/CLI configuration), followed by
.arcade-gateway-funding/v1/eip155-5042002/<canonical-lowercase-account>/active.claim.
Resolve and validate the OS home identity once; require the application root and
its descendants to be owned0700 real directories, and the exclusive no-follow
claim to be a regular owned0600 file. No caller journal path, cwd, environment
variable or operation ID can select another namespace. An injected test-only IO
adapter may use its own temporary root; never expose that override in production
CLI flags. This is cooperative same-OS-account exclusion, not cross-machine or
hostile-same-user protection. No production directory is created during tests.

Accept the synchronous captured createFundingOperation facade and independent
keyless inspectFunding, with named one-shot stage methods, bounded read-only
reconciliation, publicState and close. Close does not unlock. Lazy signer
acquisition and distinct sign/send callbacks preserve the write-ahead boundary;
unknown results retain the active claim and forbid later mutations. Public
outcomes/events are a closed facts-only projection, never provider objects.

An explicit gateway-finalize --address ADDRESS --journal PATH command may perform
the separate signer-free local terminal finalization. It must revalidate the
exact existing claim/journal identity and terminal evidence through bounded reads;
it cannot mutate chain state, resume a stage, repair/truncate a journal, infer
success from process death/absence, or unlock a still-pending/uncertain operation.
Normal execute, close, startup and reconcile commands do not call finalization.
Only validated zero-shortfall no-op, fully credited deposit, or separately proved
withdrawal destination delivery plus source debit/fee completion qualify. Record
the finalization fact durably before retiring only the matching claim; any
ambiguous retirement remains an error and must not be presented as a clean lease.

One explicitly finalizable local terminal category also covers a validated
unsigned preflight refusal: the complete durable journal must prove that no
approval/deposit/burn/mint preparation intent, signer entry or send boundary was
ever entered, and the lease must not be poisoned. This avoids making a known
pre-sign configuration refusal permanently own the account. A missing record,
partial write, prepared intent, unknown provider/signing result or ambiguous
close is not that proof and cannot enter this category. Finalization remains an
explicit separate command, never automatic cleanup on an error path.

Nonempty deployed code is not source identity. G3 is doing a bounded independent
read-only check of the retained deployment/code/proxy evidence. Freeze the actual
identity predicate separately before enabling the runtime mutation gate; missing
evidence must produce fixed unavailable/refused outcomes, never stub success.
This is a technical prerequisite, not an owner-only item or funding request.

## Source handoff acceptance — 02:58 IST

Parent fully read task11-source-handoff.md SHA-256
d2e9e7501c2540a44e8729a1c7cacc785faf7a974af621d08b215bfb0415d4e7.
Accept its proposed nine source/doc paths plus four explicit test paths as the
starting implementation scope after F9/F10 gates. No source release yet. Pure
policy/packed codecs, Bun-only account claim journal and injected runtime, strict
import-safe entry points are separately reviewable slices; session-cli consumes
the then-frozen SDK only. Raw mint calldata contains capability material and must
not enter facts journals, output or diagnostic paths. Preserve exact bounded
amount/gas/fee/height authority, one-send write-ahead stages and separate delivery,
source debit and credit evidence. No live execution, signing with operational keys,
deposit/withdrawal retry, owner request or re-use of F1 authority is approved.

Parent read the full 290-line task11-funding-readiness.md (SHA-256
993b3c204477c84f0a97b0a8a68b4f9f5cc85db61c1b1e0a45609530390fb06a).
Source is not released before the preceding F6–10 commits. Accept its explicit
funding-independent boundary and no-auto-funding rules, with the following scope
and evidence constraints. No original F1 source/journal or consumed approval may
be broadened or replayed.

Exact deposit and target-available shortfall are distinct explicit modes. Both
must use captured pinned authority, exact bigint amounts, adequate allowance,
bounded aggregate gas and a separately versioned durable one-shot operation
journal. No SDK convenience retry or receipt waiter. Uncertain submission retains
known hash/intent and permits only bounded read-only reconciliation, not another
signature, broadcast or automatic stage continuation. A zero target shortfall
is only a validated no-op; API available, pending credit and wallet funds differ.

Import/help/read-only/invalid commands must not acquire a signer. New strict CLI
paths should be import-safe and avoid the legacy parser's reflected diagnostics,
without an unrelated rewrite. No default movement, withdraw-all, arbitrary payee,
chain bridge, funding-on-error, mainnet flip, key flag or MCP funding tool.
Browser-safe buyer exports remain separate from Bun-only filesystem/credential IO.

Withdrawal is a separate BurnIntent/attestation/mint protocol, not F2 payment
signing and not a simple on-chain wallet withdrawal. Before releasing its mutating
implementation, resolve pinned Minter identity, actual attestation-to-intent
validation, finite authority and exact fee semantics using authoritative source/
protocol evidence and offline fixtures. Determine whether lost-response evidence
has a documented read-only recovery route; absent one, retain honest unrecoverable
uncertainty rather than inventing a route or automatic fallback. Explicitly review
any need for private capability persistence; F1's facts-only journal cannot be
silently changed into a bearer-signature store. Current source-inspection gaps are
technical work, not an owner request or permission to run the SDK helper.

Deposit/read-only machinery and the frozen-F9-backed session CLI can progress
independently. Do not claim full withdrawal acceptance or return a stub success
if the protocol prerequisites remain unresolved. The session CLI must stop on
uncertainty, retain capability privately, and use read-only closed-artifact recovery
without automatic close, reopen, deposit or paid replay. Exact output/transport
contract follows the real F9 implementation, not the stale plan snippet.

Current no-new-spending instruction remains controlling even after local source
verification. No implementation decision grants gas, approve, deposit, transfer,
burn, mint, withdrawal, live session or credential access. Record F12 live as not
run; implement and verify unblocked local tasks. Do not prompt the owner here.

## Primary-source protocol follow-up — September 6, 2026

Parent read complete task11-withdrawal-protocol-evidence.md (SHA-256
d7ff5adf0c84d8b7a9c3e467c7f7661bfa36de195bd2acd4b0b8b650839a3544).
The source-pinned packed spec/attestation codecs, separate EIP-191 attester
recovery and current onchain membership, value-plus-fee arithmetic and distinct
source/destination expiry semantics now support later offline implementation.
This does not verify deployed bytecode, current signer/delay/fees or live wire data;
retain those as bounded runtime read-only gates before any future authorized send.
Main must check the relevant pinned source/vector when that implementation releases.

Choose the smallest default capability policy: facts-only operation journal, no
normal-transfer UUID or attestation/signature persisted or printed, and explicit
non-resumability after losing the in-memory recovery capability. A completely lost
POST response remains unresolved: no documented spec-hash-to-UUID/attestation
lookup, no replay or new salt. A later optional private recovery-record feature is
out of this default scope unless explicitly reviewed; do not smuggle bearer-capable
bytes into the existing journal. Normal withdrawal UUIDs are not the same evidence
category as public x402 payment transfer UUIDs.

Finite maximum burn-block delta, fee cap and aggregate gas ceilings must be
explicit validated operator inputs, with no newly invented numeric defaults.
Compare the captured finite height to fresh source height plus actual withdrawal
delay and the supplied maximum delta; never convert an assumed block time into
authority or extend the signed plan silently. Such CLI/API policy parameters can
be implemented/tested offline now when F11 releases; they do not require an owner
question or grant permission to execute here. Future live instructions must supply
and authorize their exact values. Keep one-shot burn and mint as separate stages,
with independent destination delivery versus source debit/fee evidence.

Use the documented nested transfer recovery shape intentionally; do not copy the
contradictory OpenAPI top-level required list into a falsely authoritative decoder.
Only a retained validated UUID permits that read-only recovery; spec lookup alone
does not recover mint authority. No new live run, keys or spending are authorized.

## Independent pinned-source/vector audit accepted

Parent fully read final task11-independent-protocol-review, SHA-256
afc146a776f497dd374cddcd731c5002fe2dab3d8a77c9690d5d9c533734d334.
CLEAN within stated source/unsigned scope: four exact packed lengths/hashes,
15 field offsets and four recomputed magic values, plus actual attester/expiry/
UUID-recovery rules. This is not acceptance of future F11 source or a live run.

The initially incomplete flat public-source scratch was supplemented by parent
read-only retrieval of Mints.sol, TransferSpec.sol, Attestations.sol, BurnIntents.sol
and BurnIntentLib.sol at the same pinned fd51093c7a1ba8e50ea2c6029ebf1bdc2bb2b8e8.
Parent read all of Mints and the three definitions plus the exact burn encoder;
reviewer independently matched all five file hashes and read those definitions.
No codec-definition provenance gap remains, but no complete Solidity build,
dependency audit, deployed proxy/code match or current chain/API state was proved.

Important source/comment distinction: Mints' same-domain implementation checks
source token equality, not the source-contract consistency mentioned in its
comment. Keep exact full captured spec-byte binding locally. Recover attester
from the raw32-byte payload hash with the Ethereum message prefix, not hex-string
text or the raw payload. Current signer membership remains mutable and must be
read against the exact pinned runtime contract before any future authorized mint.
No new credentials, authority, owner question, withdrawal or spending follows.

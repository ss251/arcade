> Sanitized historical execution artifact. Statements reflect their recorded checkpoint and may be superseded. Historical commands are not current instructions. See the [current runbook](../../../runbook.md) for current behavior, approvals and operator commands. Personal/runtime locations and private artifact links may be redacted; public evidence and test distinctions are preserved.

# F8 executor — parent independent review

September 6, 2026, 02:14 IST. CLEAN for the two-file executor slice, not yet the
separate paid router or the eventual combined gate. Parent read both full source
and all48 test cases, the complete author report at SHA-256
727fdccc38d56209b562efd595b7a0a222903b49108e11268ac85b98831f497b,
the pure helpers and relevant unchanged legacy lineage/pipeline contracts.

One issued attempt claims before executing the runner. Duplicate rejection is
outside owner cleanup, so concurrent evaluation cannot mark the active owner's
hold uncertain. The durable F5 begin result independently guards the sole settle
attempt. Post-begin ambiguity or failure remains held, including failed atomic
finish. The 50ms uncertainty marker is explicitly interruptible inside onError's
otherwise uninterruptible cleanup; it cannot erase liability. Interruption causes
are preserved. A pre-begin interruption/defect may leave a reserved hold; no
restart replay, automatic release, retry or external reconciliation is claimed.

Captured hire capability and copied flat D coordinates cannot be changed by
later mutation of the options object. Service/rail implementations are trusted
internal dependencies, not hostile-object sandboxes. Atomic terminal persistence
precedes output/D enqueue. EIP uses actual bound payTo; Gateway/Test skip D. Both
dispatch and settlement timeouts and D/marker deadlines have executable tests.
Known failed cost may survive a fixed release; failed output/diagnostic does not.

EIP-V2 child evidence uses the existing separate A global receipt API only for
actual nonempty trees. It checks unique matching descendants, state, amount,
rail/network/reference and flat ancestry chain, reserved sum and ceiling. No
synthetic child/refund/dual-ledger guarantee. These local receipt correlations
are not fresh on-chain proofs. A's hire capability allows a separate sub-buyer
wallet; parent inspected resolveLineage and did not invent an equality between
child buyer and the parent's revenue wallet. Session roots still omit all tree
receipt fields and fee-accrual markers. Ordinary pipeline.ts is unchanged.

Independent parent command,02:13:02:

```text
bun --no-env-file x --no-install vitest run apps/hub/test/session-pipeline.test.ts apps/hub/test/pipeline.test.ts apps/hub/test/rail-pipeline.test.ts apps/hub/test/pipeline-tree.test.ts apps/hub/test/pipeline-attest.test.ts
79 passed / five files, exit0 (48 session executor,15 legacy,3 rail,4 tree,9 D)
```

Exact installed TypeScript read/parsed the absolute root config with the actual
directory/configFilePath and original options, plus noEmit/incrementalfalse.
Both absolute executor roots included: EXACT_F8_PIPELINE_ROOTS=2 DIAGNOSTICS=0.
Root ran no full suite or new supplemental fixture for this review. Author's
127-test earlier checkpoint remains separately attributed and predates the
terminal-bundle correction. Tests include controlled non-test rail seams seeded
through F5 memory, not live crypto/network or durable admission proof. Real
production-router/Broker/disk integration belongs to G14's separate evidence.

Frozen two-file hashes:

```text
84a152c3faa35cdc5e271716430d9509bf4b3c5cc4ac0a44e9ef132de206e692  apps/hub/src/pipeline-sessions.ts
1b70eb59279cc972f766b6875ee855cb5464b2c52d970dd7a6a2759361999fc0  apps/hub/test/session-pipeline.test.ts
```

No correction requested, source edits, credentials, network, live spending,
approval replay, dependency changes, Git mutation or push occurred in this review.

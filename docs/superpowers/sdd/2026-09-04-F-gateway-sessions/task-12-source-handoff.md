> Sanitized historical execution artifact. Statements reflect their recorded checkpoint and may be superseded. Historical commands are not current instructions. See the [current runbook](../../../runbook.md) for current behavior, approvals and operator commands. Personal/runtime locations and private artifact links may be redacted; public evidence and test distinctions are preserved.

# F12 source handoff — September 6, 2026

Readiness only. F12 source remains parent-held behind reviewed dependencies.
No source/test changes, execution, signing, credentials, network/research, Git or
full suite occurred here. Actual live twenty-call evidence remains **NOT RUN**;
F1 authority is consumed, and F13 fallback is not triggered. No owner request.

Read complete292-line evidence readiness, complete50-line parent decisions,
actual Plan F Tasks12/14, current F9 public types/Promise boundary, and relevant
actual runner/config, F9 owned runtime, prior owned-preload and public-feed/UI
boundaries. This condenses accepted choices, not a new scope of live authority.

## Exact proposed F12 paths and APIs

| Path | Ownership |
| --- | --- |
| `scripts/e2e-gateway-session.sh` | Tiny strict no-tracing launcher; exec Bun with --no-env-file and no implicit mode. |
| `scripts/e2e-gateway-session.ts` | Import-safe parser/gate, captured run plan, bounded facts journal, process ownership,20-call orchestration, independent evidence checks and safe summary. |
| `scripts/e2e-gateway-session.bun.test.ts` | Collected pure/help/gate, journal/failure, real owned-runtime and cleanup tests. No new test framework. |
| `scripts/fixtures/gateway-session-runtime.ts` | Inert mode-checked owned child/preload for hub/runner and finite offline facilitator/RPC seams; never an arbitrary executable/module selector. |
| `scripts/fixtures/gateway-session-probe/arcade.json` | One dedicated schema-valid childless script manifest with frozen price/bounds and no secret/egress/hire capabilities. |
| `scripts/fixtures/gateway-session-probe/run.ts.txt` | Deterministic nonempty script source, copied only into the owned skill directory as run.ts; source hash recorded, no provider/network/sub-hire. |
| `docs/evidence/m6-gateway.md` | Append explicitly offline evidence and live NOT RUN, preserving dated F1 proof and pending-credit limits. |

Proposed module exports: `parseSessionEvidenceArgs`, `runOfflineSessionEvidence`,
`assertTwentyCallEvidence`, `sessionEvidenceMain`. Keep small journal/ownership
helpers local initially; split only if parent approves a concrete need. Help and
invalid arguments return before reading operational env, importing active server/
runner entry points, acquiring signers or creating resources. No-args does not run.

Use actual `openSessionPromise({hubUrl,account,budgetUsd,rail,fetch},{signal})`;
handle methods are quote/call/status/close with optional `{signal}`. `call` uses
`seller` as serviceName (not wallet), skillId,input,maxAmountAtomic,maxWaitMs and
pollIntervalMs. Status distinguishes localIssuedAtomic/localConfirmedAtomic/
localExposureAtomic from hub amounts. Capture the final reviewed F9 API/hash on
release: current session.ts is still a dependency checkpoint, not accepted freeze.
Do not reimplement private HTTP/signing or use MCP/CLI funding as the harness API.

## One deterministic offline topology

Fixture policy:20 calls at10000 atomic each ($0.01), budget200000 ($0.20), one
skill/version/service, distinct deterministic unfunded buyer/seller roles. These
are offline fixture values, not authorized future live amounts. Freeze manifest,
original source, any added execution guard and input shape/digests; do not claim
the external-RPC usdc-flow-check skill ran when using this dedicated probe.
“Childless” means root jobs with no sub-hire/child receipts, not no OS processes.

Keep real hub lifecycle/paid/result routes, Broker, SQLite Store, F2 signature/
recovery, F3 Gateway rail and F9 buyer. Also run the real startDaemon/execSkill
script path over its owned WebSocket. F9's current integration registers a Broker
peer and synthesizes completion; reusing that alone does **not** prove F12 runner
execution. The runner can use an owned config path and explicit test-only seller
fixture credential; do not read Keychain/user config. Supply no sub-buy/ENS/AI/
canary/attestation/registration credentials or claims. Use the existing inert ENS
ticker seam to keep unrelated name renewal absent, not weaken payment checks.

Use actual server boot in an owned child with the reviewed narrow preload pattern
forcing hostname127.0.0.1/port0 and reporting a bounded unique control marker.
Keep its real router and durable real-session guard; generated private hub secret
and isolated SQLite must satisfy the guard. Poll exact health/listing/runner
readiness with deadlines/child-alive checks, not fixed sleeps or attach-to-port.
Only public manifest data crosses the handshake; source stays with the runner.

External seams are test-only, finite and counted. The controlled facilitator
checks real authorization signatures/coordinates, maps each admitted authorization
to one distinct canonical Gateway UUID and rejects duplicate/extra submissions.
Intercept only explicit external facilitator/RPC endpoints/methods needed by the
actual boot/rail; unknown origin/method/redirect/preconnect fails and increments
an external-attempt counter. Never turn GatewayLive into RailTest or infer support
from a label. Real HTTP/WebSocket is restricted to captured owned loopback ports.

## Journal, exact sequence and independent check

Create one owned0700 directory with exclusive0600 versioned facts journal and
private SQLite/evidence files. Do not reuse an existing run/journal/service,
global runner config, shared temporary outputs or F1/F11 journal. Use explicit child
environment allowlists and Bun --no-env-file, no ambient preload/debug/credential
inheritance. Parent-death/deadline guards cover hub, runner and job descendants.

Capture run/source/authority/bounds before open. Claim and fsync each open/call/
close intent before invoking its single issued Effect/Promise. A captured signer
wrapper records the exact index/nonce/typed-authority digest before its one
signature; captured fetch records paid-request intent/digest before forwarding.
Persist no raw signature/header/body/session/job token, arbitrary runner output
or secret. Fsync/write/close uncertainty poisons later mutation. Never repair,
delete, rename or replace a claimed journal to rerun the attempt.

Sequence: one open → exactly20 sequential explicit calls → at most one close
after20 validated successes. Each call caps at10000; claim index before await.
Stop on failure/uncertain503/deadline/invalid evidence; no fill-in call,21st job,
new signature, replacement session, automatic close in finally, deposit or rail
fallback. Pending202 may only perform bounded reads. Quote/header-negative/replay
cases are separate offline tests, not additional paid success-sequence operations.

Require per-index original input/expected nonempty output digest, local authorized
amount, job/session/buyer/seller/skill/version/nonce/rail/network and persisted
terminal correlation. Count independently:20 signer entries,20 paid forwards,
20 runner executions,20 begin/settle attempts,20 unique persisted root jobs and
20 unique gateway-transfer UUIDs. Do not infer all counts from a summary. No
children/tree/accrual fields; direct Gateway price goes to seller with fee0.

Validate F9/core closed artifact: exact captured identity/budget, persisted times,
complete=true, held0, spent200000, settledCalls20,20 positive-priced settled calls
and identical20-element distinct reference set. refs.length must be20, never1
or less-than-call-count. Retain the private full artifact; public summary exposes
mode/counts/amounts/source hashes/evidence limits, not session/job identifiers,
tokens, private locations or raw seller output.

Stop/reap owned writer processes, then independently inspect only the owned
database read-only: bounded21-row queries detect extras; decode actual stored
records and correlate all20 operations/artifact/terminal bundles. Do not invoke a
mutating Store bootstrap/reaper as a supposed independent read. Close that reader,
finish journal/cleanup proof, then print offline PASS with `liveEvidence: NOT_RUN`
and `fundsMoved:false`. A controlled offline reopen proves only local persistence.

Lost close retains F9 capability for bounded in-process status.closedReceipt
recovery, never a second POST. Journal/DB remain for uncertain runs; after process
exit they support local review, not invented surviving HTTP capability. Cleanup
failure prevents PASS. Abort/join polls, timers, streams, runner/jobs/hub; TERM
then bounded KILL/awaited exit only for owned handles, never PID0/port matches.
Assert listeners refuse after shutdown. Tests may remove only their own disposable
directories after assertions; normal unresolved evidence is not erased.

## Offline test/release and future-live boundary

Buildable checkpoints: pure validators/help/import/live refusal first; then owned
journal/process failure tests; then real20-call offline topology; finally evidence
docs. Cover duplicate UUID/19-or21 rows/corrupt amounts, missing persisted receipt,
late bodies/cancellation, journal failure before every mutation, lost paid/close
response, process death/cleanup failure and inherited-secret/loader escape. Use
actual collected Bun tests with explicit file invocation and exact script/fixture
strict roots; parent owns the full gate. No checks were executed for this handoff.

Future live is a separate explicit mode, no armed default and no environment flag
as authorization. Its exact scope/fresh-journal gate must refuse before credential,
network or child work when fresh authority is absent. Do not invoke it now. The
live executor is not yet implementation evidence: any remaining live prerequisites
must be listed as unimplemented/NOT RUN, not hidden behind stub success. No F1 probe,
deposit/faucet/withdrawal/canary/sweep or insufficient-funds auto-top-up. Eventual
transfer-to-batch mapping and canonical mined/source-debit/available-credit proof
are separate read-only evidence, with no one-batch promise or schema relaxation.

## Separate F14 handoff, not F12 source scope

Keep Plan F14 UI/test/README/architecture/buyer-guide work separately released and
committed; sessions docs consume actual F11 APIs. Current publicReceipt already
strips sessionId, but emits no session boolean and still links any settleTx.
F14 therefore needs a parent-approved narrow feed/test addition for a trusted
derived boolean marker and shared settlement-link policy alongside ui.ts/ui tests.
Never reintroduce sessionId/private lineage/nonce/capability to render a badge.
Gateway/Test stay unlinked even when hash-shaped; EIP links require valid nonzero
hash and known agreeing receipt network, not ambient selected chain. Preserve
escaping and later H whitelist; registry links are a distinct artifact category.
Docs state offline versus live NOT RUN, ceiling versus funds, held uncertainty,
explicit funding and tested testnet/pending mainnet. No instant withdrawal,
zero-total-gas, available-credit or twenty-calls-one-settlement claim.

Read provenance: task12-evidence-readiness SHA
`22bcb235d527bf1d88500d9947bf3ba5398e963f8a77bd9f43c7aa5326202561`;
task12-parent-decisions SHA
`f929420d57878fe2afd86c30ae2037d8819df648b13a7a59f20f8676dc87f32c`.
Observed, not final-approved, F9 session.ts SHA
`1c91030172feb22a57b371977f33d378dfefdb5434f077c5eff83a150892c56d`;
its existing runtime fixture SHA
`3e92a4a49a304006e47c8bd8b8e30c19f2645cd0753ab9b483095c2989ab95c2`.
Actual runner daemon SHA
`a9685270b530ea1555d996e7d044c02cb04e14ec7334e8d954d08f921cd3e101`.

> Sanitized historical execution artifact. Statements reflect their recorded checkpoint and may be superseded. Historical commands are not current instructions. See the [current runbook](../../../runbook.md) for current behavior, approvals and operator commands. Personal/runtime locations and private artifact links may be redacted; public evidence and test distinctions are preserved.

# F5 reference correction — independent final review, September 5, 2026

Verdict: **CLEAN** for the frozen six-file reference correction. Read the complete
author correction report and the core Session/Receipt, shared ledger and three
test changes; compared their behavior with the unchanged real TestRail. No
production edits, full suite, dependency, Git, key, network or live action.

Both accepted findings are resolved. A bound test rail alone normalizes an omitted
result kind to the honest `test` category and accepts bounded lowercase 0xtest
locators. EIP remains onchain/hash-only; Gateway still requires an explicit
gateway-transfer UUID. The canonical UUID version/variant checks are now shared
by the public call schema's behavior and the ledger. Closed receipts correlate
each settled call category with the enclosing rail and reject future batch claims.
This does not turn any category into independent mining or recipient-credit proof.

The collected integration uses actual makeTestRail challenge, verify and settle,
then the real memory or disk Store. Its result is not rewritten to look onchain.
It checks simulated balances, held/spent amounts, returned receipt category,
statistics, duplicate finish/second barrier behavior and SQLite reopen. The new
opposite-rail cases preserve queued jobs and held funds with no terminal receipt.
The earlier Gateway omissions/zero/foreign-category guards remain. No adapter,
legacy-guard, transaction, corruption-counter, payment type or TestRail source
was changed by this correction; those retain the original reviewed fingerprints.

Independent final verification completed by **2026-09-05 15:43:08 UTC**:

- Unchanged corrected private reproductions: **2 Bun / 5 assertions PASS**.
- Core and shared-kernel suites: **47 Vitest / 2 files PASS**, 5.42s.
- Actual RailTest memory + disk/reopen cases: **2 Bun / 20 assertions PASS**,
  39 other tests deliberately filtered; 246ms.
- Exact real-root compiler options over all twelve foundation files plus the
  private reproduction: **0 diagnostics**. `git diff --check` 0.
- All **14** source and unchanged rail/result hashes listed in the author's
  correction report match actual bytes. No test/owned fixture process remains.

Historical distinctions remain intact: the original private Red reached terminal
finish with the prior onchain category; the revised-contract Red failed earlier
at missing test schema support. The corrected expectation was not disguised as
the original test. Original implementer report remains
`b9148c884ff13f295a559bf58811a87db11c4f52f043ac7296b8d71d7c5c7979`;
private regression remains
`1256cb07ac25619893b39e1fe1bf3498e61bb711291fb4bfa30b07c012109048`.
Author correction report:
`b9b210dbb922971c7dd3f25991b5b133a81a797d7769eacaddab4433a980d363`.

This final bounded re-review supplements the prior full foundation source review
and its independently executed 67 Vitest / 63 Bun checks; it does not claim a
fresh full-repository gate or independently rerun the author's final 77/65 groups.
Parent owns gates/publication/commit. F6–8 must still preserve capability privacy,
pre-barrier terminal-size validation and one-shot uncertainty while correlating
the actual verified authorization/result. No remote settlement was tested here.

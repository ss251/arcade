> Sanitized historical execution artifact. Statements reflect their recorded checkpoint and may be superseded. Historical commands are not current instructions. See the [current runbook](../../../runbook.md) for current behavior, approvals and operator commands. Personal/runtime locations and private artifact links may be redacted; public evidence and test distinctions are preserved.

# F14 parent readiness decisions — September 6, 2026

Parent fully read the complete source handoff and independent readiness review
3c8142ef0a39d441bad6a4d8c7a2b628fa325f9e5764aa63d1d22bcff7a4bdde.
Accept its ten-file starting scope and three additions. F14 source remains held
until preceding F11/F12 checkpoints; no H source, test or live action is released.

Within the existing UI scope, replace page-wide every-row-is-onchain claims based
only on the current default rail with neutral mixed-receipt evidence wording.
Keep TestRail simulation disclosure while allowing historical rows from another
rail; one default does not relabel all receipts. The reference column and chips
must distinguish a Gateway transfer from a mined batch. Preserve current design.

Both publicReceipt and SSR child rows must scrub the actual pipeline fallback
where child.skillId equals child.jobId to unknown-skill. Authenticated private
receipts keep their identifiers. This is the narrow known alias fix, not an
early transplant of H1 or a pipeline/schema rewrite. Add real public JSON/HTML
negative tests alongside explicit-field redaction. Do not propagate a root's
session marker into an ordinary independently paid child.

The pure new reference helper should import the explicit local chain-config
subpath, not an ambient-selected core constant. Accept upper/lowercase canonical
legacy hexadecimal hashes, exactly32 bytes and nonzero, only for settled EIP-3009
on a ready matching configured network with absent legacy kind or onchain kind.
Children use root rail/network/kind plus their own settled/ref fields; a released
root does not erase independently settled child evidence. The separate F8 session
result helper already gates its links and stays untouched in this initial scope.

Derive session from an exact36-character ses_ plus32 lowercase-hex identifier;
an explicit length check prevents trailing-newline acceptance. Emit a boolean
only and overwrite any forged rest-projected extra. UI emits no private ID in
text or attributes. Preserve canary and session labels independently, including
released receipts. Positive links remain inspection references, not fresh mining
or receipt-status proof.

For the later H merge, preserve safe reference-kind semantics through the public
whitelist, bounded client decoder and formatter. Choose a normalized public kind
union onchain/gateway-transfer/test/unrecognized, optional only for true legacy
absence. Any unknown/malformed present kind becomes unrecognized (or a whole-row
refusal in the client), never omitted as eligible legacy absence. Formatter and
child paths accept only absent/onchain in addition to the existing settled/rail/
network/hash gates. Add the validated boolean session separately. Preserve H1's
whitelist and H4's own-data/size/accounting checks. These are explicit later H
integration requirements, not permission to modify H before F/G merge.

Begin with pure/reference/UI/public projection tests and exact server wiring
review. If an actual private-result HTTP integration extension is needed, release
apps/hub/test/pipeline-attest-http.test.ts separately with its owned preload and
finite no-env-file child checks. Existing lineage-http intentionally leaves jobs
queued and is not evidence of returned receipt projections. Do not invent a
child explorer integration result from the ordinary root-only fixture.

Root owns the full test/types/web build and local desktop/mobile rendered checks
before F14 commit, then all four full-F merge gates including Forge. Live F12 is
NOT RUN under no-new-spend; F13 fallback is not triggered. No new funding,
approval replay, production repoint, mainnet flip or push is permitted.

## Readiness erratum — 2026-09-06 05:27 IST

Keep the explicit session-identifier length check as defensive exact-shape
validation. The earlier statement that it prevents a demonstrated final-newline
acceptance was not established: the actual Bun unflagged-regex probe during F11
rejected that candidate already. Do not report a newline Red without reproducing
it against the F14 baseline. No F14 source is released by this clarification.

## Source release — 2026-09-06 06:28 IST

F12 committedfb26382 after its full test/type gate and independent source/public
audit. Release exactly the ten starting paths: B9 helper/reference-test/public
feed/feed-test; G14 UI/UI-test/server two explorer fields/README/architecture/
buyer-guide. Shared helper names receiptExplorer, receiptChildExplorer and
hasSessionMarker; actual minimal type contract coordinated between authors.
G3 independently reviews; root owns final full gates/commit and rendered QA.
The optional HTTP test path and all H source remain held.

Accept the actual Receipt.make distinction B9 checked without network: true
omitted settleRefKind has no own field, but explicit undefined retains one.
Present malformed/undefined kinds must refuse link authority, not silently become
legacy absence after JSON drops undefined. Normalize a present public kind to
onchain/gateway-transfer/test/unrecognized within the existing feed scope.
Child context must preserve genuine root-kind absence rather than manufacture
an explicit undefined field. Use the explicit relative chain-config module to
avoid the existing Vitest bare-package prefix alias; no alias/config expansion.

The baseline UI's blanket failed-calls-never-charged sentence and timeout footer
need the already accepted uncertainty distinction. A failed validation before
settlement is not an unknown result after a paid attempt. Root also accepts the
narrow false-row price correction: replace '$0 charged' with a neutral em dash
and accessible 'No settlement recorded; not a balance proof.' qualification.
Preserve amount formatting, columns, colors and recorded status/reason; no new
accounting/state classifier. These are source findings until executed Reds.

Root's ignored offline SSR preview imports only renderers and synthetic receipts,
never hub boot/keys/RPC. It will bind an owned loopback port for desktop/mobile
checks with bounded shutdown. This is not live purchase or public deployment.

> Sanitized historical execution artifact. Statements reflect their recorded checkpoint and may be superseded. Historical commands are not current instructions. See the [current runbook](../../../runbook.md) for current behavior, approvals and operator commands. Personal/runtime locations and private artifact links may be redacted; public evidence and test distinctions are preserved.

# F14 independent source and focused verification — September 6, 2026

Verdict: **CLEAN within the released ten-path F14 scope.** No new material finding
or source correction requested. Rendered desktop/mobile QA, full gates, public
publication and commit remain parent-owned and are not claimed by this review.

Read the complete F14 source handoff, readiness review, parent decisions/erratum
and actual Task14; the full helper/feed and three tests, affected UI functions,
exact private-server projection changes, and all three final documents. Compared
actual core Receipt/ReceiptChild and explicit chain configuration, F9 SDK/F10 MCP
and F11 session documentation. Rechecked held H public whitelist/client decoder/
formatter only for the already-required later integration, without changing H.
The ts-testing skill guided existing behavioral-suite reuse and explicit evidence
attribution; no new fixture, source/test/public edit, dependency, Git, browser,
child service, operational key, signing, network, live payment or full suite here.
Only this ignored report was written; every shell used login:false.

Both complete frozen author reports were read:

- task-14-reference-report.md:8441519bc238c6285ce0e80bd847def6e8b55f7738acd84ba862deab40f60da6
- task-14-ui-report.md:0f5133b372b16413426649c2d4f84e3ebf06c392ed0bec96bca8c1b3e48b2405

## Independent checks

```sh
bun --no-env-file x --no-install vitest run apps/hub/test/receipt-reference.test.ts apps/hub/test/receipts-feed.test.ts apps/hub/test/ui.test.ts
```

At06:38:52 IST:122 PASS across3 files,0 failures,940ms Vitest duration:
48 reference,25 feed,49 UI. These are pure renderer/projection/controlled-module
tests, not an actual private-result HTTP or browser run. Existing runner/mocks
were used; no additional collected or private test file was introduced.

An independent fileless compiler-API program parsed the repository root config,
then used exactly7 roots: receipt-reference.ts, receipts-feed.ts, ui.ts, server.ts
and their3 focused test files. strict:true/noEmit:true with transitive imports:
0 diagnostics,exit0,4.055s. This does not imply root/web/full-build coverage.
All7 code/test hashes matched the coordinated author freeze before/after checks;
all10 final hashes were independently verified again after document review.

Read-only Markdown check across3 docs:20 local links resolve, including3 heading
anchors;5 external links were not fetched. The scoped personal/private-locator
pattern scan found0 matches. No source-linked live research or current chain
claim was added. The author's2 SDK-snippet in-memory strict checks are attributed
author evidence, not additional tests run here; actual API sites were read here.

## Reference authority and privacy

The47-line helper imports explicit local chain-config, not the ambient-selecting
core barrel. It admits only ready positive safe chain IDs agreeing with CAIP-2
and a canonical configured HTTPS explorer origin. A link requires own settled
true, EIP-3009 rail, the exact receipt CAIP-2, a nonzero full32-byte hex reference,
and truly absent legacy kind or own onchain kind. Upper/lowercase legacy hex is
preserved. Gateway/Test references, unknown/pending networks, malformed/zero
hashes, inherited/accessor authority and every present ineligible kind refuse.
Descriptor errors are contained; there is no value coercion or getter authority.

Actual Receipt instances work: omitted kind is absent, whereas explicitly supplied
undefined is an own field and refuses. Child helper receives the original root,
not a manufactured object that erases this distinction. Root rail/network/kind
plus the child's own settled/reference determine a child link; independently
settled child evidence survives an unsettled root. Forged child provenance does
not override root context. Links remain inspection references, not new mining proof.

The feed derives session from the canonical own36-character private ID and writes
the boolean after the legacy rest projection, overriding a forged public extra.
Private IDs remain omitted; SSR emits only the session word. Canary remains
independent, including non-settled receipts; neither marker propagates to ordinary
child hires. Both public JSON and SSR replace the known child.skillId===child.jobId
fallback with unknown-skill, preserving amounts and actual settled/reference data.
Present kinds are normalized to onchain/gateway-transfer/test/unrecognized after
rest, so JSON cannot erase present undefined/unknown data into eligible absence.
Tests use actual trusted Receipt/ReceiptChild instances and check nonmutation.

This is deliberately not a wholesale future-field/privacy whitelist or hostile
same-user/proxy sandbox review. The existing trusted-receipt rest projection is
not relabeled as H1's stronger whitelist. During the later H merge, preserve that
whitelist and H4 own-data/size/accounting checks; explicitly carry validated kind
semantics and optional real boolean session through decoder and formatter/child
paths. Unknown present kind must stay unrecognized or refuse, never disappear
into legacy link authority. Those remain required later integration work, not
authorization to edit H now or grounds to expand this approved F14 slice.

## UI, server wiring and documentation

Root/child references share the helper. Gateway UUIDs remain full escaped text;
TestRail is labeled simulated. Existing pay-test/identity links remain separate.
Index/detail reference headings, semantic settlement colors, money formatting
and child indentation remain. Default rail no longer claims every historical
row is mined or simulated; TestRail disclosure composes with mixed histories.
Header/footer and false-receipt price cell no longer turn unknown paid outcomes
into zero-charge/revocation claims. No automatic payment retry advice is given.
Escaping and no-public-fallback-ID assertions cover both JSON and SSR paths.

The ordinary authenticated server change is exactly an import swap and two
explorer expressions. Independently reversing those edits in memory reproduces
the entire baseline SHA256
b0a9a2d8ffee922cf2e0a695e4ce27ab4ad40159cf275bd34bba6bbed5446e22.
No private-result HTTP integration was run, and no child HTTP coverage is inferred
from pure tests. F8's separate already-strict session result helper is untouched.

Final docs match the current captured Promise/Effect session lifecycle and8 MCP
tools, exact route/service roles, explicit inputs/ceilings and same-handle status
recovery. They preserve seller-funded child isolation, issued uncertainty and
distinct wallet/Gateway/hub balances. Funding is linked to the existing explicit
journal/gas/fee/height rules, with current credit-attribution and Minter-identity
limits; no hidden deposit, withdrawal-all, default gas budget or mutation retry.
F1 remains consumed historical evidence; F12 is offline20 roots/20 distinct UUIDs,
fundsMoved:false/liveEvidence:NOT_RUN, live entry unimplemented. No one-batch,
zero-total-gas, available-credit, mainnet-ready or approval-replay claim is made.

## Preserved author chronology, not independently replayed Reds

B9's existing feed baseline collected18:14 FAIL/4 PASS. The new helper's missing
module was0 collected, not48 behavioral Reds. Its implementation passed66, then
7 supplemental already-correct cases brought the frozen selection to73.
G14's first UI baseline collected45:21 FAIL/24 PASS; separate false-receipt-price
regression failed1 with48 skipped. Final UI passed49; later invalid-kind cases
were supplemental Greens. Newline cases are exact-shape coverage, not a claimed
JavaScript final-newline Red. This review ran only the final122-case selection;
author failures and repeated author/independent Greens are not summed together.

## Final ten-path SHA-256 inventory

| Path | SHA-256 |
| --- | --- |
| apps/hub/src/receipt-reference.ts | c70e3380fdfd37c50358b01a8cf27682ff946a7f3ac5e963f83d81a110f5212b |
| apps/hub/test/receipt-reference.test.ts | c4bc5322039c33e14e7b65fc88b67ce8acc15d7136fa359746d854711c5ec643 |
| apps/hub/src/receipts-feed.ts | 3e911fcb5e5f10959d0009f8a5323621b2ce420f717ba68e13c506339b4be3bc |
| apps/hub/test/receipts-feed.test.ts | 0e4e57f9eddd7d5aaea8202f2ac59d8b715ff2e5caf7666aa989ae3bc5b5f66a |
| apps/hub/src/ui.ts | 9c68f03d358946944b7b9209f99e93ef3d29d8b3b42c3b23d3628392a0060c7f |
| apps/hub/test/ui.test.ts | 246ff18c564b170a1711da9e8726892f3e43a8cc2e8f16e3e1b3a6029d2b2c0b |
| apps/hub/src/server.ts | 533f80fe8a6af49214bad0c778b2cd93188dae1f24d208ed2abfffba9aedc6f2 |
| README.md | f381f198260d16d1a47f0e50407ac67c7121519b734da878dc1bb981f5093bac |
| docs/architecture.md | b1dcbecc3e2add91540e6b29929c8f29a6617ae99d36c9ad57abf6c0c9cb1cb5 |
| docs/buyer-guide.md | 24da04e953c902f1c854d34e71186588530c9619a079eba45bd4a65e9582f990 |

## Narrow parent-rendered correction review — September 6, 2026

Final bounded verdict: **CLEAN after the two approved UI corrections.** The entire
original136-line checkpoint above remains unchanged, pre-append SHA256
ae279a8ef7b8930b906860b2d4a711905bfb578f91ac4e4ad744c9ad458187b7.
Its122/exact7 result remains historical and was not relabeled as a corrected run.
Read the full author addendum e7b254c71cf78e7b51ab7877e12da05bb29dd9459dfd32cf95a1f9543aa59bcf;
its original119-line0f5133b3 report prefix is explicitly preserved.

The parent's actual mobile390 browser showed document inner/scroll width423 and
table right423.359 beyond the region right366. This was real rendered evidence,
not inferred from the earlier string suite. The approved correction contains
horizontal overflow within .tape, sets a640px table minimum, and makes both index
and detail receipt regions named and keyboard-focusable with a visible outline.
Parent's related mixed-rail check found a blanket fee(5%) heading conflicting
with zero-fee Gateway rows. Both table headings now say fee; initial and refreshed
metadata explicitly say default fee. Recorded row amounts/formatting are unchanged.

Independently read only this narrow stable source/test delta. Exact in-memory
reversal of the scroll CSS/two regions, fee headings/default labels/detail local,
two new test groups and renderMeta test import reproduces original9c68f03d UI and
246ff18c test hashes. The interim test-reconstruction attempt overlapped the
author's fee additions; it is not a source defect or exact frozen-delta claim.
Final stable reconstruction matched both originals; the other8 paths are unchanged.

Independent corrected command:

```sh
bun --no-env-file x --no-install vitest run apps/hub/test/ui.test.ts
```

At06:46:41 IST:54 PASS/1 file,0 failures,1.14s Vitest duration. Exact fileless
ui.ts/server.ts/ui.test.ts compiler program:3 roots,strict/noEmit,0 diagnostics,
exit0,4.377s. Final pair before/after matches. These tests pin CSS/accessible
structure and fee semantics, not browser geometry. No new private fixture,
browser/process/service or full-suite run by this reviewer.

Preserved author chronology: scroll structure2 FAIL/49 skipped; then fee-label
3 FAIL/51 skipped. The incidental new "$0" expectation was corrected to the
existing "$0.00" formatter output, not counted as a product defect. Author54
and independent54 are repeated checks, not108 distinct cases.

Parent's final actual isolated-browser evidence on the final pair, obtained
directly: desktop document inner/scroll1280 and region client/scroll870/870;
mobile document390/390 with region340/672, overflow contained. Actual focus plus
ArrowRight produced scrollLeft>0, and the far-right reference screenshot was
visually inspected as readable. Both views had exactly2 eligible links, absent
private IDs, independent canary/session labels, no zero-charge claim, plain fee
heading and default fee5%; mixed TestRail-default readback also passed. Parent
visually inspected3 final screenshots and closed browser contexts/process in
finally with exit0. This reviewer did not repeat or claim those observations.
Parent also reports both owned previews stopped/reaped with exit0 and independent
HTTP checks returned specifically ECONNREFUSED for old64152 and final49981.
The parent's sole full gate is now running; full-gate/commit success is not claimed.

Only these two entries supersede the earlier ten-path inventory:

- apps/hub/src/ui.ts:b5387b04dd94c7c52c3a8b6c5889c223712e6a25ca703d5fc1ab1d83613045b5
- apps/hub/test/ui.test.ts:c6decb4f5f225d6104988ffef960f1a4f6fc3c7e5239768252c1778ffbcda66b

The explicit later H whitelist/kind/decoder/marker requirements and live/spend
limits remain unchanged. No further bounded correction is requested.

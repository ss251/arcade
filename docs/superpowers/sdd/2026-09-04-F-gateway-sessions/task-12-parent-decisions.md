> Sanitized historical execution artifact. Statements reflect their recorded checkpoint and may be superseded. Historical commands are not current instructions. See the [current runbook](../../../runbook.md) for current behavior, approvals and operator commands. Personal/runtime locations and private artifact links may be redacted; public evidence and test distinctions are preserved.

# F12/F14 parent readiness decisions — September 6, 2026

## Source handoff accepted

Parent fully read task-12-source-handoff.md SHA-256
d68c0a724bf1f2e8d8f0566475a396269f2b7ee6fa94bbc8aaec417795dc3e2b.
Accept the proposed seven F12 source/test/fixture/evidence paths as the starting
scope after dependencies. Source is still held; no live mode is run or authorized.
The offline topology must execute the actual runner/script path, not merely
synthesize Broker completion as in F9. Twenty local signatures/paid forwards/
runner executions/persisted root receipts need independent counts and20 distinct
Gateway UUIDs. Read-only SQLite inspection follows owned writer shutdown and may
not bootstrap a mutating Store. Cleanup failure prevents offline PASS. Preserve
F1's dated live proof and distinguish fundsMoved:false/liveEvidence:NOT_RUN.
F14 feed/marker/link-policy/UI/docs remains separately scoped/released/committed.

Parent read the full 292-line task-12-evidence-readiness.md (SHA-256
22bcb235d527bf1d88500d9947bf3ba5398e963f8a77bd9f43c7aa5326202561).
Source remains held until the preceding reviewed dependencies. Accept its owned
resources, one-shot evidence, exact header/correlation, private capability,
uncertainty and cleanup constraints, with these implementation choices.

Use a dedicated deterministic childless script fixture for offline evidence,
explicitly identified by its source/manifest hash. Do not silently substitute it
for canonical usdc-flow-check while claiming that skill ran live. Keep real F2
signing/recovery, F3 rail, F6–9 APIs, router/broker/runner and actual SQLite; replace
only the external facilitator/RPC boundaries with finite counted fixtures. No
provider calls, AI keys, F1 script, funding, withdrawal, canary or attestation writer.

Twenty settled Gateway calls require twenty distinct gateway-transfer UUIDs in
the current closed artifact. They do not require one batch or fewer references
than calls. Any future transfer-to-batch mapping and canonical mining evidence
are separate read-only evidence, never a schema relaxation or deduplication.
The run is childless and complete means exactly its admitted roots, not graph-wide
finality or remote available credit. Retain F1's dated one-payment scope unchanged.

Implement help/import and explicit offline operation without any credential IO.
A future live entry must be explicit, exact-scope and fresh-journal gated, with
no armed default, implicit funding, inherited credential escape or approval replay.
The current instruction authorizes NO live invocation: run only offline tests.
Do not confuse that executor restriction with a permanent success stub or an
unimplemented live path labeled complete. If the live path's prerequisites remain
unimplemented, document that exact limit. If implemented, it still remains NOT RUN
and supplies no live evidence. Existing credentials/environment are not authority.

Claim each open/call/close attempt once and preserve ambiguity. Stop the sequence
on uncertainty; no twenty-first live probe, replacement session, automatic close
in finally, funding-on-error or paid retry. Bounded read-only close recovery uses
the same persisted artifact. Token-free journal and retained isolated DB support
local review after exit, not an invented surviving HTTP capability. Print PASS
only after exact artifact/persisted-evidence validation and owned cleanup succeed.

F14 is still an actual full-Plan-F task after F12; F13 fallback is not triggered
by passing F1. Keep its UI/docs work narrow and separately committed. Gateway/Test
references stay unlinked, even hash-shaped strings; EIP settlement links require
valid nonzero hash and known agreeing receipt network, not ambient default chain.
A public session marker must be a derived boolean, never reintroduce sessionId
or private job/buyer/nonce/capability fields to render a badge. Preserve the later
H whitelist on integration. Do not mix registry links with payment evidence rules.

Docs must say offline harness versus live NOT RUN, current tested testnet versus
pending mainnet, local ceiling versus wallet/Gateway funds, held uncertainty and
explicit funding. No one-batch, zero-total-gas, instant withdrawal or seller credit
claim. No owner request or new spend is made by this readiness decision.

## Source release — 2026-09-06 05:27 IST

F11 is committed as325bd47 after2648Vitest/123files,501Bun/39files/4541assertions
and root/web strict TypeScript passed. Release the seven accepted F12 paths:
G14 owns launcher/orchestrator/collected test/evidence append; B9 owns the runtime
fixture, manifest and probe source. Root owns gates/commit, and G3 independently
reviews persisted correlation. No F14 or unrelated source is released here.

Freeze child modes as exact --hub/--runner. One bounded newline JSON start record:
version1, runId32lowerhex, role, canonical fresh owned0700directory, ownerPid and
deadlineUnixMs; hub-only ephemeral hubSecret or runner-only captured literal
127.0.0.1 hubOrigin. Derive database.sqlite and skills/gateway-session-probe under
that directory, with no arbitrary path/module/URL selector. Subsequent controls
are exact {version:1,runId,command:"snapshot"|"stop"} on stdin, not HTTP endpoints.
Role, identity, unknown keys, duplicate facts and extra counters must be checked.

stdout uses the fixed GATEWAY_SESSION_FIXTURE prefix and closed ready/snapshot/
stopped/failed facts. Private IPC may carry canonical job IDs/nonces in arrays of
at most20 for cross-correlation, never session/job tokens, signatures, secrets or
raw logs. Public output remains bounded hashes/counts/amounts, not identifiers.
Hub snapshots independently count real begin/finish and facilitator verify/settle;
runner snapshots count exact-entry spawn/exit/result plus actual input/output
digests. A narrow actual StoreFromEnv lifecycle/counter seam is allowed, not Store
state synthesis or Broker completion replacement. PID facts match owned handles.

Prefer a committed self-contained probe guard so actual installed/executed bytes
equal reviewed source; if any guard wrapper changes bytes, pin original and actual
executed hashes separately. Do not equate them by assumption. No new job-secret
channel or ambient credential is needed. Keep control streams/backlogs bounded.

Parent fully read and accepts independent task-12-persistence-review.md SHA256
9cb57e68b525927894df24888ff15ba1c85edd655d1efe35cd0835a2c61d0440:
after writer shutdown, readonly global1/20/20/20 row sets, scalar/JSON agreement,
exact original per-index input/output/nonce/ref/amount/times and closed-artifact
correlation. SQLite does not prove runner execution or source hashes; keep those
independent observations. Live is unimplemented/NOT RUN under no-new-spend, not
a success stub or permission to replay F1. The earlier held notes are historical.

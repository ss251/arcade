# F4 parent integration review — September 5, 2026

Read the complete boot/pipeline/discovery diff, registry and all new tests and
preload, plus all three implementation checkpoints. The earlier pure registry
was parent-authored and independently reviewed by another agent; this parent
review covers the independently authored integration and its follow-up.

CLEAN after the genuine discovery-wording correction. Default identity,
constructed inventory, selected configuration, unchanged per-listing EIP targets
and Gateway seller target match the actual interfaces. The optional internal
pipeline rail controls settlement and receipt attribution without introducing a
public selector or session accounting. No new provider request occurs during
construction. Existing explicit chain checks remain separate.

The parent independently reran all 78 focused Vitest cases across six files at
19:17:14 IST, then all nine actual-router Bun cases / 108 assertions; both exited
zero. Those actual child processes used dummy configuration with outbound
fetch/preconnect blocked, were reaped and checked unreachable after cleanup.
The simulated second TestRail is only named Gateway and is not live payment
evidence. No real key or external payment was used in this verification.

The parent read the public brief and all original report bodies, and verified
all three exports are byte-exact after the standard historical banner. The
publication preparer separately resolved fourteen local links and reported a
clean privacy scan. Historical failed checkpoints remain explicitly superseded,
not silently converted into passing evidence.

The TypeScript-testing skill guided the genuine selected-rail, payee and
uncertain-outcome regressions and actual owned-router verification. Built rails
are not live support or funding proof; neither local cancellation nor a failed
settlement request proves no charge. F5–8 still owns durable session authority.
Exact-target strict checks and F4's own full repository gate are recorded in
the additive [progress ledger](progress.md) before its separate commit.

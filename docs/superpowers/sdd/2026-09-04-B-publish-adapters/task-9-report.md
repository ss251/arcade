> Sanitized historical execution artifact. Statements reflect their recorded checkpoint and may be superseded. Historical commands are not current instructions. See the [current runbook](../../../runbook.md) for current behavior, approvals and operator commands. Personal/runtime locations and private artifact links may be redacted; public evidence and test distinctions are preserved.

# Task 9 report

- Commit: `c15e5a6 feat(runner): arcade publish accepts mcp:// endpoints and OpenAPI documents`.
- Exports exact publishTargetKind and flagAll APIs, plus narrow async runPublishIntrospection for shared dispatch/testability. import.meta.main prevents helper imports from running commands. Existing init/wallet/config guard tests remain green.
- TDD: unit missing APIs -> implementation. Root's actual CLI tests at03:01:49 reproduced eight failures: non-JSON directory preview, missing OpenAPI routing, YAML read before refusal, remote document routing/fetch absent and server --help mistaken for CLI help. Later one test expectation corrected to include decoder-provided capabilities:[], not a production bug. Zero-price regressions reproduced Schema.Price accepting an amount that parsePrice cannot settle, then fixed before any IO and in batch validation.
- All application option parsing and help recognition stops at --. Server argv remains literal, including --yes/--price/--help; only publisher --yes writes. Unknown/missing/inapplicable/duplicate single-value options, absent/ineligible selections, empty batches and invalid generated manifests fail before partial preview/write. Read-only MCP default and explicit write-tool opt-in preserved.
- Remote JSON document download is HTTPS-only without userinfo, refuses redirects/status failures, bounds streamed size to5MiB and headers/body to one30-second deadline, cancels without waiting on uncooperative streams, and hides raw upstream errors/body. YAML refused before read/fetch. Local JSON errors are generic. Source URLs/server argv/spec servers are not echoed in discovery summaries.
- Directory --json emits exactly the agreed singular preview shape, including decoded private engine/defaults and public projection. No runner configuration mutation. Prose grants now state configured MCP/API access or no model tools accurately; legacy PUBLISHED/STAYS markers retained for consumers.
- Actual subprocess tests verify directory JSON; preview-only/no writes; self-contained OpenAPI spec copy; seller edits protected until --force; selectors/auth/price/errors; offline HTTPS success/status/redirect/invalidJSON; real local SDK stdio argv and read-only selection. Temporary HOME and local fetch fixtures avoid user configuration/Keychain/network.
- Live verification at03:06 IST: `bun run arcade publish mcp://docs.arc.io/mcp --price '$0.02'` returned three tools/two listings, skipped submit_feedback, printed search-arc-docs/query-docs-filesystem-arc-docs and Nothing written. Only tool metadata discovery; no live tools/call, feedback, payments or listing writes.
- Plan mismatch: H assumes the singular --json contract handles MCP/OpenAPI sources despite B9 Step5 specifying directories only. Introspection --json is explicitly refused; do not arbitrarily select one listing. Resolution recorded in plan-h-preview-contract-mismatch.md for real wizard integration testing in H.
- Verification: 56 unit +13 subprocess cases; full977 Vitest +29 Bun, bunx tsc --noEmit, git diff --check pass. Independent read-only review and root source/interface review clean. No web/contracts changes or push.

> Sanitized historical execution artifact. Statements reflect their recorded checkpoint and may be superseded. Historical commands are not current instructions. See the [current runbook](../../../runbook.md) for current behavior, approvals and operator commands. Personal/runtime locations and private artifact links may be redacted; public evidence and test distinctions are preserved.

# Task 5 report

- Commit: `821b276 feat(runner): mcp adapter — one MCP tool per listing, stdio or streamable HTTP`.
- Exact textOf, transportFor, outputFor, runMcp and mcpEngine interfaces present; registered MCP harness and workspace SDK dependency. Lockfile adds only the existing dependency mapping, no resolution upgrade. Initial sandbox install failed on tempdir permissions; approved `bun install --offline` completed.
- HTTP header/query credentials bind by environment name from scrubbed process.env; stdio inherits only that scrubbed environment. Transport discovery remains possible before choosing a tool. Buyer cannot override the listing's tool name through input.
- All result shapes validated with SDK CallToolResultSchema; isError/malformed/empty replies cannot settle. Raw upstream errors, private URLs/commands/names, and stdio stderr never reach public diagnostics. SDK timeout options added to initialization/tool calls.
- TDD: module-missing red; four registry/stdin integration reds; actual execSkill timeout reproduced orphaned child during both stalled call and graceful close. Scoped ownership retains the exact child PID until SDK close/exit, kills that child on harness cancellation, and removes listeners. Both real regressions now green, including a child ignoring SIGTERM. Failed red tests safely reaped only their recorded fixture child.
- Additional red-to-green unit checks cover URL userinfo refusal, SIGTERM/SIGINT/exit ownership, pending-close PID capture, failed-start/close listener cleanup. No process-group kill or HTTP session orchestration was added.
- Plan deviations justified by privacy/lifecycle requirements: suppress raw stderr/errors, forbid redirects through transport fetch for GET/POST/DELETE (SDK SSE ignores requestInit.redirect), support planned auth, refuse malformed results and inline URL credentials, scoped child cleanup. Documented narrow Transport assertion handles SDK optional sessionId exactOptionalPropertyTypes mismatch.
- Verification: 48 MCP unit tests plus 7 real harness cases; full working-tree suite 756 Vitest + 29 Bun, tsc and diff-check pass. Full count includes 41 uncommitted independent Task6 unit cases; only Task5 files staged. Root reviewed code against installed SDK behavior; no material issue remains. No web/contracts edits, external provider calls or payments.

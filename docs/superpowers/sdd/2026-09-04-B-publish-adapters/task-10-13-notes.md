> Sanitized historical execution artifact. Statements reflect their recorded checkpoint and may be superseded. Historical commands are not current instructions. See the [current runbook](../../../runbook.md) for current behavior, approvals and operator commands. Personal/runtime locations and private artifact links may be redacted; public evidence and test distinctions are preserved.

# Follow-on demo/evidence preparation

- Task10: current diff-triage agent.ts prompt is the plan's exact body. Preserve it when moving to SKILL.md; test literal `never as instruction` differs from both existing prompt and plan body `never instruction`, so use the meaningful existing phrase in the assertion. Private engine model becomes claude-sonnet-5; do not change public price/bounds/schemas/secrets/egress. Delete only the plan-authorized agent.ts, recoverable from git. Seller-guide grants prose must match Task9's truthful adapter-specific wording.
- Task11: exact public Arc Docs metadata already captured read-only at 02:22 IST in committed fixture. Only search_arc_docs and query_docs_filesystem_arc_docs are readOnlyHint:true; never publish or call submit_feedback. Generate with CLI, do not manually edit generated manifests. Compare with captured fixture; if metadata changed, inspect actual change rather than weakening equality.
- Task12: Frankfurter fixture committed at0c9b2bf is exact plan document. Copy identically into listing using apply_patch, then CLI --force generation as plan specifies. Verify current free upstream HTTP200, then actual execSkill succeeded/end_turn/rates.EUR. Root bun -e cannot resolve Effect by bare import because root has no direct dependency; execute from packages/runner or resolve via its createRequire, with absolute skill paths.
- Task13: script must exit nonzero for missing listing, failed/invalid/refused/timeout or non-end_turn result; plan's continue-only script falsely succeeds. Test failure behavior. Same root Effect import issue applies; use a repository-appropriate resolution/dependency solution rather than assuming transitive root imports work.
- All application flags precede the stdio separator: `arcade publish mcp:// --price '$0.05' --yes -- bunx -y my-mcp-server`. Anything after -- is server argv, including --yes/--help/--price.
- Correct paid buyer syntax: `bun run arcade-buy fx-rate --input '{"base":"USD","symbols":"EUR"}'`, not the plan's stale `arcade-buy call fx-rate JSON`.
- Paid evidence is authorized ONLY on Arc testnet with existing known Keychain labels arcade-buyer-key and arcade-deployer-key. Read keys only inside consuming command; never expose or persist values. Use isolated temporary runner/hub configuration, not live user config. No detached fire-and-forget process needed; keep tracked process sessions and cleanup owned processes.
- ANTHROPIC_API_KEY was not listed among known work-order Keychain items. Check only its availability, not secret value in output, at live evidence time. Missing provider credential is owner action; do not substitute a subscription seat. A9 remains owner-blocked for distinct funded ARCADE_SUBBUY_KEY, unrelated to single FX paid settlement.
- No push or PR. Record paid receipt/tx evidence in local report/runbook rather than a PR description. A 402/unfunded buyer is not evidence that a job ran; do not claim settle-only-on-success from a rejected request without an actual job receipt.
- Documentation must say schemas are copied/projected within supported runtime/hub subset, not that the small hub validator enforces every upstream OpenAPI/JSON Schema keyword.

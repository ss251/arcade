> Sanitized historical execution artifact. Statements reflect their recorded checkpoint and may be superseded. Historical commands are not current instructions. See the [current runbook](../../../../runbook.md) for current behavior, approvals and operator commands. Personal/runtime locations and private artifact links may be redacted; public evidence and test distinctions are preserved.

# B13 free-route readiness — read-only preparation

Prepared in MAIN `<REPOSITORY_ROOT>`, 2026-09-05. No source edits, tests, network requests, proxy restarts, keys, services, or git operations were performed. This ignored note is the sole write. E14 remains frozen in `e-ens` (34 Bun +4 Vitest +tsc/diff Green; C1 final CLI review CLEAN).

## Authority and sequence

Read `[private owner-action ledger omitted]` in full, including the 09:10 IST free-route decision and later 09:35 live approvals, current `[private heartbeat omitted]`, complete B Task13 and actual implementation. Current explicit work order is A9 → C10 → D13 live evidence → B13 proxy. B13 source/live execution awaits root release after D13. Presence of a key or previous conductor proof is not a reason to run it early.

The owner rejected obtaining an Anthropic key or reselling a subscription seat. The approved replacement is the existing loopback CLIProxyAPI `http://127.0.0.1:8317`, model alias `glm-5.3-flash`, API-key credential. Keychain item `arcade-claudex-key` is the proxy's key. Owner reports an actual `/v1/messages` custom `tool_use` exchange succeeded in 5.5s; this preparation did not repeat or independently verify that transport. Never restart/reconfigure the proxy or send `claude-*` model IDs to it. No ENS operations are authorized by this B13 task.

## Exact affected first-party listings

| Listing/file | Current execution | Required declared route change |
|---|---|---|
| `skills/diff-triage/arcade.json:108` | `adapter: skill`, `credential: api-key`, `entry: SKILL.md`, model `claude-sonnet-5`, no capabilities; $0.07 inference ceiling/120s | Preserve adapter, credential, schema, prompt, bounds and capabilities. Change model to `glm-5.3-flash`; append `ANTHROPIC_BASE_URL` beside `ANTHROPIC_API_KEY` in secrets. This is the LLM case B13 actually runs. |
| `skills/counterparty-brief/arcade.json:142` | `adapter: claude-api`, `credential: api-key`, `entry: agent.ts`, no manifest model; web-search/hire-skills; $0.12 inference ceiling/90s | Same explicit base-URL secret declaration; add `engine.model: glm-5.3-flash`, but also resolve the model precedence defect below. Preserve granted capabilities and limits; do not pretend unsupported web search works. |
| `skills/counterparty-brief/agent.ts:22` | Hardcoded `claude-opus-5` actually used by the module-backed API engine | Must not remain the effective request model once the proxy is selected. Prefer a narrow tested manifest model override in the harness, with agent-module model retained only as fallback when no manifest override exists. Alternatively an explicit agent-module model edit is needed, but manifest-only change is insufficient today. Root chooses that compatibility fix. |

There are no first-party manifests whose literal adapter is `claude-agent`; `diff-triage` nevertheless uses that runtime through `skillEngine.run → runClaudeAgent`. The other six first-party listings are script/MCP/OpenAPI and should not receive these secrets or model overrides. In particular, `search-arc-docs`, `query-docs-filesystem-arc-docs`, and `fx-rate` retain credential `none`.

Both LLM manifests currently declare private `egress: ["api.anthropic.com"]`. That no longer describes the child transport when it contacts 127.0.0.1. Propose an honest loopback egress declaration alongside the route change; do not claim the existing egress list is enforced by an OS network sandbox (exec does not consume it). The proxy, not the skill process, performs the further upstream hop. Keep endpoint/model/secret declarations private through `toPublicListing`.

## Concrete source mismatches / defects to address by TDD after release

1. **Module-backed manifest model overrides are currently ignored.** `exec.ts:137–149` correctly sends `engine.model` in engineConfig, and `harness.ts:151` passes it to the SKILL.md loader. But `harness.ts:153–158` returns an imported agent unchanged for `claude-api`/`claude-agent`. Thus the requested manifest edit alone still sends `claude-opus-5` from counterparty-brief. Add a real subprocess harness regression which records the provider's effective model, with a conflicting module model; require GLM from the manifest, and preserve module fallback without an override. Do not broadly merge arbitrary config or capabilities into the agent as a side effect.

2. **The approved zero-price table entry is necessary, but does not cover B13's Agent SDK lane.** `claude-api.ts:22` PRICING lacks GLM; `runClaudeApi:219` rejects unknown models before iteration whenever maxCostUsd is declared. Add exactly `"glm-5.3-flash": { input: 0, output: 0 }`, preserving unknown-model rejection and existing paid models. `estimateCostUsd` still charges nonzero server-tool usage and counts input/output/cache tokens for separate token bounds. But `diff-triage` calls `runClaudeAgent`, which delegates maxBudgetUsd and uses the SDK result's total_cost_usd (`claude-agent.ts:199–200,235–240`), never this table. Actual GLM Agent SDK budget behavior needs proof; do not state that this table makes its inferred cost zero.

3. **Existing direct API completion can bypass its inference ceiling on the submitting turn.** `claude-api.ts:297` breaks immediately after seeing submit, before maxCostUsd/maxTokens/maxToolCalls checks at301–310. Parent exec later checks tokens/toolcalls but not cost. A final submit with server_tool_use charges above the cap can therefore return success even with the new zero-token GLM price. This is a source-demonstrable pre-existing defect, not a captured Red in this read-only task. Add a focused failing final-submit cost regression, then perform ceiling checks before successful completion; preserve refusal precedence. Also test final-submit token/tool-call bound behavior rather than weakening caps for the free route.

4. **Real sandbox transport coverage is missing.** Current `claude-agent.test.ts` injects a fake query; skill harness integration mocks the SDK module. They do not prove the bundled CLI can complete StructuredOutput through the actual local Messages proxy with the GLM alias. The owner tested custom tool_use, not the whole `query → bundled CLI → streaming/tool result → structured_output` path. Test the real Agent SDK against a finite loopback HTTP fixture (dummy key only), including Messages/SSE and any preparatory requests, without mocking query. Assert every model-bearing request remains GLM, the exact proxy origin receives the dummy key, no auth goes elsewhere, the output contract survives, and success/refusal/incomplete remain distinct. Do not add Claude fallback aliases or broaden tools to get a green result.

5. **Child runtime and cleanup need real-path regression probes, not assumed safety.** `exec.ts:191,200` invokes `bun run` without `--no-env-file`, despite building a scrubbed environment. Plan an actual child test with dummy `.env` sentinels in a unique test directory: ensure undeclared credentials/runtime injection cannot be reloaded by Bun. Agent SDK may also create its own child process and local files; parent exec's finalizer kills only the harness PID (`exec.ts:167–173`). A finite real-SDK timeout/interrupt test should observe that no owned descendant continues requesting the fixture after completion, and any scratch home/config remains isolated. These are concrete untested boundaries, not claims that this preparation observed a live leak/orphan. Never solve them with process-name-wide kills, owner HOME reuse or killing the shared proxy.

## Highest-value test sequence

Use the existing Vitest runner plus Bun subprocess conventions (`ts-testing`); no new framework. Capture each real failure before editing source. Proposed compact sequence:

1. Decode the two real manifests: exact GLM model, explicit base URL + key names, credential remains api-key, existing bounds/capabilities unchanged; public projection contains no model/base URL/secret names/values. Other adapters remain untouched.
2. Extend `exec.test.ts`/`engine-config.test.ts`: declared base URL reaches sandbox, omitted base URL does not; unrelated keys/seat/OAuth/HOME stay scrubbed. Real child verifies it reaches the actual SDK client, not merely an options object.
3. Extend `claude-api.test.ts`: zero cost for large GLM token/cache usage, still-positive server-tool cost, still-enforced token/tool/turn ceilings, unknown aliases rejected before any request, and final-submit cost breach refuses. The existing unknown-model-without-cap test only asserts “not rejected”; do not treat that as proof of successful execution.
4. Extend real harness integration: conflicting module/manifest model precedence plus fallback when absent, for API and Agent lanes. Keep loaded private agent prompt and schema intact.
5. Actual loopback provider integration for direct API and Agent SDK: capture method/path/model/strict schema/effort/auth privately and assert only safe projections. The direct API sends `output_config: {effort:"medium"}`, strict submit, and can send Anthropic-specific `web_search_20260209`/`web_fetch_20260209` tools. A plain GLM tool call does not establish support for those server tools. Counterparty-brief is outside B13's three rows; report any provider capability limitation rather than silently removing its research capability or fabricating citations.
6. Actual bounded subprocess timeout/abort, dotenv isolation and no post-completion requests. Unique owned temporary directories and process handles only. Exact framework tests remain offline simulations, never the live B13 proof.

Suggested focused files already exist: `packages/runner/test/claude-api.test.ts`, `claude-agent.test.ts`, `engine-config.test.ts`, `exec.test.ts`, `harness.test.ts`, `skill-engine.test.ts`, `demo-listings.test.ts`, `publish-adapters-evidence.test.ts`. A new dedicated real proxy-transport test file may make scope clearer. No tests were executed during this preparation.

## Actual B13 evidence contract and later launch constraints

- `scripts/e2e-publish-adapters.ts` is an import-safe wrapper. The runner-owned `publish-adapters-evidence.ts` defaults to exactly `diff-triage`/skill, `search-arc-docs`/mcp, `fx-rate`/openapi; validates unique IDs and expected adapters; attempts every selected case; requires succeeded + explicit end_turn + nonempty output; sanitizes provider failures and prints only byte counts. Full success requires 3/3 and exit0. Explicit --only means PARTIAL, never full evidence.
- The report explicitly says it runs no hub, no payment settlement, and no output-schema validation. Preserve that honesty. A nonempty provider result cannot be promoted to a hub schema/settlement claim. The prior independently verified B13 FX payment (`[private heartbeat omitted]`, 03:25 entry) is separate evidence; a fresh paid purchase is not necessary merely to prove the new LLM route unless root/user expressly scopes one.
- The shell does live public MCP introspection first, then OpenAPI preview, then all three private/public directory previews, then execution. `set -euo pipefail` intentionally makes a failed preview nonzero. Do not bypass MCP failures, mock a live row, or relabel partial2/3 as full3/3.
- A later isolated launch should retrieve only `arcade-claudex-key` inside the approved subprocess, set ANTHROPIC_BASE_URL to exact127.0.0.1:8317 and ANTHROPIC_API_KEY to that value, and use an explicit safe environment/no dotenv. Do not pass Arc private keys, Circle credentials, OAuth seat variables, shell injection variables, or owner config. Do not print env, raw provider responses, SDK debug logs, or authorization headers. Existing scripts should be adjusted/tested for no-dotenv semantics if needed before live use.
- No runner/hub service is required for the local three-adapter step. Do not start one, restart the proxy, or mutate owner runner state for convenience. If a real Agent SDK run writes scratch config, use a deliberately owned isolated execution arrangement and preserve exact source manifest/prompt bytes for provenance; do not silently alter the canonical listing to a different adapter.
- Live GLM/free status is the owner's route decision, not a new general pricing claim. Record observed nonempty structured output, stop category, full summary, exit code and duration, without pretending to have verified upstream billing through a zero table entry. Unavailable/unsupported transport remains a failed case until genuinely resolved.

## Status

READ-ONLY PREPARATION COMPLETE. Await root's B13 implementation/live GO after D13. No changes or reruns of frozen E14. Source-level findings above are ready for precise Reds; no fabricated Red/Green or live success is claimed.

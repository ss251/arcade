> Sanitized historical execution artifact. Statements reflect their recorded checkpoint and may be superseded. Historical commands are not current instructions. See the [current runbook](../../../runbook.md) for current behavior, approvals and operator commands. Personal/runtime locations and private artifact links may be redacted; public evidence and test distinctions are preserved.

# Task 13 — Publish adapter evidence

Commit a4be9f5 (base f98cab8); independent read-only review clean with all 31 new evidence tests rerun successfully.

Implemented the three-adapter evidence shell, a thin root TypeScript wrapper and a testable runner-owned module. Defaults to all three; repeatable `--only` is explicitly PARTIAL, names exclusions and never presents partial execution as full proof. Missing, duplicate, wrong-adapter, failed, refused, incomplete and empty outcomes fail; remaining selected cases continue. Raw provider diagnostics and result bodies are not logged. Updated the seller guide and appended verified Plan B evidence to the runbook.

TDD: missing evidence module failed on 2026-09-05 03:19:09 IST; 31 offline tests passed at 03:22:04. Tests cover unit outcomes plus actual Bun wrapper and shell subprocesses from unrelated working directories, argument forwarding and pipeline exit failures. Final gates at 03:27:10: `bun run test` passed 1,021 Vitest tests in 65 files and 29 Bun tests in four files; `bunx tsc --noEmit`, `bash -n scripts/e2e-publish-adapters.sh` and `git diff --check` passed. No web or contract code changed.

Live command: `bash scripts/e2e-publish-adapters.sh --only search-arc-docs --only fx-rate` exited 0 with 2 succeeded, 0 failed, explicit partial evidence; MCP 42,790 bytes, FX 69 bytes, both succeeded/end_turn. Full live three-adapter execution is OWNER-pending: ANTHROPIC_API_KEY is absent and no authorized Anthropic Keychain item was supplied. Exact owner action recorded in main [private owner-action ledger omitted]. No subscription credential substitution or key discovery was attempted.

Independent paid FX proof used a real loopback-only hub (127.0.0.1:28787), FX-only runner and actual buyer CLI on Arc testnet. Existing buyer/deployer keys were read only within consuming environment assignments. Buyer maximum was $0.01. Receipt settled=true, reason=ok, rail=eip3009, network=eip155:5042002; 10,000 atomic price, 9,500 seller, 500 platform fee accrued, no withdrawal. Transaction: 0xb0cbe2a50de1c4daa1f56d2a33acadfb9ac32649bc33e2f99b7ec6e3c1aca613. One independent eth_getTransactionReceipt returned status0x1, expected FeeSplitterV2 destination and matching six-decimal ERC-20 transfers. No waitForTransactionReceipt. FX result USD/EUR 0.86044, date2026-09-04, upstream cost0.

Stopped only the owned hub PID58743 and runner PID60365; verified no listener remained on28787. Retained evidence database at <LIVE_RUN_ARTIFACT> No saved runner configuration, Keychain item, public deployment or mainnet change; no push. A9's distinct sub-buy-key prerequisite remains separately pending.

Deviations: root Effect imports in the literal script cannot resolve from root dependencies, so implementation lives under the runner and the root wrapper imports it. CLI arguments precede `--`, preserving B9's server-argv boundary. Testable strict failure checks and partial mode replace the plan's overly optimistic output-only printout. Local execution expressly does not claim hub schema validation or payment; the separate paid proof does. B implementation is complete; full Anthropic live evidence remains owner-pending.

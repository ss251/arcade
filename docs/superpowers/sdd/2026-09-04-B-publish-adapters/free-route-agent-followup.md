> Sanitized execution/review checkpoint, September 5, 2026. This is local fixture evidence, not live provider or payment evidence. Original private source retained unchanged; later dated records can supersede pending statements.

# B13 Agent SDK safety follow-up — September 5, 2026

Status: owned SDK source frozen for independent review; final complete-response relay native verification passed. No live proxy, provider, key, Keychain, payment, or Git operation performed by this agent. Public historical SDD snapshots are unchanged. Parent owns review, final gates, commits and any later live authorization.

Owned source: `packages/runner/src/engines/claude-agent.ts`; owned tests: `packages/runner/test/claude-agent.test.ts` and `packages/runner/test/free-route-agent.bun.test.ts`. A separate sibling owns the new per-job relay helper and its tests.

## Genuine test-first evidence (IST)

- 13:15:30: throwing `Query.close()` bypassed abort/reaping/scratch cleanup. A real inert child was owned by the regression and cleaned by its `finally`. Fixed external-close guarding and signal-error accounting; 25 Vitest tests passed at 13:17:06.
- 13:31:51: API scratch options lacked native bare mode. Installed SDK 0.3.220 bundles CLI 2.1.220. Native source's `rqi()` prefetches two Keychain records unless `rf()` (the `CLAUDE_CODE_SIMPLE` / `--bare` control) is true. The OS-restricted real native test reproduced an EPERM startup crash because execution of `security` was denied. API-only `CLAUDE_CODE_SIMPLE=1` suppresses the attempt; the personal/Keychain deny rules were not weakened.
- 13:35:49: terminal refusal was erased by the SDK's later teardown exception. Stop reading after the terminal result, retain normal close/abort/reap in `finally`; do not relay private SDK prose.
- The five actual `execSkill → harness → SDK → bundled native CLI` scenarios then passed: StructuredOutput success, refusal, provider HTTP 400, outer eight-second timeout and Effect interruption. All observed child identities disappeared and request counts stopped increasing. The installed CLI's same-origin HEAD `/api/hello` is an unauthenticated connection warm-up, not another model request; both auth headers were absent. Every Messages/count-token model was exactly `glm-5.3-flash`, with StructuredOutput as the sole tool.
- 13:38:02: 30 failing accounting/privacy/terminal-state assertions. Reject missing, negative, nonfinite or unsafe counts/cost; include cache tokens and server tool counts, enforce all final ceilings; use fixed error categories and own-property bounds lookup. 55 tests passed at 13:39:09.
- 13:40: native 302 handling genuinely forwarded the dummy API key to a second owned loopback origin. Both origins were OS-allowed, so this was an application redirect defect, not an OS-blocked request. Parent authorized a bounded per-job transport guard; relay integration remains in progress.
- 13:41:18: strict end-turn-only handling rejected the real SDK's StructuredOutput `tool_use` terminal. Permit that terminal only after an observed StructuredOutput call and the successful structured result; real native success passed afterward.
- 13:44:46: an actual inert child filling its stderr pipe stalled until timeout. Custom SDK spawn must consume stderr itself. Incremental discard (`resume`) fixed it without logging private content; 58 unit tests passed at 13:44:57.
- 13:46:25: relay integration tests genuinely failed because no relay was invoked, original upstream environment remained exposed, and relay-close failure was ignored. Integrated the sibling's real per-job relay; native receives a random local capability and local URL only. Upstream auth, arbitrary Anthropic headers and alternate provider/auth modes are removed from native environment. Relay close failure refuses success.
- 13:53:28: four contradictory/missing `is_error` success flags genuinely failed; require the SDK's explicit false success flag. 65 unit tests and global typecheck passed at 13:53:58.
- Seven actual native scenarios passed in 17.94 seconds, 93 assertions, with the first relay implementation: prior five plus HTTP 302 credential-isolation and retryable HTTP 503. Redirect target received no requests; both error cases caused exactly one upstream Messages request. The sibling is hardening relay response completion against a separate Bun partial-stream issue; these results do not pre-claim the final relay version.
- 13:54:55: two genuine regressions showed an ambient proxy could divert the native local capability and the subscription binary override also applied to API jobs. Strip native proxy variables for relay jobs and keep the bundled API binary; 66 unit tests passed at 13:55:10, followed by global typecheck and diff check. Subscription mode retains its existing explicit wrapper control.

## Final focused checkpoint

Sibling relay's complete-response version froze at 08:30:38 UTC after 21 helper tests / 79 assertions plus typecheck. The seven actual native scenarios then passed again in 17.92 seconds / 93 assertions (owned exec session 42498, exit 0). Both redirect and 503 cases made one upstream Messages request; the redirect destination received none. Timeout and Effect interruption removed the observed owned native/harness processes and request counts remained stable.

Read-only compatibility observation sent to parent: Bun fetch may decode compressed responses while `Content-Length` still describes encoded bytes. Parent authorized the sibling's identity-encoding request/response policy. Its genuine gzip regression failed at 08:33:34 UTC, then 22 helper tests / 84 assertions and typecheck passed at 08:34:35 UTC. Compressed responses are explicitly refused before any native bytes and poison subsequent upstream sends.

FINAL: actual seven native scenarios passed again against that exact identity-encoding helper in 18.04 seconds / 93 assertions (session 96375, exit 0). Unit suite 66/66 passed at 14:05:17 IST, followed by global TypeScript and diff check exit 0. Parent approved a fixture-only noisy-child allowance of two seconds (test four seconds) to avoid startup pressure flakes; the original 0.5-second undrained-pipe Red and all assertions are retained, with no product deadline change. Sibling's independent SDK source/test review was CLEAN. This agent independently read the complete final relay helper and its test file: CLEAN for the agreed custom-API route scope. Parent full gates and Git operations remain separate; source is frozen.

## Proof boundary and fixture safety

The native proof uses the installed SDK and installed bundled binary, never a mock SDK or substitute native executable. Only provider Messages/SSE responses are simulated. Workers receive a dummy key and owned loopback provider URL. API config is unique private scratch; parent HOME is not replaced. The macOS fixture denies external outbound networking, reading personal home except required repository/runtime inputs, executing `security`, and contacting the security daemon. Native/harness PIDs are selected only by the owned worker's child tree, and cleanup checks PID start/command identity before signaling.

Before relay integration the OS profile allowed only the two selected fixture ports. The real relay binds another ephemeral port, so parent explicitly approved a loopback-only profile for the final fixture; this is not claimed to be exact-port network isolation. Exact provider paths/models/credentials and no redirect arrivals remain separate assertions. Non-macOS skips this OS-specific native proof explicitly.

The final fixture also denies personal-home writes; it never touches real Claude configuration or substitutes a `security` executable. Its dummy-only temporary diagnostic observer was removed after diagnosing startup. Only explicit custom API-key base URLs use the transport guard; this report does not claim that the unchanged default endpoint or subscription lane uses it.

Fixture-only corrections are not product Red/Green claims: initial sandbox listen restriction, invalid sandbox address grammar, a simulated response that never resolved during fixture cleanup, and Bun 1.3.14's SIGUSR1 crash. The interruption proof now uses an owned marker to invoke real `Fiber.interrupt`, not a runtime-reserved signal. No Bun crash report was intentionally submitted; external networking was denied.

SDK-reported cost is not independently verified provider billing, and a custom model may receive the SDK's fallback pricing. No claim that the SDK's `glm-5.3-flash` cost is zero. Failed envelopes with cost zero do not prove zero provider spend.

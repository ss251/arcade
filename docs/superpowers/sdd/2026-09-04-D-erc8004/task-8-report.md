> Sanitized historical execution artifact. Statements reflect their recorded checkpoint and may be superseded. Historical commands are not current instructions. See the [current runbook](../../../runbook.md) for current behavior, approvals and operator commands. Personal/runtime locations and private artifact links may be redacted; public evidence and test distinctions are preserved.

# D8 — bounded downstream attestation queue

Follow-up05:39:47 captured five genuine Reds for transferred/malformed/unreadable/throwing/stalled current owner lookups. A fresh5-second ownerOf==job.seller guard now runs after validating all documents and before any write;26/26 Green05:40:04 with tsc. This prevents a job from borrowing another seller's agent through a stale Hello or shared approved hub operator. External cancellation remains interruption, and unavailable ownership never changes the receipt.

Exact AttestJob, AttestOutcome, Attest, Tag/Layer/processOne/makeAttest APIs; makeAttest correctly declares its Scope requirement rather than the plan's impossible never environment. A single scoped worker serializes role-wallet transactions. A dropping512 queue accounts accepted work before offering, so idle cannot resolve between dequeue and processing; failure/shutdown cleanup preserves cancellation rather than spinning.

All three documents and hashes validate before the first write, reuse the receipt timestamp, exclude private payloads/reasons, and check Arc registry/chain provenance. A settled claim without a full transaction hash is refused. Request failure prevents response; an independently failed response does not erase truthful payment feedback. Each call has a90-second deadline and retries once ONLY when the error explicitly proves retryable:true. The plan's blanket retry could repeat an already broadcast transaction; production D2 deliberately marks uncertain sends false. Unspecified retry safety also means no retry. Diagnostics are fixed public-safe strings.

TDD missingmodule Red05:08:37,19 Green05:10:16,21 Green05:14:31 after deadline/closed-scope tests. Tests cover exact hashes/order, malformed data, privacy, safe/unsafe retry, synchronous throws/defects, queueoverflow, nofalseidle, stalled-worker shutdown, and post-scope calls. Independent source/all19-initial-test review CLEAN; two later tests changed no source. Root fullgate containing first19 tests passed1411Vitest+48Bun beforeD5 commit; D8 will have its own ordered finalgate/commit afterD7. No registry/network/key calls.

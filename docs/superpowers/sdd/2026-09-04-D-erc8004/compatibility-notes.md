> Sanitized historical execution artifact. Statements reflect their recorded checkpoint and may be superseded. Historical commands are not current instructions. See the [current runbook](../../../runbook.md) for current behavior, approvals and operator commands. Personal/runtime locations and private artifact links may be redacted; public evidence and test distinctions are preserved.

# Plan D implementation corrections

2026-09-05 04:36 IST. Full plan read before work. Sources verified read-only: https://docs.arc.io/arc/tutorials/register-your-first-ai-agent (all three Arc addresses and write/read signatures); https://raw.githubusercontent.com/ethereum/ERCs/master/ERCS/erc-8004.md (registration wire metadata, events, read tuples, approval prohibition). No key or transaction involved.

- D1: current ERC registration type is its registration-v1 URI and uses services/supportedTrust. Keep plan endpoints/supportedTrusts names as compatible aliases, add standard fields, optional known agentId registration linkage. No made-up token id or image URL. Current hub has no HTTP /mcp endpoint; local buyer stdio MCP cannot be advertised as a nonexistent hub route. Only actual endpoints advertised.
- D2: literal wallet wrapper swallows receipt errors/reverts and returns an unconfirmed hash. Must return success only after confirmed successful receipt; no write retry after uncertain broadcast. ChainConfig, no duplicated hardcoded chain; invalid/empty env keys disable only feature with fixed diagnostic. Reader data must be structurally validated and filtered by validator, agent, tag, response, update time; feedback must filter attester/tag/value/decimals/not-revoked. Cache bounded and concurrent reads deduplicated; RPC reads bounded, no getSummary. Errors cannot contain raw RPC URLs/keys/provider causes.
- D3/D6: reject forged mint logs from other contracts or nonzero Transfer senders; unreadable balance is unknown, not fabricated funded. Persist a successful mint before operator approval so rerun cannot mint twice if approval fails. ERC721 setApprovalForAll also enables transfer, and warning/explicit authority must say so. Do not retry a submitted transaction as a fresh mint. Use test path seam instead of repurposing HOME.
- D5/D7: liveness must account for canary delisting and actual runner/listing identity; agent claims bounded, no forged ownership claim counted as verified, no extra fields in signed digest.
- D8/D9: all sync throws/defects and queue failures caught without swallowing interruption or leaking private provider errors. Queue capacity bounded, worker survives failures and maintains idle counter even on defects. No chain operation on settlement path; timeout/interrupt-safe handoff after durable receipt only. Real server must not attest simulated-rail receipts as real chain facts.
- D10: persist bytes before memory state/chain write; reject conflicting immutable bytes rather than silently commit a different document hash. Handle sqlite failure without publishing false durability. Public docs contain hashes only and safe canonical reasons, never buyer payload/provider error text.
- D11/D12: no score wording even in negation (plan tests reject it); withhold counts for stale/unverified ownership and make recent bounded denominator precise. Escape all UI fields. No missing-hash fake links.
- D13: OWNER keys not created/accessed/substituted. Implement safe isolated evidence script with bounded checks and actual independent chain evidence, never unsafe kill0/init/HOME rewrite from literal example. Log exact owner steps and continue independent tasks.

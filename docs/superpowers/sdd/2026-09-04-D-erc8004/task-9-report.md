> Sanitized historical execution artifact. Statements reflect their recorded checkpoint and may be superseded. Historical commands are not current instructions. See the [current runbook](../../../runbook.md) for current behavior, approvals and operator commands. Personal/runtime locations and private artifact links may be redacted; public evidence and test distinctions are preserved.

# D9 — receipt-first pipeline hook

Pipeline-only implementation adds exact optional RunJobArgs.attest context. Service lookup is optional and follows successful store.putReceipt. The hook carries terminal outcome/payer/payTo and the receipt's own timestamp. Both the server integration (pending D7 server handoff) and pipeline guard must exclude simulated rails. An Effect.suspend boundary captures synchronous service throws; a50ms enqueue timeout bounds a broken hook without waiting for any chain work. Queue defects do not change the persisted receipt or settled balances. Canary and lineage fields remain untouched.

Initial copied short testjob id failed current JobId schema, so the fixture was corrected before judging the intended regression. Genuine missing-hook Red05:17:04 (two cases); implementation then28/28 pipeline/unchanged pipeline/tree tests green05:17:28 with tsc/diff. Nine new tests exercise successful/failed facts, actual receipt visibility at enqueue, missing service/context, simulatedrail suppression, synchronous/Effect defects, a stalled queue, and receipt-persistence failure. All settlement amounts in these tests are simulated; no real-rail evidence claim.

Server integration complete05:50:13: shared Erc8004 layer provided to AttestLive, runtime includes AttestTag; context only for real-rail, armed, ownership-verified identities. The worker independently rechecks current ownership before broadcasts. Actual HTTP tests use real router/pipeline and explicitly simulated Gateway-shaped test service, no RPC; successful/failed/brokenqueue paths all see an already-stored receipt with exacttimestamp/input/output/payTo, while unverified/missing/unarmed/test cases neverenqueue. Initial fixture encoder/reason typos were corrected before genuine3missing-hookReds05:45:22;35/35 focusedGreen05:50:13. Root complete source/test review clean. D7 snapshot was staged before D9 server changes; those changes stay outside D7 commit. Ordered D9 fullgate pending.

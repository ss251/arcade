# H10b6 — active chat purchase authority and custody

After H10b5 `e87ae07`, root-only under the owner load restriction. No new spending,
owner keys, production changes or push. Wire the real Chat/SDK/Confirm to H10b4.

Use a conversation-owned private approval scope, pending permit/wallet snapshots,
bounded active operations and separate capability-free UI views. Create/close the
owner through StrictMode-safe setup/cleanup. Restored initial purchase calls,
replayed output, generic transcript rendering and approval flags alone cannot
authorize any signature. Mint only in a live genuine Confirm callback against
the exact original approval/tool-call/input/ceiling and complete displayed quote.
Drop/burn before effect re-entry and before SDK or downstream failure can retry.

Pending UI must obtain full bounded actual-input/ENS terms and a selected wallet
account/chain, block unknown states, and offer connect/switch only on explicit
user action with fixed diagnostics. Feed full private identity to H10b5; changing
input/context while held cancels. Only a matching approved SDK output consumes a
local permit. Source signing coordinates from the retained quote, never output.

Retire the old auto-mounted Settlement/signPayment and /api/settle courier path.
The retired server route performs zero quote/signature forwarding/result IO.
Render only closed H10b4 views as live purchase evidence; raw historical tool
results remain qualified unverified transcript claims. Never send private run
output, recovery tokens or signatures into SDK messages/model/history/SSR/URLs.
Keep complete paid output escaped/selectable and storage limitations explicit.

Prewrite focused capture/lifecycle/no-reentry/restore/error/custody tests. Preserve
old E13 actual-input/ENS/redirect/retry/poll/privacy regression intent at the new
direct boundaries instead of silently deleting coverage. Exercise the actual
React/SDK path and two-origin browser transport with bounded owned synthetic
fixtures, actual local signature recovery and no live funds/provider. Report
simulated receipts and native DOM/CORS separately from independent chain proof.
Reap all owned services. Freeze, exact strict, self-audit and one max4 sequential
full gate before the local commit. Buyer/session/H11–14 remain separate.

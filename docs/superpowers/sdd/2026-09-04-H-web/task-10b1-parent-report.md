> Sanitized historical execution artifact. Statements reflect their recorded checkpoint and may be superseded. Historical commands are not current instructions. See the [current runbook](../../../runbook.md) for current behavior, approvals and operator commands. Personal/runtime locations and private artifact links may be redacted; public evidence and test distinctions are preserved.

# H10b1 single-threaded approval foundation

September 6, 2026, after H10a `e8ea873`. Owner machine-load restrictions remain
active: no delegates, parallel reviews or overlapping gates. Parent authored and
reviewed this small pure foundation; no independent-review claim is made.

## Contract and actual work

Read the accepted H10 authority/transport preflights, full original Task10 and
current chat/Confirm/purchase/settlement/history/wallet/input contracts. Installed
AI SDK approval signature, validation and request-output sources confirm the
server HMAC binds approval ID, tool call ID, tool name and canonical input digest.
That server control remains unchanged. Restored tool output is not a new human
gesture; actual UI composition still needs the later H10b integration.

Wrote the brief and38 focused tests before implementation. Did not run a missing-
module failure or call it a product Red. Implemented the private WeakMap token
scope; first run at13:49:14 IST passed38/38 (944ms total). Parent source review
then added six bounded supplemental cases: fresh invalid-environment import,
retained unused authority at capacity, bad-binding consumption, delimiter-safe
SDK identity pairs, inherited toJSON/null-prototype input, exact node/array bounds.
These were Green on the implementation, not claimed as additional product Reds.
Final focused run at13:51:05 passed44/44 (761ms total,50ms test time).

The testing skill guided prewritten boundary cases and explicit evidence limits.
Installed Node-hosted Vitest used command-local maxWorkers4/minWorkers1/
maxConcurrency4 in a clean environment with no automatic dotenv. Exact two-root
TypeScript program using the actual nested web config returned zero diagnostics;
no source casts to silence diagnostics, generated support or emitted files.

Scope approval captures own-data IDs/tool/skill, a canonical positive ceiling,
bounded canonical actual input and a separate immutable H10a context. Quote
freshness is a later controller check, not proved by this pure capture. Only explicit
EIP3009/Gateway contexts qualify; Test cannot mint a spending permit. The opaque
null-prototype frozen object has no own data and cannot be reconstructed from
JSON/properties or consumed by another scope. A private request is returned only
once after a matching binding; mismatches burn the token too, before wallet work
could begin. At most128 identity-pair tombstones remain without eviction/rearm.
Unused authority expires at five monotonic minutes; invalid/backward clocks
refuse, and closing drops unused authority without revoking signed downstream work.

Input bounds are131072 encoded JSON bytes,16384 nodes and depth32. Serialization
uses only captured own data, canonical object ordering and original array order;
no getter/toJSON invocation, symbols, sparse/extended arrays, cycles, exotic
objects, lone surrogates, undefined, bigint or nonfinite numbers. Immutable input
text prevents later source mutation. Global/proxy hostility is not sandboxed.

## Frozen source pins

- `apps/web/src/lib/purchase-approval.ts`: `ca60dff468fff696c969237b1db50e67f36cc31458c48c990cc3d61719dc466a`
- `apps/web/test/purchase-approval.test.ts`: `42790a2e468d4b3fa12fa8a381f75a880166261e4919b82752ca5b522651ef06`

No active Chat/Confirm/wallet/signing/relay/HTTP/storage/F source changed. No
network request, real wallet/key, payment, spending approval replay, deployment,
production ENS record, mainnet operation or push occurred. No native browser
approval, SDK continuation or direct purchase flow has been proven by these pure
tests. Source is frozen for the sole bounded full gate and local atomic commit.

## Sole full gate checkpoint

An approved read-only process inventory found no test-gate/worker candidates and
printed no raw process arguments. The sole full gate started13:54:08IST using
the same explicit command-local caps as H10a: Node-hosted Vitest maxWorkers4/
minWorkers1/maxConcurrency4, sequential Bun max-concurrency4 without parallel
workers, then root/web strict and actual client/SSR builder with envDir:false.
Vitest passed3,652/153 in49.07s. Bun/type/build stages remain in flight at this
written checkpoint; no full sweep is repeated and no delegate is active.

## Completed gate

The same sole gate completed successfully: 3,652 Vitest tests across153files
(49.07s);834 Bun tests across54files with6,091 assertions (162.50s); root/web
strict TypeScript; actual client309ms and SSR149ms builds. The process closed
with exit0. Assertion counts are recorded from this run, not copied from H10a.
No test sweep was repeated. Source pins remained unchanged; public-copy audit
and an explicit six-path atomic commit follow. This remains a pure unused
foundation, not completed H10 or proof of active purchase safety.

# H13b — original-name browser approval

2026-09-06 after 2d9b67e, root-only/max4. H13a provides keyless ENS resolution;
this step connects it to the real Chat/Confirm lifecycle. Use existing UI tokens,
TypeScript tests and owned native harness. No new dependencies or live money.

Keep the original exactly-one target distinct from its resolved skillId. The
private scope captures both original binding and complete displayed context;
consumption compares name/id/input/ceiling/call identities and burns once. A name
approval requires the displayed verified ensName to match. Pending SDK output
must retain original name and resolved skillId from the approved context. Raw,
restored, preliminary, late, changed and duplicated output cannot mint authority.

Client quote keeps string id compatibility and accepts only a closed name target,
sends actual input to fixed same-origin POST, and requires name/id coherence in
both response projections. Known expiry/payee mismatch are closed typed results
from the response decoder, unwrapped outside ordinaryHttp; never forward raw
provider or server details. The real card shows the requested name and resolved
id, or an honest typed blocker. Do not request a wallet for failed resolution.
Original-name quotes run before and after signing; compare all displayed terms,
refuse changed resolution before signing or forwarding, no silent ID fallback.

Tests before source: capture/scope/conversation name binding and one-use/replay
barriers; client target transport; actual route and offline signer; UI/native
name success, mismatch/expiry and stale completion. Strict compiler, actual
finite isolated browser with synthetic SDK/provider/hub and owned cleanup.
One full sequential gate after source/native freeze, small local commit. No
owner key/live ENS/replayed approval/payment/production change/push. H14 owner
review/capture, durable session gap and the remaining canonical queue follow.

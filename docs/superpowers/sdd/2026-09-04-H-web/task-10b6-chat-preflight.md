> Sanitized historical execution artifact. Statements reflect their recorded checkpoint and may be superseded. Historical commands are not current instructions. See the [current runbook](../../../runbook.md) for current behavior, approvals and operator commands. Personal/runtime locations and private artifact links may be redacted; public evidence and test distinctions are preserved.

# H10b6 active Chat integration preparation

September6,2026, while H10b5 source is frozen. Root-only preparation; no source
release, new live authority or concurrent test gate. H10b4 is committed afe2026;
H10b5 confirmation gesture fixes/native checks await their single gate/commit.

The installed SDK preserves approval on output-available. Extend the structural
UI part with toolCallId and bind approval.id/toolCallId/toolName/original input/
skill/ceiling, never output-supplied money/resource. Reopened initial call IDs have
no locally minted authority; they must not auto-sign or become a fresh active
card solely because a stored part claims approval-requested/approved. Require a
new request instead. Pure Thread/default transcript rendering is passive.

Create a conversation owner in effect setup, before the messages effect, so
StrictMode's initial setup/cleanup/setup leaves one ready live owner. Cleanup
closes unused scope permits and aborts every active run. Keep a private ref/map of
approved requests and selected provider/buyer; remove a pending entry before
calling H10b4 so duplicate output/effect execution cannot repeat signing. Callback
updates check the exact owner generation before setting component state. All
public progress/result views are separate from SDK messages/onChanged/history;
do not attach tokens/signatures/recovery rows or paid output to model payloads.

PendingPurchase needs a complete captured quote, canonical actual input and
current selected wallet account/chain. Unknown reads block rather than optimistically
enable. Account/chain inspection is read-only; connect/switch stays an explicit
remedy. Capture and bound wallet reads and stale completion, keep raw errors out
of UI, and carry the returned connected address instead of discarding it. Final
H10b4 signer still rechecks actual account/chain and recovers the signature.

Mint only in Confirm's real completion callback, then place the private entry
before addToolApprovalResponse. If mint/SDK dispatch fails, burn/drop the permit
and show a fixed refusal without raw errors. Use the full private decision key
for hold cancellation and a fresh React key for a genuinely new displayed
decision. A server refusal burns/drops the pending entry with no signing. Only
a matching available tool output can consume a still-live local permit; raw
awaitingSignature or restored approval flags alone cannot do so.

Replace the current auto-mounted Settlement/signPayment/courier path entirely.
Raw historical call_skill output cannot get a settled success/link presentation
from unvalidated fields; render a qualified archived/unverified state instead.
New views use H10b4's exact qualified explorer/UUID semantics and complete escaped
selectable output. No raw reason/detail or no-charge promises. Keep recovery
storage failures explicit; accepted rows are not proven spend.

Retire /api/settle only when the new direct path is wired, returning a fixed
non-forwarding refusal. Move the old E13 actual-input/ENS, redirect/retry, bounded
poll and private-diagnostic test intent to the direct browser boundaries and
retain a zero-IO retirement regression. Preserve earlier historical records.

Native follow-through should exercise actual useChat/SDK tool-part state changes,
the real Confirm, one local signature and a two-origin direct browser POST with
captured capability-header reads. Isolate synthetic HTTP/fixture signing keys;
no live provider, real funds, owner-key read, production URL or old approval replay.
Cover restored output, duplicate effect/re-render, remount/cleanup and denied or
changed decisions. Distinguish real DOM/CORS/shared helper use from simulated
receipt claims; no independent on-chain proof can be inferred from fixture hashes.

H9 restored jobs lack retained buyer/nonce/context and require a weaker historical
hub-reported projection. F session export/continuity remains a separate support
decision; never serialize its mutation-capable bearer as a read-only recovery key.

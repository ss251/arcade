> Sanitized historical execution artifact. Statements reflect their recorded checkpoint and may be superseded. Historical commands are not current instructions. See the [current runbook](../../../runbook.md) for current behavior, approvals and operator commands. Personal/runtime locations and private artifact links may be redacted; public evidence and test distinctions are preserved.

# H7 parent implementation and review checkpoint

September 6, 2026. Source/SSR/focused checks accepted; actual browser review,
the sole full gate and atomic commit remain separate at this checkpoint.
This is isolated H7 preparation on H1–6, not a premature G → H main merge.

## Contract and decisions

The pure bounded geometry retains the plan's exact API and dimensions, validates
the whole positional topology before placement, preserves caller data and never
calculates money. The component consumes the actual H4 TreeView, not fabricated
direct edges from public flat descendants. It renders no new route, request,
capability store, signing UI or payment shortcut.

Every qualified SVG anchor requires recorded settled status, a nonzero full
transaction hash, and the exact H4-supplied ready-network Arc URL. Missing or
unqualified links are not reconstructed. Native details repeat the full labels,
reasons, anonymous positional node/parent relationships and qualified links.
The two normal fixture transactions have two SVG and two text anchors; this is
not four transactions. Root job IDs and unknown/private props are not rendered.

The digest is explicitly recorded evidence, not an independently mined proof.
Incomplete/unresolved states do not imply a refund, untouched balance or zero
spend. Unknown digest/budget fields remain unavailable; malformed topology is
unavailable before partial rendering; empty input has a neutral fallback.

The existing warm semantic design system supplies all colors and typography.
Small node-state text now uses ink for contrast; green/red borders still encode
recorded settlement status, blue remains money, native links use ink. Styles
are tree-scoped, use contained horizontal scrolling and add no animation.
The frontend design and TypeScript-testing skills guided those decisions.

## Actual test-first sequence

- Initial absent-module collection failures are setup absence, not executed
  behavioural Reds. The geometry author's 40 prewritten tests passed on first
  implementation; that distinction is preserved in the author report.
- Parent's initial component run passed21 tests. A later real SSR regression
  for negative settlement wording failed before its correction (22 collected,
  one failed). The text-alternative link/position assertions produced two
  failures among23 collected cases before correction; combined geometry and
  component then passed63 cases.
- A collected details-link-focus CSS assertion failed (23 cases, one failed)
  before scoping SVG focus suppression and giving native text links an outline.
- The synthetic fixture's existing `$0.20` was confirmed against actual
  formatPrice. An earlier `$0.2` review suggestion was wrong; fixture source
  stayed unchanged. Only the separate component test's ceiling/expectation
  changed from `$0.2` to `$0.20` to match canonical display.
- Independent review measured declared 10px state-text pairs at4.3949637:1
  (light settled) and3.4766428:1 (dark negative), below normal-text AA4.5.
  Parent independently computed the same unrounded sRGB ratios. The contrast
  skill wrapper would install absent dependencies, so no install was run;
  the direct fileless formula supplied the calculation instead.
- Parent added the specific small-state-text regression first: **24 collected,
  1 failed /23 passed** at10:41:33 IST. Changing only that scoped fill to ink
  then passed **64 tests /2 files** at10:41:50 IST. Final declared ink/card
  ratios16.4392662 and13.6295243 pass normal AA/AAA. These are source-color
  calculations, not yet browser-computed readings.

## Independent review and boundary checks

Parent fully read the layout/fixture reports and initial/final independent
reviews. Initial review tested100 deterministic bounded trees and actual
H3 → H4 → SSR fixtures; its five-node repeated-skill fallback-link failure
preceded the correction. Final independent review passed64 focused tests,
exact four-root strict checking, and the same real producer probe with five
SVG links, five details links, five distinct rows and no private IDs.
These reviewer results are not additional unique test cases or parent runs.

The fixture uses actual TreeGraph/current CSS, seven synthetic stress states,
loopback-only ephemeral HTTP, no script/provider/hub/wallet service, and finite
process/connection/response bounds. Its import-inertness/SSR checks are author
evidence. The first parent-owned listener served HTTP200/4816 bytes, but its
isolated browser stalled at navigation/evaluation. **No visual pass resulted.**
The listener and browser lifetime fuses ended the run; Chrome required the
supervisor's final KILL after TERM. Same process handles were reaped, the private
harness daemon stopped, and independent probes confirmed both old ports refused.
A separately owned corrected browser run is now under the fixture author's
bounded visual review. Its result and exact cleanup remain pending here.

The accepted F/H integration prerequisite remains open: preserve kind presence
through H3/H4, keep F's session guards/public boolean without IDs, and adapt
three incompatible ordinary-receipt privacy fixtures. H7 cannot recover missing
upstream provenance. No present-invalid kind is approved as legacy on-chain data.

## Frozen parent delta

```text
f71e9702a2efdc1ca93a1e319ecde86717ae91e3aceb8c0f4b853d2ab51b39cc  apps/web/src/components/tree-graph.tsx
503918fda8fa025914270aac9defcc1e8e57d838018c22eda92dd8d07770b1ad  apps/web/src/styles.css
3505068621267764678f7293ff2e843e7eacc3305a9fcea0ab628f90ae4a844c  apps/web/test/tree-graph.test.tsx
4edac462f5b75b30213a14922972a2500c188687af6c63245734be6f9c0c71e5  apps/web/src/lib/tree-layout.ts
a737ea079cf44d5f83b5f3322df3c87488149861a98288cae5f54653d921f18a  apps/web/test/tree-layout.test.ts
1aa87050123bfae9d62fb5f7f52e3c780552b1d449891bd66d03c2504050076f  apps/web/test/fixtures/tree-server.tsx
```

No parent full gate, final client/SSR build, H7 commit, main merge or public
deployment is inferred. No wallet key, model request, paid query, approval replay
or push occurred. Later dated evidence will supersede these pending states.

# H14 — bounded read-only page evidence

2026-09-06 after780f958, root-only. Implement six pages times light/dark using
installed Chrome, one browser with max2 renderers, a fresh owned profile/output
and finite startup/page/run deadlines. No added browser dependency. Explicit
hub, seller, skill and trusted Chrome executable; no implicit production reads.
Use the already-built actual Start handler behind a screenshot-only loopback
GET server. Refuse payment/model/preview/private-job endpoints and non-GET
upstream requests; pass no inherited credentials. No automatic build/full-gate
replay inside the script. Output PNGs, source/time/scheme observations and cleanup
metadata to a fresh temporary directory, never committed. Stop owned services.

A new browser profile has no saved jobs or connected wallet: buyer is deliberately
empty and publish is hosted/passive. Do not claim real purchase recovery or local
CLI preview from these frames. App failures/empty feeds are visible observations,
not fictional data. Actual light/dark style must be checked, not inferred from a
filename. Owner ranks the twelve frames before cosmetic polish and separately
drives a real funded-wallet recording. These remain owner-only, not replayed here.

Tests cover closed option/route policy, keyless read-only fetch and child process
cleanup. Run the real script sequentially against selected public data, inspect
every image, then freeze source and run one capped full gate before committing.
No concurrent browser/gate, subagents, new spend, owner keys, approval replay,
production configuration, mainnet operation or push. H session completeness
still precedes H merge; deferred G8/G9, vendor tasks and I follow.

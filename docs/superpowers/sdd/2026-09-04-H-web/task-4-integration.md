# H4 parent integration — September 5, 2026

Parent read the three complete library files, all three new test files, the six
compatibility diffs and both complete original reports. The decoder was checked
against the actual H1 public projection/counters, H2 nullable seller accounting,
H3 positional tree contract, D nested listing evidence and E name responses.
Original quote-function bytes and shared hub-http/purchase/signing/relay files
remain unchanged; no fee or token authority moved into a read-only helper.

The six legacy fixture edits supply actual required public shapes. Their old
behavior assertions are unchanged. The quote streamed-body fixture now stalls
only the payment challenge, retaining its ten-second cancellation assertion;
a separate new fake-clock regression proves detail's 15-second success and
25-second refusal. The V4 dummy language-model correction follows installed
provider declarations, not a new real model or usage/billing observation.

The worker's final frozen checkpoint passed 153 focused Vitest tests, three
owned-loopback Bun tests / 26 assertions, root/web TypeScript, explicit nested
Vitest/Bun compilation and the web build. Its complete reports retain earlier
failed runs and distinguish checker setup errors from real fixture diagnostics.
Parent source review is CLEAN. Parent independently repeated all 153 focused
Vitest tests and three Bun tests / 26 assertions, including stopped-listener
refusal, plus exact web-project/eight nested Vitest and nested Bun strict checks
with zero diagnostics. Independent public-copy review is CLEAN: both reports are
exact bannered copies, 23 local links resolve and privacy checks pass. The full
precommit test/type/web gates follow this frozen checkpoint.

This is a bounded read-only client, not live browser evidence or independent
on-chain/Graph verification. Gateway links remain conservative, monetary unknowns
remain explicit, and large feeds can be refused by unchanged transport limits.
The full F dependency still precedes H/G integration. No live action or push.

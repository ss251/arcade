> Sanitized historical execution artifact. Statements reflect their recorded checkpoint and may be superseded. Historical commands are not current instructions. See the [current runbook](../../../runbook.md) for current behavior, approvals and operator commands. Personal/runtime locations and private artifact links may be redacted; public evidence and test distinctions are preserved.

# H7 independent source/geometry review — initial checkpoint

2026-09-06. The frozen layout pair is CLEAN in this bounded review. Initial
renderer checkpoint has a concrete accessible-fallback gap below. Root is still
authoring its wording/visual corrections; this is not final UI, browser, merge,
or complete H7 acceptance. Canonical G → H integration remains separate.

## Scope and pins

Read complete H7 plan and accepted readiness, full layout/component/test sources,
all H7 style additions, actual H3 builder and H4 decoder/interface/formatter, and
the complete pure-author report. H globals/full plan and F/H preflight were read
for the preceding task. No source/public edit, Git, full gate, build, browser,
network, key, payment or external child/service operation was performed here.
The only written file is this ignored review. B9's separate visual fixture and
root's browser checks are outside this independent runtime coverage.

The TypeScript-testing skill guided public-API geometry tests, real producer /
decoder composition, exact project options and setup-versus-behavior attribution.
The review skill and its code/design checklists guided source/contract analysis.
Its broader Git/network/auto-fix/telemetry/extra-dispatch workflow was not run:
the explicit parent release is read-only, bounded and already independently
delegated. No full skill-pipeline or cross-model execution claim is made.

| File | Initial SHA-256 |
| --- | --- |
| apps/web/src/lib/tree-layout.ts | 4edac462f5b75b30213a14922972a2500c188687af6c63245734be6f9c0c71e5 |
| apps/web/test/tree-layout.test.ts | a737ea079cf44d5f83b5f3322df3c87488149861a98288cae5f54653d921f18a |
| apps/web/src/components/tree-graph.tsx | c5b2f92aec04b7d5fd4e5b19e8bce821305d5ad76d756e600300a0b4150f24c4 |
| apps/web/test/tree-graph.test.tsx | 30a97646fb768be5ea37a4d545b7d23176156c821a39ba8e94edce14e644f1c1 |
| apps/web/src/styles.css | 661f23ca1fd0675b11c862baf4c983dc9599550905fd6bf1995e679d66ae3a58 |
| internal/task7-layout-report.md | 76e5ceb17a84bf1d2b9a120963dacfaba2d2f3394b8713c15a21b6d6972630b4 |

All five source/test/style pins matched before and after the initial focused and
strict checks. G3 independently confirmed its two-path freeze and author report.
Initial renderer stayed at c5b2f92a through the composition supplements below.

## Finding requiring root correction

**P2, confidence 10/10: the full-text fallback omits qualified links and cannot
uniquely identify repeated-skill parent relationships.** In TreeGraph's details
list, the only children are text spans; its parent expression is
`hired by ${layout.nodes.find(parent => parent.nodeId === node.parentNodeId)?.skillId ?? "unknown"}`.
It does not expose safe positional node/parent labels. Two distinct branches
with the same skill and price can therefore have identical accessible rows.
The accepted readiness explicitly calls for full-text links and relationships.

Actual fileless H3 → H4 → SSR reproduction on c5b2f92a built five legitimate
receipt nodes: one root, two same-skill branches, each with a same-skill leaf.
The root used the real canonical flat-descendant treeHashOf commitment and
matching amounts. H3/H4 accepted it as complete. Observed five SVG links,
**zero detail links**, five detail rows but **three distinct row texts**, and no
private job IDs in the output. An assertion expecting five qualified links in
the fallback genuinely failed, exit1. The duplicate-row count was directly
observed, not a separately executed failing assertion (the first assertion
stopped that script). An earlier repo-root invocation could not resolve web-local
React; moving the unchanged probe to apps/web corrected that setup-only error.

Smallest correction: reuse suppliedLink for textual anchors and include the
nonprivate positional node/parent coordinates in full labels. Preserve the SVG
whole-node links, escaping, private-ID omission and layout geometry. When adding
text links, scope `.tree a:focus-visible { outline: none }` to SVG or give textual
links their own visible focus style; the current substitute styles only `rect`.
No change was made by this reviewer; root received these facts immediately.

Root separately announced a genuine-SSR-regression-first change from “not
settled” to “not recorded settled.” That corrects uncertainty wording for an
unresolved reservation. It is parent-owned and not yet verified in this initial
checkpoint. Browser contrast/layout corrections, if any, are likewise separate.

## Independent checks

- Installed Vitest, **61 pass / 2 files** at 10:14:46 IST, exit0: frozen layout40
  plus initial renderer21. Test times31ms/24ms; command duration622ms. Invocation
  from HROOT used empty environment except explicit installed Node PATH and
  absolute Bun with `--no-env-file node_modules/vitest/vitest.mjs run` followed
  by only tree-layout.test.ts and tree-graph.test.tsx. No package-manager command,
  install, route fixture, service or full suite was invoked.
- Exact four-root web TypeScript program: **strict:true, diagnostics0**, exit0.
  Ran from actual apps/web cwd, parsed its absolute tsconfig file identity and
  inherited options, explicitly included the two source/two nested test paths,
  and set noEmit / nonincremental / noncomposite. No generated output/config.
- Fileless deterministic geometry supplement: **100 bounded trees / 24,428
  total nodes**, all accepted. Checks covered exact membership and edge count,
  finite nonnegative in-canvas coordinates, fixed hop columns, every same-column
  pair separated by at least68px, immutable output, unchanged input and exactly
  equal output after input reversal. 923,848 individually executed assertions
  passed in354ms. This is deterministic bounded correctness coverage, not a
  general performance benchmark or proof of arbitrary tree inputs.
- Fileless actual H3 → H4 → TreeGraph SSR supplement: **7 cases / 62 assertions
  pass**, exit0 in154ms. Complete, unresolved reservation, released descendant,
  missing full receipt, root-only, Gateway UUID and actual-shaped Test locator.
  Verified expected node counts/completeness/flags, no private IDs or mined /
  balance-untouched / no-charge text, and no Gateway/Test anchors. All receipts,
  canonical commitments and decoding were real local code; no Broker, signing,
  HTTP, chain or synthetic authorization was involved.

The pure author accurately reports missing-module collection failure with zero
behavior cases, first collected40 Green, and checker cwd/type-resolution setup
failure then exact0. This review does not relabel them behavioral Reds. The
initial renderer's missing-module setup and21 first Green are parent-attributed.

## Geometry and integration conclusions

Layout's own-data bounded capture and fixed exception wrapper, canonical parent
prefix/hop validation, contiguous numeric siblings, topology-before-placement,
disjoint leaf intervals and final visit count provide the stated geometry.
Only H4 display fields enter frozen node copies; rootJobId and arbitrary extra
props do not. It neither sums money nor infers rail or economic completion.
No correction to the frozen layout pair is requested.

H7 uses H4's qualified explorer and checks supplied URL/hash/settled relation; it
cannot recover provenance H3/H4 have already erased. The separately accepted
F/H rebase prerequisite remains OPEN: H3 snapshot drops settleRefKind, H4 feed
decoder/format ignore it, and H1 lacks F's public session boolean/kind projection.
See h-rebase-preflight-current.md. These are upstream integration obligations,
not silently fixed or waived by the isolated H7 tests. No H7 route or new session
capability, refund claim, indexed provenance, global stats or G8 was introduced.

Final component/CSS source checks after root corrections, actual desktop/mobile
rendering, keyboard accessibility tree, readable labels/focus/contrast and exact
owned-browser cleanup remain root's acceptance work. Static markup/CSS assertions
do not establish these visual or interaction facts. No full gate or merge claim.

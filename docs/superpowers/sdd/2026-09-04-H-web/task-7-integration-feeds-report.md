> Sanitized historical execution artifact. Statements reflect their recorded checkpoint and may be superseded. Historical commands are not current instructions. See the [current runbook](../../../runbook.md) for current behavior, approvals and operator commands. Personal/runtime locations and private artifact links may be redacted; public evidence and test distinctions are preserved.

# F/H public-feed integration — scoped author checkpoint

September 6, 2026. **Frozen: 114 canonical Vitest tests /3 files, 19 Bun tests /
319 assertions /3 files, exact six-root strict diagnostics 0.** Root owns the
integrated full gate, independent review, publication and commit. No full-gate
or live-provider result is claimed here.

## Scope and contract

Fully read the retained F/H preflight, actual rebased feed/reference helper,
all five owned test files, relevant Store/session-token/router boundaries and
actual package/Vitest/TypeScript options. The complete TypeScript-testing skill
guided existing-runner use, public boundary tests and explicit failure/fixture
attribution. Only the six released paths below and this private report changed.
No helper, Store, server, tree/web source, config, dependency, main/GROOT file,
Git state or public document was edited. No real key, external network request,
wallet signing/funding operation, browser, Graph command or build was used.

Production delta is limited to the private kind projector and fixed reason:

- Only genuine property absence omits settleRefKind. Enumerable own data keeps
  exactly onchain/gateway-transfer/test; own undefined, malformed data, hidden
  data, accessors, inherited presence and failed reflection become unrecognized.
  Descriptor/presence inspection does not invoke kind getters or coercion.
- session_released remains a fixed public reason, with its authorized amount
  and fee arithmetic unchanged. It is not a refund or no-charge assertion.
- H's explicit root/child whitelist, exact monetary display, bounded references,
  flat-descendant shape, private-child alias scrub and publicReceipt===scrubReceipt
  remain. F14 receiptExplorer/receiptChildExplorer still own link eligibility.
  Children use original root provenance and their own settled/reference fields.
- Root session is still derived only by unchanged hasSessionMarker from a
  canonical own private ID. Forged public fields, inherited/accessor IDs and
  compact-child session claims confer no authority. Canary stays independent.
  No general arbitrary-object receipt decoder or Store-guard weakening was added.

All three HTTP fixtures now persist valid ordinary rows. Malformed PRIVATE_SESSION,
token/future-field sentinels are introduced only into fresh copied allReceipts
read projections. They are adversarial projection inputs, not fabricated stored
session evidence. Mutation guards additionally cover open/begin/finish/mark/close
session methods; existing reserve and ordinary mutation guards remain. No Store
internals or persisted receipt object is mutated to bypass its guard.

## Actual chronology

1. Initial two-suite baseline at11:28:40 IST: **54 collected,52 pass/2 fail**.
   Both were stale integration expectations: a fixture now records EIP3009 but
   expected test rail; H's exact root key list omitted F's public session boolean.
   Their expectations/descriptions were corrected, not production rail/privacy.
2. Added new regressions before changing production. At11:30:30 IST, **10 selected
   failures /27 skipped**: nine kind accessor/inherited/hidden/reflection cases
   plus the fixed session_released reason. One getter actually ran, throwing
   getter/descriptor sentinels escaped, invalid presence disappeared or was
   published as onchain, and the release reason became generic. These were real
   collected failures on the rebased feed, not missing-module setup or seeded
   bad production code. Narrow projector/reason correction followed.
3. The first corrected combined command directly ran installed Vitest JavaScript
   under Bun1.3.14. Own feed suites passed66/66, but six unchanged F14 configuration
   mock cases failed (108 pass/6 fail total) at11:31:07. Root reproduced48/48
   isolated and114/114 combined using the canonical Node-shebang CLI. No helper,
   test, lock or config correction was justified. This is retained as a runner-
   specific mock boundary, not a new production defect. Earlier baseline/Red
   invocations also used direct Bun-hosted Vitest; final acceptance uses the
   project's canonical installed CLI and reruns all those corrected assertions.
4. Initial three Bun fixtures: **1 SQLite pass/3 beforeAll failures/5 assertions**.
   No route assertions ran in those failed suites. The public-feed child trace
   showed SessionConflict at ordinary putReceipt; the other two fixtures had the
   same source-level malformed session injection. Moving private sentinels to
   copied read projections restored setup without changing production guards.
5. First actual route run after that repair: **17 pass/2 fail/283 assertions**.
   Both failures were stale pretty-JSON expectations on F's compact job refusal
   body, including the new cross-realm case before its no-IO assertion. Tests now
   retain exact compact F and pretty H refusal bytes, not a weakened arbitrary
   response match. This was fixture-format adaptation, not an auth defect.
6. Final canonical Vitest at11:35:25 IST: **114/114 across3 files**, exit0,912ms.
   Final owned-loopback Bun repeat: **19/19 across3 files,319 assertions**, exit0,
   1.336s. Exact changed six-root strict subsequently returned0 diagnostics.

Two new inherited/accessor session-marker cases passed on first execution; they
are supplemental coverage, not additional historical Reds. The prior 25 F14 feed
cases remain, with corrected fixture wording/rail expectation and H child price
display retained. Existing helper48 plus feed37 plus public aggregation29 =114.

## Focused acceptance and resource ownership

Commands used an empty environment plus the verified installed Node PATH and
absolute installed Bun. Canonical focused runner:

```sh
bun --no-env-file x --no-install vitest run apps/hub/test/receipt-reference.test.ts apps/hub/test/receipts-feed.test.ts apps/hub/test/public-feeds.test.ts
bun --no-env-file test apps/hub/test/public-feeds.bun.test.ts apps/hub/test/seller-summary.bun.test.ts apps/hub/test/receipt-tree.bun.test.ts
```

Bun fixtures used scoped permission for owned ephemeral 127.0.0.1 listeners;
their children receive only fixed fixture/network/test-rail/secret selectors and
PATH. External fetch/preconnect is disabled. Three exact child close events were
awaited with existing TERM/KILL bounds, stream retention is capped, and post-close
loopback probes rejected. Owned SQLite was reopened through the real adapter to
check its source seam and receipt deduplication, closed finally, and its owned
temp directory removed. No process remains active. There was no
independent OS-wide process inventory or external network observation.

Tree fixture now uses actual F sessionJobToken/sessionToken functions with a
fixed offline secret. Session job capabilities in query/header fail on ordinary
tree, job and result routes; the ordinary token with session headers fails F's
selected result lane. Seven refusals preserve fixed404/private-no-store/no-cookie/
no-redirect responses and leave all Store read/write/external counters unchanged.
The valid ordinary token then returns the complete tree with exactly one read.
No session was opened/funded or claimed persisted by this test. Existing Unicode,
wrong-child/root, malformed path/method, storage-defect and recovery checks remain.
This is not a rerun of F's separate capacity-saturation/payment pipeline matrix.

Public routes also exercise read-boundary inherited/accessor/own-undefined kinds,
valid onchain/Gateway kinds, legacy absence and zero kind-getter invocations,
while retaining canary and omitting private sentinels. Seller summary preserves
unknown costs/funding and fixed storage errors without writes/external calls.

Exact strict used TypeScript readConfigFile/parseJsonConfigFileContent on the
absolute real root tsconfig and createProgram with only the six changed roots,
inherited strict options, noEmit and nonincremental/noncomposite. No output or
configuration mutation. Final six-path pins were captured after acceptance;
previously captured helper/server/Store pins still matched. A prior missing-wiring concern was my case-sensitive search
pattern error; actual makeSessionCallRoutes import/construction/dispatch existed.

## Frozen SHA256 inventory

```text
439b5631a565633fef2f6683b78d51376e9e81f23c0944d2f2db3495fc60c4ff  apps/hub/src/receipts-feed.ts
ce03e73006ffc35a99a361674f30fe7baa050163b419d055e7650b763cd35ad5  apps/hub/test/receipts-feed.test.ts
4c14c97874a6229ca45a22ceadc376d60e0c17a488300d5cbeb2a52cfe2e5611  apps/hub/test/public-feeds.test.ts
6d85af208b5b611f0c5c52267d4a2c364de3bd6bed10b1876fe3a7ee4c19d018  apps/hub/test/public-feeds.bun.test.ts
de82b2d86d8d02bf27bac1644bd978d97471065d820ee8b2021997205f6188a1  apps/hub/test/seller-summary.bun.test.ts
4b0061cdeafc6a889782895bb62501813530025d0d53b4dbb6a4c1219b0204fa  apps/hub/test/receipt-tree.bun.test.ts
c70e3380fdfd37c50358b01a8cf27682ff946a7f3ac5e963f83d81a110f5212b  unchanged apps/hub/src/receipt-reference.ts
5362e18f456cb843f2d9452cabc9385e897d84de77b1f1e4f08d63421a50b519  parent-owned apps/hub/src/server.ts
46f8e30a643cae99afe97606876b03d34887079741d0615e64372cdb24393617  parent-owned apps/hub/src/store.ts
```

Initial rebased feed was e40fa991583b9740ad51f68dd36cc41acdee2fddad2cc45af39df15aba74664c.
All six paths are frozen with no further correction pending. Tree/web provenance,
Store/session guard review, integrated gates and merge remain separately owned;
none of their outcomes is inferred from these focused checks.

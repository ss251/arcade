> Sanitized historical execution artifact. Statements reflect their recorded checkpoint and may be superseded. Historical commands are not current instructions. See the [current runbook](../../../runbook.md) for current behavior, approvals and operator commands. Personal/runtime locations and private artifact links may be redacted; public evidence and test distinctions are preserved.

# H8 actual Start/H4 route fixture report — September 6, 2026

Author-owned slice frozen. No production-source change. Parent owns final
integration review, browser QA, whole-repository gates, publication and commit.

## Scope and contract

Created only `apps/web/test/fixtures/skill-server.ts` and
`apps/web/test/skill-route.test.ts`, plus this ignored report. Fully read current
H8 readiness, the entire actual Task8, existing H6 fixture/test, current H4 public
types/decoders and bounded transport, and applicable repository instructions.
Used the fully read `ts-testing` skill: real public behavior and explicit async
ownership, existing Vitest runner, no mock of the production loader or decoder.
Coordinated exact loader DTO, route generation and component availability with
their authors before running. Missing modules/routes were not run as product Reds.

The fixture runs actual installed Vite/Start against an owned native HTTP hub,
prints `[h8-origins] {"web":...,"hub":...}`, and exposes only a bounded local
`GET /__fixture?mode=<known-mode>` control/reset. Counters are exactly
`{detail,receipts,names,other}`. Real H4 performs the describe/receipt/name reads;
the fixture does not fabricate loader results. Identity registry, network and
explorer use the actual local arc-testnet configuration, not a chain observation.

Data includes canonical private PayTest job IDs, repeated history timestamps,
private nested extras, two public mixed-rail rows and a flat child/grandchild
list with deliberately private omitted ancestry. It includes no genuine wallet,
secret, token, payment or current-chain authority. The `long` mode uses the exact
maximum uint256 six-decimal human price, a 32-character service name, a
500-character unbroken description and a bounded 3,800-character schema string.

## Executed chronology

1. **12:08:00 IST**: first actual route run, all **9 tests passed**, 1.903 s
   test time / 2.16 s runner duration. The production route, loader and components
   existed before this run. This is initial Green, not a manufactured Red.
   Original acceptance checked awaited child exit and both TCP ports refused;
   it did not check a zero process exit code. First exact two-root web typing
   also returned **0 diagnostics**.
2. Added assertions against the now-observed fixed visible error/history/count
   messages, and strengthened cleanup to require exit0/no signal. At
   **12:09:38 IST**, all **9 route tests passed**, but the suite's teardown
   assertion genuinely failed: exit code **143**, expected0. This is a
   **fixture lifecycle Red**, not a listing-page or privacy defect.
3. Read actual installed Vite8.1.5 `dist/node/chunks/node.js`: its
   `setupSIGTERMListener`/`parentSigtermCallback` (around3061) installs a
   handler whose `closeServerAndExit` (around25960) closes Vite and then exits
   the whole process. That competes with the fixture's own second-listener
   cleanup. Parent approved the narrow isolated-child correction.
4. Capture the existing SIGTERM listener identities before `createServer`;
   afterward remove only the newly introduced installed Vite callback named
   `parentSigtermCallback`, plus the same callback from stdin-end. Existing
   listeners and the fixture's early signal handlers remain untouched.
   `web.close()` still performs Vite's normal internal teardown. This does not
   alter installed dependencies or production code.
5. **12:11:22 IST**: same complete focused file **9/9 passed**, 1.673 s test
   time / 1.95 s runner duration. Final teardown directly observed awaited child
   **exit0**, no terminating signal, and independent native TCP connects to
   **both exact former ports returned ECONNREFUSED**. Final exact two-root web
   strict check returned **0 diagnostics**. No processes or tests remain active.

The runner did not report an assertion total; none is invented here.

## Behavioral coverage

Nine named cases make21 actual route observations, resetting/asserting exact
request counters for each. Direct valid names cause one detail GET and one
`receipts?limit=20` GET; successful ENS adds exactly one name GET. Typed expiry
or name outage performs only that name GET; invalid inputs perform zero hub IO.
All observations require `other:0`: no tree, quote, result, paid call, retry or
unexpected route. Real redirects are refused before outside transport.

- Actual SSR HTML **and its actual inline Start hydration serialization** omit
  canonical private job IDs and all supplied private extras. The test requires
  `payTestHistory` and the grandchild in serialization, so the privacy assertion
  is not against an absent hydration payload. Schema/prose cannot become the
  supplied executable tags. No invented tree graphic/edges appear.
- Eligible EIP-3009/onchain reference gives exactly one explorer link. The
  Gateway UUID, pay-test hash, announced registration hash/path and seller
  address do not acquire fabricated authority links. Session and canary remain
  independent public observations in the supplied decoded record.
- Detail-only, receipts-only and combined failures retain independent good
  evidence, fixed visible messages and serialized tags. No outage is called
  definite nonexistent listing or an empty receipt result.
- Empty receipts, absent history and empty history remain distinct. Unknown,
  stale and unverified identity counts stay unavailable; explicitly observed
  zero survives, including visible zero wording.
- Invalid space, uppercase ENS and oversized skill names produce fixed
  invalid-name states with zero reads. Actual typed ENS expiry and outage differ.
  Seller mismatch suppresses listing while retaining independently available
  public rows. A wrong skill ID is rejected by **H4 before the loader**, so that
  actual route case correctly expects `listing_unavailable`, not a fabricated
  exercise of the loader's separate injected-read `name_mismatch` branch.
- Maximum exact price and bounded long content survive H4 and Start, providing
  a reusable owned native-browser fixture without claiming visual acceptance.

## Ownership and evidence limits

The fixture uses `envDir:false`; parent/test launches Bun with `--no-env-file`
and an allowlisted environment. Its fetch fence permits only the exact owned
hub origin, allowed GET paths, no credentials, no redirects or preconnect;
unknown requests increment `other` and refuse. The fake hub accepts only GET
and exact describe/name/receipt queries. Nothing invokes chain RPC or a signer.

Early signal handlers, recorded startup promise and acquired Vite handle,
parent-PID watch, a300s lifetime and2.5s cleanup fuse bound child ownership.
The test has early-exit startup detection, bounded captured output, a15s
whole-response deadline/1MiB cap, and a bounded body-cancel wait. Teardown sends
SIGTERM, waits3.5s, then SIGKILL only if needed and waits1.5s. Final acceptance
needed no forced kill. Startup-abort fault injection, parent-death timing and
hard-fuse activation were **not separately executed**; normal shutdown was.

Canonical focused command, from the H worktree with a clean tool PATH:

```sh
bun --no-env-file x --no-install vitest run apps/web/test/skill-route.test.ts
```

Executed under Node-hosted installed Vitest3.2.7 with scoped permission for only
owned loopback listeners. Exact typing used the actual absolute
`apps/web/tsconfig.json` with only the two owned files as roots, inherited strict
options, `noEmit:true`, `incremental:false`, `composite:false`; no source-option
relaxation. No full gate/build, browser, dependency change, Git, external network,
operational environment, key or live payment was performed by this author.

## Frozen owned inventory

| Path | SHA256 |
| --- | --- |
| `apps/web/test/fixtures/skill-server.ts` (164 lines) | `6722bab0821ad4028a11209cd59c64c9778d9b2a5b6e558d46ea736112308ce1` |
| `apps/web/test/skill-route.test.ts` (211 lines) | `cf92e99cf53ed1ad2fd294d26e936dcbd6484ab6874ecff115bc7ba56cedd33e` |

Relevant read-only dependency pins captured after the final run; other authors
still own their final source freeze and this report is not their acceptance:

| Path | SHA256 |
| --- | --- |
| `apps/web/src/lib/skill-page-data.ts` | `4496ffb12f2b988141a6b6c05ecfd53a374ae50b6a81f3a53663e881f8205f89` |
| `apps/web/src/components/skill-page.tsx` | `cf4645fad8bd78ea6e8874fe3c30159f1b133e149287f0d0522b6914ec41a4f0` |
| `apps/web/src/components/evidence.tsx` | `2d2fee785b6adec8b589789f0be6bbf9ba9f4f735a84338a251c3df0fc95ef88` |
| `apps/web/src/routes/skill.$name.tsx` | `1514db40caddeb0d077e1058a7361b4fbe93a4917f08fd4f7adc6d4bacae9f71` |
| `apps/web/src/lib/hub-decode.ts` | `2b2d10c48a363d7f5dc7379ebfdf7963de0e5c11b61e58791d691b575dc2f6af` |
| `apps/web/src/lib/hub.ts` | `506b21a5585be87fc2b4523fa9d2fb686bbef2fa1f60fa49bff0b48afc1abb99` |
| `apps/web/src/lib/hub-http.ts` | `25d571ddba8f4114e927ed23653e5a9e6eeeb3db7f044e7d9fdf05c71a7b6110` |
| `config/chains/arc-testnet.json` | `fdfe40ccd270dc1c6ce34fee12a5586a1786de322696fff54709624df3f86199` |

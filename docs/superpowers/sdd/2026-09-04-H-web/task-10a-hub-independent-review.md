> Sanitized historical execution artifact. Statements reflect their recorded checkpoint and may be superseded. Historical commands are not current instructions. See the [current runbook](../../../runbook.md) for current behavior, approvals and operator commands. Personal/runtime locations and private artifact links may be redacted; public evidence and test distinctions are preserved.

# H10a hub slice — independent review

2026-09-06. **CLEAN within the frozen six-path hub scope; no correction requested.** This is not acceptance of H10b/H10c, native browser CORS, deployment or live payment. Web source and its separate transitive-import finding are outside this verdict. Parent owns the integrated full gate and any dependency correction.

## Read scope and unchanged pins

Fully read the final H10a brief, complete author report, both new helpers, all three owned test files, and all 1445 lines of the server (in bounded sections). Reviewed placement relative to boot preflight/layers, F dispatch, ordinary challenge/admission, token authentication, private result/tree reads and independent job execution. Read the installed root/base strict compiler settings. The testing skill informed behavioral verification; no new test framework or speculative counterexample was introduced.

Author report: `internal/h10a-hub-author-report.md` SHA `38e68291230d6dcccb51b8e86b209112234099422d90d562f6f4f2d1861986e1`. Final brief SHA `8e544946e100216c7db11ea53e9b22b2a7ed3186b8a8b72d542a20f707e20b71`. Both read completely; pending-gate language remains accurate at this checkpoint.

The six source/test pins matched before and after independent checks:

```text
bcbafad14474edfb3cce27256882d67d3349d34709b30afd1e508df37c6b63fc  apps/hub/src/browser-cors.ts
a306b4e0dc79d102eee70af6585c768ed4eb90093817450e11ce68d84ace1405  apps/hub/src/ordinary-http.ts
2784838936d9638e590758534929fc755162e1900f63d7bf51dc27609202b0a4  apps/hub/src/server.ts
c50d857aeda83a8f28021866f4e638fcf958e5ba918d3dfd0fe6f1061caacb63  apps/hub/test/browser-cors.test.ts
3cb27bbb946f6154785d1e0a7fce15ba34497133b4b48ec13200ba0ad166ae60  apps/hub/test/ordinary-http.test.ts
84f626d674ea410a98ff067fcf2b502bfa7674912a7324e6b2516dc86a8c34be  apps/hub/test/browser-cors.bun.test.ts
```

F dependency pins also matched: `server-sessions.ts` = `af7c3467a69c94873200f08cf92f53b50f61d61aab1167841a85548139445968`; `server-session-calls.ts` = `10b5a25bc9c3ab365e2a9db8b395d67954a7880912bd464d8f1a5f14f007103c`. No source/test/public file was edited. Only this private review was created.

## Boundary review

- The CORS wrapper executes before both F route dispatchers. Exact Origin, canonical path/method, forbidden authority fields and preflight grants are refused before business callback/body/Store/rail/admission work. It is not payment authentication. No-Origin traffic calls its existing handler without CORS rewriting; F tokens and ordinary HMAC domains remain distinct.
- Configured web/public origins are captured before constructing application layers or listening. Armed public origin cannot later drift to Host/forwarded/Referer or a changed environment value. Absent policy remains closed to Origin-present traffic, including same-origin requests; it does not quietly expose other hub routes. No session/hire/lineage CORS, query-token browser fallback, wildcard, credential or private-network grant was added.
- Preflight accepts only the bounded closed requested-header names for the selected ordinary route. Actual POST accepts a primary or legacy payment header alone, or identical nonempty dual headers; present empty, oversized and conflicting fields refuse instead of becoming an unpaid probe. GET rejects payment/content-type authority. Native browser/proxy-managed headers are not confused with custom preflight grants.
- Allowed status and original body streams remain intact; existing Vary entries are retained while Origin/private-no-store headers are added. Pure tests cover byte preservation and 200/202/400/402/404/429/503. Actual-router tests cover auth/input/Store failures with CORS headers and fixed diagnostic handling. Forbidden policy responses intentionally carry no allow-origin grant.
- Ordinary 402 adds the selected rail name without changing accepted requirements. The actual Test challenge remains Test. Armed acceptance advertises its captured public hub origin; the legacy query-bearing `poll_url` remains old-client compatibility, not something new browser helpers may follow. Acceptance/result cache headers are private/no-store.
- Input is bounded before JSON parse and challenge/admission: declared/actual 131072 bytes, exact length agreement, fatal UTF-8 and five-second whole-body deadline. Empty legacy input remains empty for the original caller interpretation. One completed read removes its abort waiter; a run exceeding 32 empty chunks refuses. The limit addresses finite work, not a claimed OOM/heap measurement.
- Ordinary retrieval passes its local signal into the actual Effect runtime for Store reads and sleep, checks before/after each await and closes timers/listeners. The actual client-abort test observes a pending Store Effect become inactive with its finalizer executed. Result lifetime is 120 seconds server-side and tree ten seconds; the independent web reader's shorter result limit may cancel retrieval. None of these signals is attached to the separately admitted `runJob`, so this does not claim to cancel/refund settlement.
- Legacy ordinary result-release/receipt semantics are retained, not re-certified as fresh buyer-side terminal binding. F's selected terminal pair and stronger session-result guards remain unchanged. No capability realm, ledger schema or session mutation API was expanded by this hub slice.

## Independent executed checks

All shell calls used HROOT and `login:false`, a clean explicit environment and the installed toolchain. No full suite, build, Git, external RPC/API, key, funding or live payment ran.

```text
env -i PATH=<owner-home>/.nvm/versions/node/v24.15.0/bin:/usr/bin:/bin <owner-home>/.bun/bin/bun --no-env-file x --no-install vitest run apps/hub/test/browser-cors.test.ts apps/hub/test/ordinary-http.test.ts
```

At **13:20:44 IST**: **71/71 Vitest, two files** (CORS56 + ordinary HTTP15), exit0. This independently exercises the final finite-empty-stream refusal and removable-listener cases; it does not rerun the pre-correction code or claim those Reds as reviewer-authored.

```text
env -i PATH=<owner-home>/.nvm/versions/node/v24.15.0/bin:/usr/bin:/bin <owner-home>/.bun/bin/bun --no-env-file test apps/hub/test/browser-cors.bun.test.ts
```

With approved owned-loopback permission: **11/11 Bun, 174 expect calls, one file, 4.70s**, exit0. Actual server startup/HTTP handling, no-business-IO preflights/refusals, Test challenge, bounded input, acceptance/public URL, private result/tree responses, single/dual payment headers, actual Store Effect interruption and startup configuration refusal passed. The fixture's synthetic Test signature and Broker completion are controlled test seams, not a real wallet or actual runner proof.

The exact six-root strict program loaded the real root `tsconfig.json` with its explicit config-file path, inherited strict options, `noEmit:true`, `incremental:false`, `composite:false`, and collected config/parse/all pre-emit diagnostics. **Zero diagnostics**, no filtering, shims or generated source. Roots were exactly the six inventory paths above.

## TDD and execution limits

The author's seven original production-router failures and later one finite-empty-stream Red are clearly distinguished from supplemental cases and setup in the report; their reported chronology is consistent with the final assertions/source. This review did not recreate their historical source or independently claim original failure counts. Author171 Vitest/40 Bun704 and reviewer71 Vitest/11 Bun174 are separate matrices, not additive proof totals.

Owned router children use explicit no-env-file/minimal synthetic configuration, memory Store and denied external fetch/preconnect. Output is capped; startup/deadline/fuse and TERM/KILL/reap/drain cleanup are present. The executed tests verified child termination, sentinel nonreflection and refused requests to their closed listeners. No reviewer process remains active. These are actual HTTP/router checks, **not** native browser enforcement, deployed reverse-proxy behavior, Gateway funding/settlement, mining or active UI approval. Native two-origin verification and full integrated acceptance remain parent-owned later gates.

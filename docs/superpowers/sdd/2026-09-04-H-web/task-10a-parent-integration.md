> Sanitized historical execution artifact. Statements reflect their recorded checkpoint and may be superseded. Historical commands are not current instructions. See the [current runbook](../../../runbook.md) for current behavior, approvals and operator commands. Personal/runtime locations and private artifact links may be redacted; public evidence and test distinctions are preserved.

# H10a parent integration — passive ordinary browser protocol

September 6, 2026, after H9 `7cc4409` on the isolated H branch. Main remains
`5259f9a`; owner Pages exclusion commit `8b8e8e5` was fast-forwarded first and is
retained. No push or deployed configuration was changed.

## Reviewed scope and supplemental composition

Parent fully read the accepted brief, both new hub helpers, the scoped production
router diff and all three new hub tests; both new web helpers, the H9 capture
rename, quote/API diffs, three author tests and existing H4 dependency contracts.
Both complete author reports and independent hub review were read. Initial web
review identified the separately recorded transitive import correction below.
The testing skill guided bounded behavioral checks, not a new framework or gate.

Hub author 171 focused Vitest/4 and 40 actual-router Bun/4/704 assertions passed;
independent hub review 71 Vitest/2 and 11 Bun/174 assertions passed. Web author
332 focused/7 and initial independent 337/8 passed. Each exact strict program
passed after the explicitly recorded generated-route support setup correction.
These are separate matrices, not additive totals or full gate results.

Parent added `apps/web/test/browser-context-rails.test.ts`: five actual challenge
producer checks for direct EIP3009, V1/V2 splitter routing, Gateway and Test.
Requirements pass through real JSON serialization before browser capture and
must preserve all original fields. Fetch/read/broadcast seams refuse any IO;
no key, signer, verify, settle or native HTTP request runs. At 13:15:53 IST,
five tests passed; exact nested single-root strict later returned zero diagnostics.
This supplemental compatibility check was Green, not a manufactured Red.

## Corrections and limits

The hub draft retained reactions from each Promise.race on a shared pending stop
promise. Parent flagged that and an empty-chunk work gap. Author reproduced a
finite 64-empty-chunk failure, then introduced removable per-read waiters and a
32-consecutive-empty-chunk bound. Final independent hub review is CLEAN. This is
bounded stream behavior, not measured heap growth or an OOM demonstration.

The initial independent web review reproduced eager server environment selection
through H4's core barrel and money's direct chain import. Parent accepted the
minimal shared integration: explicit decoder imports and a fixed six-decimal
USDC math scale, already required by every ChainConfig schema. Collected import
regressions must precede that source patch; original eight web and six hub author
files/reports remain frozen. The later correction/recheck will be appended here.

Runbook additions document opt-in exact `ARCADE_WEB_ORIGIN`, separately captured
`ARCADE_PUBLIC_URL`, absent/invalid refusal, restricted ordinary route/header
support, private/cache-free retrieval and the non-revoking meaning of abort.
No native two-origin browser CORS or reverse-proxy delivery is claimed by the
mocked-fetch/native-HTTP suites. The current active wallet/relay remains unchanged.

H10b fresh Confirm-bound approval, rail-correct signing/direct one-shot submission,
browser acceptance persistence and terminal correlation remain next. H10c buyer
UI and F session capability/status recovery are explicit separate gaps. An ordinary
H9 row cannot manufacture a session handle. No live payment, key, prior one-shot
replay, production ENS change, mainnet flip or push occurred. Full H10a gate pending.

## Import correction and machine-load checkpoint

Reviewer collected three genuine fresh-import failures at 13:25:23 IST before
the two-source patch. Regression SHA256:
`4c7f59511a38da67b4c8811410afeae7218ed32baeb7d7227559c0b3d1d85b22`.
Parent then changed only the decoder import declarations and money's scale
declaration. Corrected decoder `6e9b150639b7c7c098bd9fcdd7899beeb1f0598ee1dda1c041789b8ad9495419`;
money `6a337b2ed8608bfc3cfc53ddbb327dd9a261c54cc0b351ab8c98494ebf8f7593`.

Before interruption, the reviewer relayed final independent CLEAN: 354 Vitest/11
at 13:27:51 IST (prior337 + import3 + money8 + chain-config6), exact15 strict0,
and the unchanged narrowed fileless import probe now succeeded for all three
modules. In-memory source reversal restored prior decoder/money pins exactly;
all author/parent/route-support pins stayed unchanged. This paragraph records
that received report, not a parent-run repetition. The initial written reviewer
artifact remains immutable and still says correction pending; its final append
was interrupted before publication.

Owner/conductor machine-load alert immediately stopped delegated work; all agents
were interrupted or idle, with their commands already exited. Single-threaded
work and no parallel reviews remain mandatory until lifted. Root will use the
installed CLI's explicit Vitest four-worker/four-concurrent-test limits; Bun gets
max-concurrency4 without parallel-file workers. No full gate has started yet.

Root's restricted process inventory initially returned EPERM; an approved
read-only inventory then reported no active gate candidates. It emitted only
process IDs/classifications, never raw arguments. The sole full gate started at
13:31:37 IST, no delegated work active. Command stages use clean environment and
no automatic dotenv: installed Node-hosted Vitest with `--maxWorkers=4
--minWorkers=1 --maxConcurrency=4`, then `bun test --max-concurrency=4 .bun.test`
without parallel workers, then root/web typecheck, then the existing actual Vite
client/SSR builder with `envDir:false`. Vitest passed 3,608/152 in 59.15s. The
sequential Bun/type/build stages were still running at this written checkpoint.

The sole gate then completed exit0: **3,608 Vitest/152 files (59.15s), 834 Bun/54
files/6,093 assertions (168.31s), root/web strict TypeScript, actual client423ms
and SSR231ms builds**. No sweep was repeated; no Solidity changed and no Forge
gate is claimed here. Root retained the gate's complete output chunks rather
than rerunning for counts. All tests/build processes from the gate exited.

Pre-publication audit had already verified eight exact historical copies, 14
literal home substitutions and all18 source/test pins. This finalized parent
record is the ninth copy; final copy/link/privacy/pin audit and commit follow.

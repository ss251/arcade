> Sanitized historical execution artifact. Statements reflect their recorded checkpoint and may be superseded. Historical commands are not current instructions. See the [current runbook](../../../runbook.md) for current behavior, approvals and operator commands. Personal/runtime locations and private artifact links may be redacted; public evidence and test distinctions are preserved.

# H7 independent final source-delta review

2026-09-06. **CLEAN in this bounded source, SSR, geometry-regression and typing
scope after the contrast correction.** This is not browser acceptance, a full
gate, a canonical G → H merge or approval of unresolved upstream integration.

The initial review remains byte-unchanged at
`cb894f5d490010de7b74db5b73176fad781e47c2e13d8b44473d40b569c27dc4`
(`task7-independent-review.md`). Its initial 61-test checkpoint and genuine
fallback-link Red are historical evidence, not rewritten as final results.

## Scope and exact pins

Read the complete final component and component tests, all graph CSS and its
actual global colour tokens; compared with the initial review and previously
read real H3 producer/H4 decoder contract. The frozen layout pair and visual
fixture were not edited. Only this new private report was written. No Git,
source/public changes, full gate, build, browser, live network, key, spending or
child-service operation was performed by this reviewer.

| File | Final SHA-256 |
| --- | --- |
| apps/web/src/components/tree-graph.tsx | f71e9702a2efdc1ca93a1e319ecde86717ae91e3aceb8c0f4b853d2ab51b39cc |
| apps/web/src/styles.css | 503918fda8fa025914270aac9defcc1e8e57d838018c22eda92dd8d07770b1ad |
| apps/web/test/tree-graph.test.tsx | 3505068621267764678f7293ff2e843e7eacc3305a9fcea0ab628f90ae4a844c |
| apps/web/src/lib/tree-layout.ts | 4edac462f5b75b30213a14922972a2500c188687af6c63245734be6f9c0c71e5 |
| apps/web/test/tree-layout.test.ts | a737ea079cf44d5f83b5f3322df3c87488149861a98288cae5f54653d921f18a |

The pre-contrast delta checkpoint had the same component/layout pair, CSS
`76944d622aac061890ebfa29542a041623baf4d35277ecfeebd132a06030de1c`
and test `566594bd2b9212e9c26503f481ccc65a44f734f4911ae10e781d7e556d4b7c40`.
All five final pins were checked before and after final focused verification.

## Accepted corrections

- Full-text rows now retain the same qualified native transaction anchors as
  the SVG nodes, with `_blank`/`noreferrer`, safe positional node labels and
  explicit positional parent labels. Repeated skill names do not erase the
  relationship. No private job IDs become labels or links.
- Both accessible description and details qualify negative settlement as
  “not recorded settled”; unresolved evidence does not imply a refund or an
  unused authorization. Existing positive text remains recorded evidence.
- SVG focus suppression is scoped to the viewport; details links get their own
  ink outline. Static rules do not prove actual keyboard or accessibility-tree
  behaviour, which remains root's browser check.
- Fixture ceiling is `$0.20`, matching actual `formatPrice`; this is a display
  fixture correction, not a money calculation or product defect.
- The final local contrast correction changes only small state-text fill to
  existing ink and removes state-specific text overrides. Semantic border
  colours, component behaviour and geometry remain unchanged.

Fileless in-memory reversal of only component wording/position/link edits
exactly reconstructed initial component `c5b2f92a…`. Reversing the final contrast
CSS change reconstructed `76944d62…`; reversing only its focus edits then
reconstructed initial CSS `661f23ca…`. Removing only the new contrast test
reconstructed prior test `566594bd…`. One earlier focus-only reversal assertion
ran during root's explicitly announced CSS thaw and failed because the contrast
delta was now also present; it is a checker snapshot/setup issue, not a product
Red. Final pin-checked reconstruction passes.

## Contrast finding and resolution

The source-derived sRGB relative-luminance calculation found two 10px state
text pairs below normal-text AA 4.5:1 on pre-contrast CSS:

| State/theme | Foreground / card | Ratio | Normal AA |
| --- | --- | --- | --- |
| settled/light | #1a8a4a / #ffffff | 4.394964 | Fail |
| negative/light | #97231a / #ffffff | 8.167422 | Pass |
| settled/dark | #2ca96c / #1e1c19 | 5.665491 | Pass |
| negative/dark | #c24840 / #1e1c19 | 3.476643 | Fail |

Root received the concrete finding before thaw, added a collected regression,
and reports 24 component cases / 1 genuine failure before its CSS correction.
That Red is parent-attributed; this reviewer independently calculated the
failing colour ratios and reran the corrected suite. Final ink/card ratios are
**16.439266 light / 13.629524 dark**, passing normal AA and AAA. These are exact
declared-source colour pairs, not browser-computed colours or screenshot tests.

The contrast-check skill guided thresholds and pair analysis. Its wrapper
would install missing `node_modules`, so it was not executed: no download was
authorized. A fileless local sRGB formula performed the calculation instead.
The TypeScript-testing skill guided use of existing Vitest, real producer /
decoder composition, and the actual nested project options without new tooling.

## Independently executed checks

- Pre-contrast **63 Vitest / 2 files PASS**, exit0 at 10:40:09 IST; exact four
  nested web roots strict:true / diagnostics0. Pins matched around that run.
- Final **64 Vitest / 2 files PASS**, exit0 at 10:42:49 IST: component24 plus
  unchanged geometry40, duration474ms. Command from HROOT: empty environment
  except explicit installed Node PATH, absolute Bun `--no-env-file`, installed
  `node_modules/vitest/vitest.mjs run`, and only tree-graph.test.tsx and
  tree-layout.test.ts. No new dependency, HTTP fixture or full suite.
- Final exact four-root TypeScript program **strict:true / diagnostics0**,
  exit0, using apps/web's absolute tsconfig and inherited options with noEmit,
  nonincremental/noncomposite. Roots were the two source and two test files.
- Unchanged real H3 → H4 → SSR fallback probe now **PASS**: five legitimate
  Receipt nodes, real flat-descendant treeHashOf commitment, complete decoded
  tree, five SVG anchors, five details anchors, five distinct detail rows and
  zero private IDs. All eight executed assertions pass, including the original
  expected-five-links assertion that failed on the initial component. No mock
  decoder, receipt network request, signing or chain proof was involved.

## Remaining acceptance boundaries

No additional material source finding remains in the released H7 delta.
Actual browser rendering, mobile overflow, keyboard focus/accessibility tree,
text readability and owned-browser cleanup remain root/B9 work; the earlier
isolated renderer timeout is not a pass. This review does not claim those
observations or broaden the selected tests into a full gate.

The accepted F/H rebase prerequisite remains separate and unresolved here:
H3 snapshot must preserve settleRefKind presence, H4 must distinguish absence
from present-invalid kind, and H1 must retain F's public session boolean without
IDs. H7 consumes qualified explorer data; it cannot recover provenance erased
upstream. See `h-rebase-preflight-current.md`. No route, G8 activation, refund
proof, indexed attribution or new payment authority was introduced or waived.

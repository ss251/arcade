> Sanitized historical execution artifact. Statements reflect their recorded checkpoint and may be superseded. Historical commands are not current instructions. See the [current runbook](../../../runbook.md) for current behavior, approvals and operator commands. Personal/runtime locations and private artifact links may be redacted; public evidence and test distinctions are preserved.

# H12b3 wizard route readiness

2026-09-06 while the single H12b2 gate runs. Read-only source review; no next
source, compiler, gate, browser or process launcher started.

Read the remaining literal H12, current seller/buyer/quote routes, Nav and
hub-http body/response readers. The literal wizard overclaims "no tools means no
network/filesystem" and "never transmitted"; render actual model grants separately
from adapter transport, and qualify private/public columns at the hub boundary.
Full private metadata may include literal seller text/paths; keep it local-only,
escaped, never in URL/log/chat/localStorage.

Prefer a fixed POST /api/publish-preview handler rather than unbounded Start
RPC serialization for the full private JSON. It can inspect the actual Request,
reject authority before reading a body, bound incoming target JSON to4KiB and
time/cancellation, call the already-bound H12b2 runtime exactly once, and return
closed JSON with no-store/nosniff. Default/hosted GET page is passive; its small
server-only enabled flag must never start a child or touch source files.

Do not reuse requestJson unchanged: it uses131072bytes/10s and does not bind
the incoming signal or prevent late body completion after rejection from resolving
downstream. No broad change to the existing quote helper for this page. Add a
scoped bounded reader/client transport or a small parameterized independent helper.
Client response1MiB plus exact wrapper overhead, overall42s, bounded read chunks,
no redirects/retries, same-origin credentials omitted and abort propagated.

Keep Node filesystem/process module behind the actual Start server handler.
The page should import only closed DTO/types, pure projection and UI state.
Actual build must demonstrate child/filesystem/preload strings absent from the
client graph, not infer security from a .server filename. API fixed errors:
disabled/request invalid/preview failed. Never send raw cause/stack/stdout on error.

UI: native labeled input, explicit Preview and Cancel, no automatic load. Typing,
cancel, unmount and newer operations clear private preview and abort old owner;
late resolution cannot repaint. Show all bounded generated entries/skip reasons,
not silently first. Directory snapshots read only selected manifest, do not
validate executables/sibling worktree; generated entries are unwritten and price
default$0.05. Manual commands shell-quote every target; generate explicitly before
serve, never launch/write from the page. Existing selection may differ from a
hypothetical generated skills directory; qualify runner-wide registration action.

Append publish-only CSS at EOF with contained keyboard-scrollable JSON at390px
and desktop/light-dark. No payment/signing/provider interaction or new typography.
Actual Start route/native proof should cover passive default, active local SSR
then hydration, real directory/OpenAPI CLI through POST, hosted/authority refusals,
source mutation absence, output escaping/containment and stale cancellation with
owned fixtures. Use fresh directories/one web process/max2Chrome renderers and
exact PID/port cleanup; no live MCP or owner configuration needed.

H12b2 source remains frozen; this readiness is not implemented acceptance.

# H11 — public seller dashboard

After ordinary buyer completion `65e38c0`, root-only under the owner machine-load
restriction. Read the complete literal Task11 and current-source readiness note;
use the actual H2/H4 contract instead of the stale sample's unconditional totals,
raw errors, unqualified transaction links and chain-switching wallet convenience.

Own new `lib/seller-page-data.ts`, `lib/seller-address.ts`,
`components/seller.tsx`, `routes/seller.tsx`, their focused tests and owned seller
browser/route fixtures, append-only seller CSS, generated route registration and
SDD/runbook records. Do not alter H2 accounting, H4 decoder/transport, ordinary
recovery, shared signing, F sessions or deployed configuration.

One public address-selected summary read; no authentication, signature, payment,
ENS RPC, retry loop or new cost aggregation. Validate bounded own input without
coercion/accessors; project known scalar fields and revalidate via H4 before Start
serialization. Missing, invalid and unavailable are distinct fixed states.
Preserve null versus zero, complete versus partial known costs, failed-job
inference overhead, historical totals without current listings and the settled
call denominator for per-listing margin. Do not equate test/canary-inclusive hub
records with customers or independently verified cash flow.

Wallet address selection is optional and explicit, bounded and abortable. It
requests accounts only, never chain changes/signing. Late wallet responses must
not replace newer typed/query selections or an unmounted owner. The public address
is intentionally bookmarkable; no secret capability goes into a URL.

Use native forms/links and existing ARCADE tokens. Show full escaped public
addresses/references, unknown ENS liveness and qualified hub identity claims;
never guess rail/network for a pay-test hash or call a summary fresh chain proof.
Append seller-scoped CSS without changing any existing stylesheet byte prefix.

Prewrite real H2→H4/data/presentation/lifecycle tests; exercise the actual Start
route and one serial owned synthetic browser at desktop/mobile-width light/dark.
No real wallet/key/payment/model/production request. Bound workers to four and
browser renderers to two, independently verify cleanup, run one full sequential
gate after freeze, preserve genuine failures and targeted recovery, then commit
locally without push. H12–14 and later vendor-neutral/PlanI work follow.

> Sanitized historical execution artifact. Statements reflect their recorded checkpoint and may be superseded. Historical commands are not current instructions. See the [current runbook](../../../runbook.md) for current behavior, approvals and operator commands. Personal/runtime locations and private artifact links may be redacted; public evidence and test distinctions are preserved.

# H8 — parent native-browser acceptance

September 6, 2026, approximately 12:12–12:14 IST. This is an actual local
Start/H4/browser run with synthetic public data, not a deployed-service or live
payment proof. Production source was not changed during or after the run.

## Result

All 16 exact-document cases passed: 1280×900 and 390×844, light and dark,
with normal detail, maximum uint256 price/long prose and schema, absent identity
evidence, and typed ENS expiry. Every case checked its exact URL, heading,
viewport/scheme, document/body containment, complete DOM private-field exclusion
and exact hub request counters. Each ordinary case made one detail and one
receipt read; expiry made only one name read; all had zero unexpected requests.
Browser initialization's separate normal navigation preceded those reset counters.

Four native keyboard sequences tabbed through navigation and the chat link to
the input summary, opened it with Enter, then focused its preformatted JSON.
Both summary and JSON focus were visibly indicated. Four native horizontal-wheel
sequences moved the long schema inside its own scroll region without widening
the page. The long schema's scroll width was31519px, client width426px desktop
and340px mobile; resulting scrollLeft was700,350,350,350px. These are observed
native movements, not JavaScript scroll setters or fabricated touch tests.

The first desktop/light wheel call timed out with the owned tab hidden. The
already-read browser-harness skill required activating that exact owned tab and
retrying once; it then moved. Its700px result may include queued input and is
not attributed to exactly one350px event. The later three visible cases needed
no activation or retry. This was a harness visibility mechanic, not a product
failure. No shared browser, user profile, consent dialog or outside link was used.

Sixteen screenshots are retained. Parent visually inspected six representative
images: desktop/light normal, mobile/dark normal, desktop/dark long with focused
scrolled JSON, mobile/light long, mobile/dark expired, and desktop/light missing
identity. Long-case screenshots reflect native focus scrolling down the page;
they are not claimed as top-of-page captures. Layout stays within the viewport,
machine references wrap, prices retain exact text and schema overflow is local.
DOM assertions cover the lower evidence text; no full-page image or complete
accessibility certification is claimed.

Actual computed text/paper pairs, standard sRGB luminance calculation:

| Pair | Light contrast | Dark contrast |
| --- | ---: | ---: |
| Quantity blue / paper | 6.6401 | 5.7295 |
| Note slate / paper | 5.1180 | 6.1950 |
| State ink / paper | 15.6140 | 14.6310 |

All six exceed4.5:1 for normal text. Decorative borders, every possible element,
touch gestures and keyboard-only horizontal movement were not separately tested.

## Owned execution and cleanup

Private directory: `<private-h8-browser>`.
The retained supervisor reuses the previously verified H7 ownership pattern:
actual Bun `--no-env-file` fixture, isolated installed Chrome for Testing and
the installed browser-harness daemon as direct children. It preserves real HOME
while isolating profile/cache/harness paths, disables recordings/telemetry/domain
skills, fences fixture fetches to its exact hub, denies non-loopback DNS and blocks
page HTTPS links. This is not a packet-capture guarantee about Chrome internals.
The220s supervisor fuse and160s command bound were not reached; all streams were
drained with64KiB retention. No dependency or harness update was performed.

Command: `bun --no-env-file <private-h8-browser>/supervise.ts`.
Supervisor66693, fixture66696, Chrome66991, harness67353 and check67395 were exact
owned handles. Check, Chrome and fixture exited0; harness was terminated by
SIGTERM; supervisor exited0. No KILL escalation was necessary. Independent later
checks found all five PIDs absent with ESRCH and all three former ports refused:
web5173, mock hub61538 and CDP61546 returned ECONNREFUSED. Nothing remains running.

## Frozen artifacts

All runtime artifacts remain private. `evidence-index.json` inventories the16
PNG hashes and six measured color pairs; screenshots are not copied into SDD.

| Artifact | SHA256 |
| --- | --- |
| supervise.ts | fedbb1a7616c9b139ddb0b6b62b046b0041239a7b800537dd23cb17608382dd2 |
| check.py | 415ff6b28d4de28213f3ae197948b6c7937fe1aaf55a8965b5a6789549d7268c |
| observations.json | e450eac4da8572a9b8b9e1f21231d11d05d9ecfae999923dccc3820da8bc0ddb |
| cleanup.json | ed9157c53f0c09a9f72123f2e90844d3bb705401b47c365217e60563d3148ef2 |
| post-cleanup.json | 076ad4f8b0e7e0e01357d9c85db902f458f9101497dc8fcd5321ecbeb322fbd4 |

Observed source: fixture6722bab0, loader4496ffb1, Evidence2d2fee78,
pagecf4645fa, stylesb986bc5d and root route1514db40, matching the authors' frozen
files. Main and the H branch's committed HEAD were not mutated. This browser
acceptance does not replace independent source review or the single full gate.

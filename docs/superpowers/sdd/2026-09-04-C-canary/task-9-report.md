> Sanitized historical execution artifact. Statements reflect their recorded checkpoint and may be superseded. Historical commands are not current instructions. See the [current runbook](../../../runbook.md) for current behavior, approvals and operator commands. Personal/runtime locations and private artifact links may be redacted; public evidence and test distinctions are preserved.

# C9 — Scheduled hub lifetime and operator configuration

Canary fiber starts after Bun binds, using the actual server port (including PORT=0) or explicitly configured public origin. Effect.forkScoped lives within existing Effect.scoped; server cleanup finalizer added. Public preflight warns without a key, does not invent recent evidence, and retains existing dated history. Invalid config errors identify only the field, never the supplied key/value. Runbook documents four variables, immediate first tick, persistence/reconnect behavior and per-purchase versus total-budget distinction.

TDD: four genuine real-process preflight failures04:14:57 (missing warning/scheduler, invalid interval/tick accepted). Actual scheduled HTTP Red04:17:53 timed out waiting for the first three automatic failures, confirming that no loop was wired. After implementation, preflight14 and manualC6HTTP7 passed; automatic cycle completed but its new test incorrectly expected buyer identity in the intentionally redacted public feed. Corrected to assert buyer absence and matching settlement tx. Adding nonempty real jobId evidence captured a true second integration defect04:20:44: SDK types say jobId, poll wire envelope says job_id. The private authenticated receipt contains canonical jobId, so canary verdict now falls back to that exact field without changing the SDK/public feed. Final04:21:20:79 focused tests, tsc/diffcheck green.

New automatic test uses a real loopback hub, isolated durable sqlite, signed Hello and real scheduled SDK HTTP calls with test-only cryptographic signer recovery over an in-memory rail. It proves three output-validation failures delist, disconnected details404, reconnect alone stillhidden/explanatory, held job cannot prematurely recover, one passing result relists with canarymarkedtx/job, discovery returns and public buyer privacy persists. All temporary child/socket/database resources are owned and cleaned; no real network/funds/keychain used. C6's existing preload disables only the new scheduler unless TEST_AUTO_CANARY=1, preserving its deterministic manual security tests.

Independent review/full gates/commit recorded on completion. C10 live key remains OWNER pending; exact provisioning/faucet/run instructions appended to main ignored [private owner-action ledger omitted].

Completionc55e1f3. Independent read-only review plus79 focused tests at04:23:28, tsc/diffcheck passed. Full04:24:29 precommit gate1,247 Vitest+37 Bun, tsc/diffcheck;6 intended files only, exact subject/trailer. No live key used.

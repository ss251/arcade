> Sanitized historical execution artifact. Statements reflect their recorded checkpoint and may be superseded. Historical commands are not current instructions. See the [current runbook](../../../runbook.md) for current behavior, approvals and operator commands. Personal/runtime locations and private artifact links may be redacted; public evidence and test distinctions are preserved.

# C6 — Delisting at discovery and the actual paid route

Pure import-safe claim/refusal helpers (re-exported from server), all three machine discovery builders, JSON catalogue and HTML/feed omit delisted records. Detail remains reachable with exact public history and last verdict. Request order remains input validation, early delist refusal, lineage, challenge/verification, verified-payer recheck, then reservation/job. Only configured verified canary calls receive the C5 marker.

TDD: helper missing-module and five discovery failures at03:50:59 ->66 focused green03:53:42. HTTP fixture setup initially used an expired-form Hello nonce; corrected to timestamp+random suffix. Genuine endpoint Red03:54:35 showed missing detail fields, stranger execution, missing verified-payer refusal, and unmarked canary receipt. Forged-header fixture encoding was also corrected to raw base64 JSON. Final73 focused plus tsc green03:56:11. Independent read-only review and73-case rerun04:02:00 clean. Full precommit04:02:36 gates passed1,149 Vitest+37 Bun, tsc/diffcheck.

Seven actual HTTP cases use real server, durable isolated sqlite and signed runner/buyer messages, with test rail plus real offline EIP712 signer recovery. No funds, real keys or chain calls. They prove every catalogue omission, reachable detail, input gate ordering, unchanged buyer handshake, modern-header precedence, forged signature rejection, postverify refusal, zero job/reservation/receipt side effects before rejection, and successful verified-canary marked receipt.

Compatibility: headerless challenge-only probes are necessary for the unchanged buyer to sign. They create no job or reservation; every signed request faces both early claim and authoritative verified-payer gates. Invalid canary key yields fixed boot diagnostic, never its value. Test cleanup only stops owned child/socket and removes owned temporary directory. No pushed changes.

C9 follow-up: isolate the automatic background scheduler in this HTTP test's preload; long tick/interval alone cannot stop the initial tick. This keeps its three seeded failures and single manual dispatch deterministic.

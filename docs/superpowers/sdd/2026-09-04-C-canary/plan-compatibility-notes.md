> Sanitized historical execution artifact. Statements reflect their recorded checkpoint and may be superseded. Historical commands are not current instructions. See the [current runbook](../../../runbook.md) for current behavior, approvals and operator commands. Personal/runtime locations and private artifact links may be redacted; public evidence and test distinctions are preserved.

# Plan C compatibility review

Read the complete 2,221-line plan before implementation. Root and independent reviewer identified these literal-code conflicts; preserve interfaces and record TDD fixes per task.

- Task1/2: currently eight first-party listings, not four. Add real canaryInput to every shipped manifest, but preserve canonical loop-probe $0.30 and descendant budgets. Task2's blanket $0.25-cap assertion is stale: validate all declared inputs, then explicitly assert loop-probe is skipped above cap and others are testable. B generator fixtures remain exact after removing only the new public canaryInput enrichment; do not weaken arbitrary-field equality.
- Task2: bound hostile schema traversal, allocation and recursion; preserve own-property keys; validate declared/derived input with existing hub validator. Do not guess patterned/formatted values. Unsupported schema shapes must not crash the hub. Literal maxLength0 handling and fractional integer minima need tests.
- Task3/4: persisted chronology and in-memory chronology must match, including equal timestamps; normalize seller case for storage/pruning. Derived decoration must strip caller-supplied payTested/delisted, not retain a forged payTested when history is empty. SQLite failure must not publish a verdict that was not durable.
- Task6: current server starts on import, so pure delisting/claim helpers need a separate import-safe module, with exact exports preserved where required. Literal prose places check after verify while code comment/index mandate position2 before402; index wins.
- Task6/8: ordinary callSkill starts headerless and receives403 from the literal precheck before it can get402/sign. Permit header-absent challenge-only probes when canary configured, still no job/settlement. Refuse noncanary signed requests early and always recheck against verified.payer before any reservation/job. Use modern payment-signature before legacy x-payment consistently; literal claimedPayerOf used opposite precedence and would allow forgedlegacycanary+verifiedmodernstranger without a postverifycheck. Validate header/from shape. Tests must exercise the real buyer handshake and show zero effects on refusal.
- Task8: malformed declared canaryInput is a seller error (plan self-review says record failure), while absent underivable input/above cap are skips. Literal helper returns null for both: resolve deliberately. Catch synchronous buy throws too, protect per-target persistence/logging without leaking causes/secrets, preserve interruption so scopedfiber dies. Parse positive finite bounded intervals; zero/overflow must not enable spending loops.
- Task10: runner disconnect removes listing; /listings/:id and /skill/:id correctly return404, not delistedJSON/HTML. Offline evidence must separately prove404+discoveryabsence and read durable pay_tests for exactlisting+seller to establish threefailures. Delisted page assertion belongs afterreconnect beforepass. Do not resurrect listings. Isolate runnerconfig/skills/db/ports, bindloopback, neverkillPID0, onlystop ownedprocesses. Deterministic reconnect/pass timing rather than sleeps racing15sinterval. Ownercanarykey remains required; never substitute existingbuyerkey.

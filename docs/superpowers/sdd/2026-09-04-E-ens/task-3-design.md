> Sanitized historical execution artifact. Statements reflect their recorded checkpoint and may be superseded. Historical commands are not current instructions. See the [current runbook](../../../runbook.md) for current behavior, approvals and operator commands. Personal/runtime locations and private artifact links may be redacted; public evidence and test distinctions are preserved.

# E3 setup execution boundary (implementation design)

Owner-gated script, import-safe and dry-run without private keys. Exact CLI retains --root-label/--seller/--seller-label/--skills/--hub/--web/--mcp/--agent-id/--ttl/--dry-run, plus explicit --confirm-root-label and public --owner/--daemon for keyless planning. Automatic fallback can propose a label only; live registration requires exact explicit confirmation matching the proposed label before any write/key use. Default MCP path is not real and must not be fabricated. Pure tests and injected chain driver cover parent flow; no funding/registration live action by executor.

Live writes remain guarded before startup until E4/E5 hooks are present, preventing Task3's literal stub-after-mint/deploy side effects. Parent registration calls currentisAvailable/getRegisterPrice andreads MIN/MAX_COMMITMENT_AGE from selectedSepoliaregistry; exactMockUSDC feeapproval isquote-derived, no100tokenblindapproval. Neverretry uncertainwrite; recordprebroadcastintent andknownhash in privatecheckpoint, redactdriverexceptions; boundedreceiptreads (noArcwait) andactualRPCchaincheckbeforeeachwrite. Commitsecretnotwalletprivatekey, 0600checkpoint neededforresumingknowncommit/reveal, otherwise failclosedmanualreconciliation. FutureE4proxiesverifiedagainstfactory/currentinitializerroles before reuse, not arbitrarygetSubregistry pointertrust. E5records must readactual402 payTo(V2splitter) notlisting.seller, parseatomicintegerprice, checkexpectedlistseller/inputmanifest, and ownerauthorizesexactgrantsto distinctdaemononly.

Main key constraints and live auto-review refusal recorded separately. No stored private keys, no ownerHOME changes while developing, allfiletests ARCADE_ENS_STATE/checkpoint temp overrides. Publicresults neverincludecommitpreimage/walletkeys/privateprovidererrors. Writefinal EnsState onlyafterexactconfirmedreadback; rerunresumesratherthanregistersfallbacknewnamespace. Parentpointerirreversibilitydisclosed butnotlocked.

> Sanitized historical execution artifact. Statements reflect their recorded checkpoint and may be superseded. Historical commands are not current instructions. See the [current runbook](../../../runbook.md) for current behavior, approvals and operator commands. Personal/runtime locations and private artifact links may be redacted; public evidence and test distinctions are preserved.

# E1 — pure shared ENS primitives

Missing-module Red at 06:26:01 IST; 20 focused tests and all237 core tests Green at06:27:56, TypeScript/diff checks passed. Exact named exports cover Sepolia ID, text keys, hierarchical names, bounded ERC7930 addresses/registry IDs, role masks and ABI-encoded salts, finite safe TTLs. No node/Bun/process dependency or write/key access. Independent review/full gate underway.

Evidence-based plan corrections: installed viem returns DNS a.b.eth bytes0x016101620365746800 (one terminator). The Arc vector in the plan has an extra zero nybble after reference-length03, giving an invalid odd-length hex string; correct is0001|0000|03|4cef52|14|address. The mainnet ENSIP25 official vector matches, and an independent offset-decoding assertion verifies Arc reference length3 and integer5042002. Pure TTL parser defaults6h; runtime callers explicitly pass environment to preserve browser safety. Names reject dotted-component hierarchy escape; generic ENS helper normalizes full names. String agent IDs stay registry-defined perENSIP25 rather than incorrectly forced numeric. All inputs bounded; numeric IDs/chain/salt/TTL reject unsafe integers. Role comments state root-admin authority can alter grants, so withholding transfer does not imply immutable trustlessness.

Sources checked2026-09-05: https://docs.ens.domains/ensip/25/ ; https://docs.ens.domains/ensip/26/ ; https://eips.ethereum.org/EIPS/eip-7930 ; official contracts-v2 RegistryRolesLib.sol and PermissionedResolverLib.sol on main. ENS deployments/docs and deployed97a57293f3b4279d94b571e678edb53ce62638f4 ABI sources checked forE2; registrar isAvailable differs from plan available.

Baseline: frozen offline install succeeded1044packages, untouched Vitest baseline06:23:01 attempted in restricted sandbox, yielding43failed/1597passed/10skipped and18listenEPERM exceptions across11HTTP suites. This is permission denial, not product behavior or the stated timing flake. Full required gate rerun with approved local-HTTP permission; no test weakened or omitted. Main predecessorall4gate had1650Vitest+92Bun/tsc/webbuild/16Forge green immediately before this worktree.

> Sanitized historical execution artifact. Statements reflect their recorded checkpoint and may be superseded. Historical commands are not current instructions. See the [current runbook](../../../runbook.md) for current behavior, approvals and operator commands. Personal/runtime locations and private artifact links may be redacted; public evidence and test distinctions are preserved.

# Task10 — sandbox hiring by ENS name

Owned files are buyer hire.ts/hire.test.ts and runner hire-broker.ts/hire-broker.test.ts. The sandbox sends exactly a normalized name or validated skill ID, never a private key or routing authority. Broker strict own-field XOR rejects empty/null second targets and ignores supplied routing/lineage overrides. Caps must be positive finite six-decimal values and cannot silently widen the remaining inherited budget.

By-name requests skip catalog lookup and call the SDK with the issuing hub's expected origin; the SDK resolves exactly once and pins the origin before any capability-bearing HTTP. Fenced output identifies the ENS name. Existing ID path, parent capability and reservation accounting remain in place.

TDD: four genuine selected Unix-socket Reds08:31:54 →51 E10 tests Green08:35:21. Actual SDK integration fixture aligned to production poll token/job_id; combined E9/E10 77 Green08:37:39 and global tsc/diff0 before E9 amount followup. Tests exercise real Unix sockets and actual deterministic offline viem signatures, same-hub3HTTP/4ENSreads, no lineage header on polling, foreign host/port0HTTP, cycles/missing/outage/payee mismatch and malformed caps/targets. Root reviewed all four diffs CLEAN. No live keys, network spending or owner state touched. Full staged gate and commit appended after completion.
Final staged gate session89041: 1,932 Vitest +158 Bun/718 assertions +TypeScript and both diff checks Green. Exact task commit50777bd with required trailer, four intended files, no private ledgers staged.

> Sanitized historical execution artifact. Statements reflect their recorded checkpoint and may be superseded. Historical commands are not current instructions. See the [current runbook](../../../runbook.md) for current behavior, approvals and operator commands. Personal/runtime locations and private artifact links may be redacted; public evidence and test distinctions are preserved.

# E13 — verified ENS provenance in the web purchase path

Commit `241f449` (base `0d9b2e0`), exact plan subject and Codex trailer. Sixteen explicit web files; no dependencies, keys, live payments, deployment or owner configuration changes.

The card presents an ENS name only after the same actual-input quote verifies current name routing, seller/skill/chain/payTo and canonical402 terms. The original quote requirements flow through signing. Settle requotes the exact input before forwarding one signature, bounds full JSON bodies/redirects, correlates terminal receipts and same-origin polls, and never resends payment. A signed but unconfirmed outcome is not represented as uncharged. The final read-only result poll uses its remaining90-second budget; quotes and paidPOST retain10-second limits.

TDD:28 genuine Reds09:00:40,20 Reds09:06:32,8 Reds09:12:50, signed-failure copy Red09:16:02; later disclaimer Red09:19:25 and final long-poll/pending-copy three Reds09:25:53. Final151web tests, root+web typechecks/build/diff Green. Staged full gate session73430:2,037Vitest across107files +158Bun/718assertions, both typechecks, productionwebbuild anddiff passed beforecommit.

Root source review and B9 independent review CLEAN. Initial P2 premature10-second real-hub long-poll timeout fixed with actual-shaped15-second completion regression, stalled-body90-second abort/cancel regression, one paidPOST/no resend assertions. Post-sign progress now says authorization signed/awaitinghuboutcome.

Visual check used actual React Confirm and production styles in an ignored, offline private fixture: desktop1280 and mobile390×844; full195-character names wrap, full payout address retained, document does not overflow, blocked/unblocked hold buttons and decline click behaved. Final disclaimer verified after rebuilding fixture. Darktheme checked; do not claim lighttheme or livewallet/hold-to-pay testing. Fixture server session88219 stopped and owned browser tab closed/reset; no published artifact.

Plan deviations are safety plumbing needed for honest provenance: actual input through quote/settle, canonical bounded402, independent name verification, full-body deadlines, same-origin poll correlation, and truthful signed-uncertainty UI. Merely copying an advertised listing name would falsely claim ENS authority. No private ledger staged.

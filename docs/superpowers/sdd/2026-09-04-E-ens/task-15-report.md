> Sanitized historical execution artifact. Statements reflect their recorded checkpoint and may be superseded. Historical commands are not current instructions. See the [current runbook](../../../runbook.md) for current behavior, approvals and operator commands. Personal/runtime locations and private artifact links may be redacted; public evidence and test distinctions are preserved.

# E15 — live ENS namespace evidence (complete)

Owner approved exact `arcade` parent, distinct funded owner/daemon roles, and isolated loopback hub/web URLs. Conditions: owner retains every record's update authority; exact future production re-point command; pending owner re-point before September13; honest temporary URLs and independent post-run chain reads. No production configuration or runner was changed.

## First finite run: completed setup, safely stopped before purchase

Private reviewed supervisor reused actual setup, runner, buyer SDK and demo code, with purpose-specific child environments, owned cleanup, no automatic resend, and one approved $0.01 purchase maximum. Original private TDD missing-module Red preceded implementation; 11 tests/69 assertions and private strict typecheck passed, independent review clean. Final review corrected real result-shape, split log, watcher-cadence and long-poll boundaries before launch. These are offline checks, not chain proof.

Root consumed approved Keychain roles inline only at05:41:15UTC September5. Fourteen Sepolia setup transactions completed05:46:57UTC; parent `arcade.eth`, seller `scf821769ed.arcade.eth`, skill `usdc-flow-check.scf821769ed.arcade.eth`. The actual temporary web route returned200 with HTML; it was the existing hub skill page, not a new production frontend.

- SellerRegistry `0x80f1ba46a19702eedc3aa4fe28f6c2071a2a0928`.
- SkillRegistry `0xdafbdd2d7109d4706999573f60a3d1c17a96bdc6`.
- PermissionedResolver `0x7070e0805ec7ef405c02e3f38fef453ecb712190`.
- Parent registration `0xf20d14c99278b133f7567bb6050c317526df2c54d86b57e024434851e0681bed`.
- Seller registration `0x021ec6bd72c2ef2a678e156a57ab2b9bee2d46fc871e637b9478d1fd67333aff`.
- Skill registration `0xd7d2470b13a97d2229ce65e5ad3e42ba515e8726a40856228a0a50244ba7b2da`.
- Records `0xda352327b520f72dca2f6f4498f21a2188329061f640081972c34f41dff96a5c`.
- Scoped renewal grant `0x6bdf44c5182aad4abcea70c9331f159b1f4916e61a900f8b359e3ea0df4b3426`.
- Scoped price grant `0x12ed2aed1e59acb7154ac5b9b6662e2943904d47b9f444b98492f8c7af1c9f0e`.

Runner confirmation did not arrive. The supervisor honestly FAILED and stopped all owned processes05:49:59UTC, without buying or changing price. Read-only root reconciliation then proved the exact daemon renewal transaction `0x412e2c70961f2582fbbbc53f2bb5e18f56cfb1386e1856233592f8df32d7d1bb`: success atblock11638428/hash`0xee6bddf5368b377cee6aea43ab9b98a078bf4e129e94df353c9f53ee42ec4155`, exact signer/recipient and calldata `renew(labelId("usdc-flow-check"),1788588119)`. The persisted runner entry remained submitted; the proof did not resend or silently relabel it. Initial setup expiry1788587784 increased to1788588119.

Root cause: production EnsWriter stopped receipt polling after four reads and only3.5s of configured delay, before ordinary Sepolia mining. Its uncertainty handling correctly paused the ticker and retained the hash. Price-lock's daemon bump uses the same writer, so root refused to launch the prepared remaining-phase resume merely to beat expiry. That mode's 14 offline tests/84 assertions and typecheck passed but it was NEVER executed. No second key retrieval or new transaction occurred.

## Independent first-run post-cleanup proof

C1's public-only verifier used no state/journal/key material. Fresh block11638471/hash`0xea5e4a84902eb4b7e968368ddfa0a36e325b9ab6a020f2ee917b5a3f92101123` on Sepolia verified all three current proxy implementations and hierarchy; owner rawALL roles and daemon root0 on each; all six original records at price10000; six same-value owner `setText` simulations; live retained seller/token/expiry at that block. Token23970333715751076124179036471974348465258195195405327158223653295910559940609; initial live resource is token−1. GET to loopback51989 refused connection after cleanup. This is **PARTIAL_STATE_VERIFIED, not full-demo PASS**.

Original public evidence and private state/journals are retained separately. Never publish commitment secrets, raw journals, SQLite, tokens, headers or keys. The temporary URL origin was `http://127.0.0.1:51989`; it served only while the owned processes ran and stopped on cleanup. Owner authority survived cleanup independently of endpoint availability.

## Recovery approval and pending work

The user explicitly answered yes to ONE owner-signed root renewal of the existing skill for a fresh15-minute window plus Sepolia gas, followed by the already-approved purchase/price/expiry checks. This is not permission to register again, widen daemon roles, change payee/chain or resend an unknown transaction. Root will first land a separately reviewed bounded receipt-window fix with full gates. Fixed revival expiry must be durably selected in a dedicated journal before signing.

The exact owner-only production text re-point command is embedded in the E15 runbook. Its Bash/JS syntax and actual imports passed independently offline; it has not been executed. It updates only endpoint/web/context and preserves price/payment authority/ownership. Production origin selection, actual re-pointing, any eventual price restoration/regrant and post-demo revival remain separate owner steps.

Plan deviations: only existing `usdc-flow-check` was registered, not future G's absent `counterparty-graph`; local endpoints explicitly owner-approved; TTL is persisted setup `--ttl`, not unimplemented ARCADE_ENS_TTL; 6h quarter-TTL cadence is16renewals/day; scoped daemon cannot revive expired registrations; synthetic 402 tests are not paid evidence; old failed run stays failed even if a later explicitly approved continuation passes.

Final live result, gates, exact E15 documentation commit and merge are pending.

## Receipt-window follow-up committed

Atomic follow-up `7820ac1` (`fix(ens): bound Sepolia receipt confirmation by the full operation`) changes only `packages/runner/src/ens.ts` and its focused test file, with Codex attribution. Four genuine delayed-mining/pending/stop Reds06:02:30UTC preceded implementation; cancellation-hook Red06:05:41 then final47 focused tests passed (33 writer/liveness,8 journal,6 daemon). Independent C1 source review CLEAN. Root's full `bun run test && bunx tsc --noEmit` gate89444 passed, including213 Bun tests/1128 assertions; staged diff and unchanged frozen source checks passed before commit.

The ticker stays60s. The writer's55s total operation covers preflight, one broadcast, known-hash receipt polling every2s, exact proof and durable readback. Stop aborts pending waits/client and blocks late sends. Permanently pending or unknown outcomes stay uncertain with retained journals; no broadcast retry or acceptance weakening was added. Both the reviewed owner recovery helper and the separate remaining-phase continuation must be ready before consuming the one approved revival, so the fresh15-minute window is used for evidence rather than development.

## Main integration and recovery readiness

Main fast-forwarded to7820ac1, preserving all E task commits without squash. The first full merge gate hit five host subprocess timeouts; the four affected suites all passed sequentially in isolation. Repeating the unchanged complete gate passed2054 Vitest tests across108 files,213 Bun tests/1128 assertions, strict TypeScript, web build and16 Forge tests. No assertions or timeout limits were weakened.

Final frozen private recovery gate passed51 tests/300 assertions, all three strict TypeScript projects and diffcheck at06:29UTC. Owner helper, remaining-phase supervisor and completed-resume post-verifier received independent cross-reviews, all CLEAN. Twelve genuine post-verifier regression Reds preceded exact resume-path/provenance support. The resume must independently verify both original daemon and new owner transaction signers/calldata/receipts, then fresh retained state with at least180 seconds of runway before purchase and price operations. It never repeats setup or starts a renewing runner. The dedicated owner helper allows only one durable15-minute expiry and one owner-root renewal; its210-second outer fuse bounds cleanup as well as execution.

Root launched the explicitly approved one-owner-renewal consuming command at06:30UTC; the command is tracked, not detached, and reads only the owner Keychain item inline. Result is pending at this checkpoint. Original failed evidence and pending daemon journal remain unchanged.

## Approved continuation: renewal, purchase and price checks confirmed

The single owner renewal confirmed successfully, tx0xb06f82ad01e791ecd27a57cde0a141842f6f2ce3a9362965436fc8c48fa5b6f2. Postread block11638640/hash0xa3d6532c3370aec400d21469e5d76671cfdbf1163ffdb0daaf3a678aab78f473 proved retained identity/roles/records and fixed expiry1788590760 (06:46UTC). Root launched the separate reviewed continuation immediately. It independently verified both renewal transactions and fresh hierarchy/state; existing setup proof is reused with originalSetupAt, not executed again. Separate resume evidence does not overwrite the first FAILED run.

By-name purchase completed06:32:45UTC: job_eed1146faa6a45c0912a, Arc settlement0xf3d8b2eef12c3d68c96f8641be5f1f94d31f29f29a6ca65da8e04baadecca4ef,10000 atomic USDC split9500 seller/500 fee. Exact ordinary receipt, authorization correlation, successful transaction and splitter/USDC events independently verified atblock60536850/hash0xc7e7cb65db59af56bdbc27b71e61fd67554c2bbc6decb7df49934c6d765c4394. No additional purchase was attempted.

Price-lock completed06:34:16UTC. Daemon update10000→11000 confirmed0x007e6bec63896613711ffab3628245cdaf320c48cb84ec0738ba631e23fa0ffb; owner revocation0x2442e78156df1d7fd58c19e124b91c16ac83beea32d966f52c7e1919431cd3fc. Exact subsequent simulated denial EACUnauthorizedAccountRoles/0x4b27a133 and role/account/resource matched, non-price records unchanged. No restoration/regrant occurred. At06:34:27 two synthetic real-SDK tamper probes refused wrong payee/chain with ens_payto_mismatch, zero signatures/paid requests; explicitly not paid evidence. Non-renewing seller remains connected for true expiry/watcher proof; baseline block11638654/expiry1788590760. Full PASS and cleanup are pending at this checkpoint.

While awaiting passive expiry, B9 independently reviewed the future owner re-point command. Its actual documented whole-command timeout did not cover uncooperative open/cleanup. C1 is reproducing this with offline source-extracted regressions before root fixes the documentation; no frozen live helper is edited. Personal shell paths were replaced with portable repository-root checks. Context wording will explicitly distinguish unchanged raw price from synchronizing the embedded context price. No production re-point has been executed.

Re-point follow-up now verified: C1's actual-source extracted stalled state/open/close tests produced3 genuine Reds; root then added a300-second soft abort shared with Request signals and a330-second hard failure fuse retained through final shared cleanup. All4 source-bound tests/19 assertions, standalone strict TypeScript and diffcheck passed. B9 and C1 independently reviewed the final delta CLEAN. The documented command fails without a success claim on stalled I/O and still uses the existing durable single-send discipline. Root/main preconditions precede key retrieval; context documentation now explicitly says its embedded price is refreshed while raw price/payee/chain remain unchanged. This is documentation-command validation, not an executed production operation.

## Final live PASS and independent post-run verification

Actual passive expiry1788590760 (06:46UTC) was confirmed at06:46:59.313UTC, block11638715, chain timestamp1788590808. No additional renewal/unregister/register occurred. The seller remained connected without the daemon renewal key; matching detail200/ensExpired and catalog omission prove the watcher caused removal, not a disconnected row. Owned cleanup completed06:47:00.333UTC, supervisor post-cleanup readback/PASS06:47:10.487UTC, exit0. Postread block11638715/hash0xe8faeedc519be10bc92075fe03840353e3bf7c3818ccea3ac309b275a315fb0f.

Root explicitly authorized C1's separate frozen public-only post-run verifier AFTER the process exited. C1 command45834 exited0, fullDemoPass:true, at fresh Sepolia block11638719/hash0x5ec46dd8e11213de49e80618d98c1b77c383c0462def18996e9515d68f6927bd/time1788590856. All three current implementations/mounts/owner rawALL/daemon root0, six exact records, six owner same-value setText simulations, expired retained token/resource/currentOwner0/expiry and actual loopback connection refusal proved again. No key/state/journal/DB reads, signatures or transactions were used. Thus every written record remains owner-updatable after expiry and cleanup. Raw price11000/context embedded10000/scoped price grant revoked remain explicit, not restored. Any new owner revival or production re-point is separate approval-gated work.

Root retained unchanged recovery state/journals/public evidence/config/DB in main gitignored handoff/ens-demo-2026-09-05 (0700; files0600). All11 selected files compared byte-identical after copy; originals remain in the temporary directory. Public runbook command now references the retained state using portable PWD paths, never a personal home path. Private OWNER ledger12:21IST records completed approval consumption and exact pending production/recovery directions. No private state/journal/handoff is staged.

Final source/command/docs review C1 CLEAN: all public transaction hashes match the actual continuation evidence; exact separate postproof coordinates, statuses and temporary endpoint limitations match. Seven ENS Bash examples pass bash -n via stdin without execution;25 imports resolve inertly; four source-bound lifecycle tests/19 assertions and standalone strict typecheck pass. Root final E15 gate58428 exited0: full Vitest and213 Bun tests/1128 assertions, strict TypeScript and web build. No production operation or test assertion weakening occurred. Exact E15 documentation commit and main merge gate will be appended after they complete.

## Completion

Task 15: complete (commits7820ac1..f300b79). Exact subject `docs: ENS namespace evidence — registration, revocation and self-expiry on Sepolia`, Codex trailer, only README.md/docs/runbook.md. The README also corrects its stale pre-lineage/pending-A9 wording to already-verified main evidence. Main fast-forwarded to f300b79 without squash; required all-four main gate64027 exited0:2054 Vitest/108files,213 Bun/1128assertions, strict TypeScript, web build,16 Forge tests. No push. Plan E is complete in code and approved isolated live evidence; production re-point and any post-demo revival/price restoration/regrant are still explicit owner actions, not silently marked done.

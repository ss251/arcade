> Sanitized historical execution artifact. Statements reflect their recorded checkpoint and may be superseded. Historical commands are not current instructions. See the [current runbook](../../../runbook.md) for current behavior, approvals and operator commands. Personal/runtime locations and private artifact links may be redacted; public evidence and test distinctions are preserved.

# Task12 — hub discovery and read-only ENS observation

Root owns server.ts and real HTTP test/fixture; B9 owns ens.ts and unit tests. API and lifecycle agreed before implementation. Two genuine HTTP Reds09:00:14: missing GET/names and missing detail ensName/ensExpired. Simulated ordinary signed paid path already Green; test subsequently strengthened to wait until a stuck ENS read actually starts before purchase. No live chain/key actions.

Root wiring creates one observer/reader per hub lifetime, filters after canary delisting, leaves detail visible, adds read-only /names, and persists ENS metadata only after fresh matching seller/runner/publication checks in one synchronous Store Effect sequence with an active-generation guard. No transactional ledger/table or payment-order edits. Callback preserves all current fields, not pre-RPC snapshot evidence. Stop finalizer cancels observer before stopping server. Disabled root means no observer work and ignores inactive mapping/timer values.

Literal plan corrections: E8's exact-registration reader is mandatory because wildcard text survives expiry; unavailable reads return503 rather than invented404expiry. ensExpired is last observed absence, not proof of why a name is missing. Explicit public ARCADE_ENS_SELLER_LABELS maps actual full lowercase sellers to setup-compatible canonical labels; mapping is required for custom setup handles because Hello carries no label. No owner config is modified. OpenAPI/store discovery intentionally remains as specified by Plan12; ENS visibility filtering is limited to /listings, not silently added to payment authorization.

Focused/full gate, independent review and commit evidence appended after completion.
Final evidence: helper38+HTTP4 testsGreen, added actualsame-seller/newrunner/publication replacement whilefourlookupresultsheld; lateoldresolution cannotwriteENSmetadata orregistrationdoc. Wholehub586tests Green09:08:30. B9 independent rootintegration/replacement reviewCLEAN; roothelperreviewCLEAN afterfixed-orderstarvation Red→rotation andstricttruecurrent-hook Red→refusal. Final staged full2,033Vitest+158Bun/718assertions+tsc/diff Green64266, commit0d9b2e0 exactsubject/trailer/fivefiles. No live evidence claimed.

# I14 — bounded local packaging checks, not a live-proof shortcut

Implement the safe part of [Task14](../../plans/2026-09-04-I-packaging.md) after
I13. Keep the planned shell entry; no shared log overwrite, in-place diagram
regeneration, nested full gates, new credentials or replay of paid evidence.
I9/I10 media and I2/I3 CLI capture remain absent/paused, not test failures to hide.

Use existing Git/word/generator logic, exact README/timeline/partner checks and
retained render hashes. Expose the generator's actual scene object in memory
without changing emitted artifacts. If an export exists, probe a pinned local
descriptor with bounded file-only/four-thread decoding; metadata alone cannot
certify a human voice or honest demonstration. Unknown/manual/live rows remain
unchecked, exit incomplete. No readiness override or invented wire fixture.

Use native Bun tests for actual wrapper cwd behavior, early help/refusal,
malformed data and unsafe aliases; exercise one deliberately synthetic codec
fixture separately and remove only that owned fixture. Focused/strict checks,
one sequential full four-worker gate per atomic commit, then exact-one main FF.
No push, account action, keys, spending or authorization-policy change.

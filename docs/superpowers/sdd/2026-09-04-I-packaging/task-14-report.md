# I14 local evidence checker checkpoint

[Operator contract](../../../packaging-evidence.md) documents the shell/native
entry, exact scope and intentional incomplete status. This is not completion
of Task14's missing live/media prerequisites or final submission acceptance.

Actual RED: shell entry absent (exit127), then native test import failed before
the implementation existed. After implementation the first8 tests passed with
55assertions in1.168s; two-root strict check had0 diagnostics. A subsequent
native shell test covers cwd independence and early invalid-flag refusal.

Actual JSON invocation returned exit1: eight local checks passed, video missing,
Circle captured-header verifier and owner review `not_checked`; fullGate not_run,
liveEvidence not_verified and submissionReady false. The pure generator scene
function is shared with its writing entry, but this checker invokes only in-
memory generation. Tests compare unchanged README/scene/Mermaid bytes afterward.

An owned synthetic codec fixture, not demo footage, proved the native inherited
descriptor path:120s,1280×720,24fps,2880 decoded frames,152452bytes, silent audio.
Generation/probe used four-worker limits. Its media row passed while overall
readiness stayed false. Only that generated temporary directory was removed;
no source media or user artifact was deleted. Nothing was recorded or uploaded.

The old `/tmp/pe.log`/in-place regeneration/unbounded nested-test pattern was not
copied. No package install, `.env`/Keychain/model/payment/chain RPC call or policy
change. The checker lists unimplemented proof gates instead of substituting an
offline payload for a captured live Circle authorization. J4 remains paused.

Final9 native Bun tests/64assertions passed in1.295s; two-root strict0
diagnostics, shell syntax and executable help passed. Native wrapper runs from
outside the checkout; unsafe flags refuse without PATH commands. Full scope/
link/privacy freeze passed:9 paths,8new local links, empty index and no privacy
heuristic matches. Sole76879 full gate passed:5327 Vitest/242 files in69.08s;
1422 Bun/97 files,11964assertions in196.45s; root/web strict and client/SSR
355/184ms. Only three result records were annotated afterward; four code/test
files, operator contract and brief remained at frozen hashes. Atomic local
commit/exact-one main FF follows, then isolated installation reproducibility.

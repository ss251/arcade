# I11/I14 — restart, installation proof and submitted check-in

The owner requested a locked reinstall on main and a truly fresh-clone build
check before merging the single submitted check-in record. Reproduce resolution
without a source workaround; preserve manifests, locks, payment policies,
unrelated files and earlier failed observations. Keep lifecycle scripts disabled,
network/lifecycle/test workers bounded, and all gates sequential.

Use a new no-hardlink local clone, separate empty installation HOME/cache, root
and subgraph frozen installs, and exact recorded contract gitlinks. No ignored
source-checkout data, credentials or installed dependency directories may be
copied. Same-host local-clone results do not prove a new host or public GitHub.
Include local Forge tests with pinned nested libraries, not any deployment.

After green verification, preserve the owner-authored check-in document and
rebase only its one docs commit if required for the requested fast-forward.
Owner relay confirms September8 submission at07:47IST; do not access or repost
the form. Keep its dated failed-gate snapshot and original wording unchanged.
Add current installation evidence and honest technical qualifications separately.

Update only README/checklist/plan-owned execution records. Run one sequential
four-worker full gate for the new documentation checkpoint; no repeated failed
whole gates. Scrub personal paths, credentials and private handoffs. No push,
spending, key access, authorization-window change, escrow treasury assumption,
live J4 capture or J6/J10 deployment.

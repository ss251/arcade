# I8 shot-source checkpoint

The [shot list](../../../video/ethonline-2026-shotlist.md) retains eight windows
and basenames, aligned with I7's 225-second human narration draft. Each view has
an evidence label and actual local content command. It distinguishes dated
testnet/Studio observations, offline application fixtures and source documents
from new live purchases, current production and captured footage.

The legacy recorder was inspected but not run: display-0/fixed-crop capture,
overwrite mode and a non-failing low-frame-rate diagnostic make it unsuitable
unchanged on the shared desktop. I8's **exact safe-region recording command is
still pending**. I9 needs owner-approved capture framing, an actual probe and
human voice; I10 needs measured media/in-points. No takes/audio/video invented.

Native preview returned valid `diff-triage` public JSON. The first outer check
failed `unexpected_home_write`: filename/size-only inspection found 22 Bun
runtime `.pile` cache files under the fresh private home's Library/Caches/bun,
not runner configuration. A second preview against that owned home passed an
exact cache-path allowlist. Public JSON SHA256:
`74635b3aa89a2f7a178a96b7b40f275935a34cef653865c37e685af7804d8842`.
No private projection was displayed or written to a file. This is a source-
inspected native preview, not an independently instrumented network audit.
The owned temporary cache remains for later ordinary cleanup.

The actual pending-mainnet wrapper first asserted the manifest is pending,
then observed child exit1 with exactly:

```text
arc-mainnet is pending: fill config/chains/arc-mainnet.json from docs.arc.io (see docs/mainnet-runbook.md)
```

The source returns on that branch before key/RPC access. No configuration,
authorization bound or replay protection changed. Historical A9/C10/D13/E
approvals were not replayed; no Base query, Circle call or escrow send occurred.

All eight exact fenced content-command blocks then ran sequentially in a clean
environment and returned exit0, with expected source markers and bounded output.
The isolated audit initially mismatched its own comma-normalized heading check;
normalizing both expected and observed strings fixed that check, not product
code. A9's display range was narrowed to its actual milestone, excluding earlier
historical spending commands. All eight timeline boundaries matched their
30/22/45/23/32/20/23/30-second windows; total225 matched the live word checker.
The actual human voice remains unmeasured. No screen/audio recorder ran.

Five-path scope audit passed:15 new local links, empty index, privacy heuristic
without matches; `git diff --check` passed and no other gate was running.
Sole62914 four-worker full gate passed:5327 Vitest/242 files in69.36s;
1413 Bun/96 files,11898 assertions in195.33s; root/web strict and client/SSR
builds381/189ms. Only the three result records were annotated afterward;
shot list and task brief stayed at frozen hashes. Atomic local commit/exact-one
main FF follows. Owner recording and live gaps stay open.

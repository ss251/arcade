# I7 — eight evidence-qualified human-voice drafts

Implement [Task7](../../plans/2026-09-04-I-packaging.md) after I6. Preserve the
old narration/assembler and their non-recursive input paths. New text belongs
only in docs/narration/ethonline-2026. Eight fixed windows:30,22,45,23,32,20,23,30
seconds;225 total. Voice must be human; no TTS, key reads or audio fabrication.

The source-plan sample overstates current live proof. Match the retained A9,
C10,D13,E,F,G,H/J records instead: hub-record commitment is not trustless
completeness; uncertainty is not a refund; failed roots do not unwind child or
external costs; Graph paid queries/Gateway mined batching/escrow deployment
are not proven. Keep J4 paused, authorization bounds unchanged and mainnet an
owner action. Do not replay earlier live approvals to supply narration shots.

Write a read-only checker for the exact eight files with bounded sizes,
nonempty words and floor(window ×2.5) budgets. Reproduce the missing-directory
failure, add native-shell regression fixtures, then draft the text and check
all eight rows. Word count is only a proxy; owner delivery and the final
assembled duration remain unverified. One full four-worker sequential gate,
atomic local commit/exact-one main fast-forward; no push. I8 shot list follows.

# I1 — historical prerequisite record

The [interop document](../../../interop/circle-cli.md) transcribes the committed
J4 public preflight and links its original source trace. It distinguishes the
agent wallet from its backing EOA and dates the balance observations. No raw
wallet JSON, invented terms acceptance, faucet receipt, captured header or
settlement hash is provided.

The installed1.0.0 session was already usable in the previous observation.
This checkpoint performed **no CLI/wallet/chain call or key read**.
Provisioning is not replayed, and current freshness remains unclaimed. I2/I3
live stay paused; vanilla-root source behavior is explained, not exercised.

The executing-plans and worktree skills guided reuse of the clean isolated
workspace and explicit task-state ledger. The owner rule overrides fan-out and
duplicate baseline gates: no subagents/install/extra gate. Original plan
templates are retained as plans, with a current execution note, rather than
asserted as completed evidence.

Validation passed: seven scoped docs paths, fourteen new/rendered local links,
empty index and no privacy-pattern matches. The audit excludes historical code
templates rather than treating their interpolated URLs as actual links.
Sole21567 sequential four-worker gate:5,327 Vitest tests/242 files (69.77s),
1,362 Bun tests/92 files with11,713 assertions (192.96s), root/web typechecks
and client/SSR builds (333ms/169ms). Frozen content rechecked before commit.
No settlement source, validity window, cap, replay rule, production URL or
owner-only state changed. I2/I3 live stay paused; I4 is the next unblocked task.

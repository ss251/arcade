# I1 — inherit Circle CLI prerequisites without replay

Create the Plan I record from J4's committed read-only preflight. Record the
client pin, chain token, agent-wallet/backing-EOA distinction and dated balances.
Do not call login, accept terms, create a wallet, faucet, sign, pay or read keys.
Do not paste raw private responses or invent a faucet transaction/terms audit.

Document the exact source-only root Gateway refusal and absence of a sent
validBefore. Keep I2/I3 live paused with J4; unchanged vanilla verification is
not permission to run an alternative payment. Mark the rest of Plan I honestly.

Acceptance: every factual prerequisite links to the prior evidence; no claimed
captured header or settle transaction; local links/privacy/scope audit; one
four-worker full gate, small docs-only commit and exact-one main fast-forward.
Plan I skills use the existing clean isolated worktree and preceding passing
baseline, without a second baseline gate or dependency install.

# I6 — current architecture, deterministic source and actual DOM export

Execute [Task6](../../plans/2026-09-04-I-packaging.md) after the I5 README
checkpoint. Keep the inherited product palettes and straight-line typography;
include lineage/input gates, canary, ENS, ERC-8004, The Graph and sessions, plus
the owner-approved J delegation/escrow delta. Explicitly distinguish live
evidence, offline implementation and paused prerequisites.

The plan's exact sample layout predates J and includes a false universal-refund
footer. Update the content/geometry to fit current facts; do not copy that
sentence. Do not run a no-diff baseline generator blindly: inherited Python
hash seeds are process-randomized. Reproduce/fix determinism and negative-arrow
label centers with tests, preserve old media/assembler/narration, and emit a
Mermaid companion from the same source. No payment/runtime policy edits.

Use an existing pinned offline renderer through the supported in-app browser,
not a shared browser-harness, external Excalidraw service or sanitizer shim.
Bound the owned loopback listener/body/output names/lifetime, start from a fresh
temporary output directory, inspect real light/dark PNGs and DOM-render the
J12 Mermaid flows. Stop owned tabs/processes and independently check cleanup.
Copy only validated generated overview artifacts; retain scrubbed hashes and
honest limitations. One sequential four-worker full gate, source/result audit,
atomic conventional commit and exact-one main fast-forward; no push or spend.

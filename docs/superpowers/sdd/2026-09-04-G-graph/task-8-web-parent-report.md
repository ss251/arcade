# G8 web parent report — separate index provenance

September 6, 2026. Base c8d6337; root self-review only under the owner's
single-threaded machine limit. This is the G8 web follow-up, not a new live
Graph, ownership, payment, session or H14 owner-acceptance claim.

## Implementation and focused checks

The public listing decoder and the independent skill-page serialization boundary
now retain only G9's inert four-field Graph projection. An absent, malformed or
accessor-only optional graph does not discard a valid listing. The new optional
section revalidates its input and uses existing ARCADE skill-facts/skill-code/
skill-note styles; no CSS changed. D registry observations remain separate.
Index provenance says hub-reported and cached, not independently verified or
payment-backed; block and freshness are not supplied. No buying controls,
ownership assertion, stats relabelling, private state or network capability added.

The initial nine tests produced three genuine failures (six passed): first
decoder omission, second projection/render omission and lost zero evidence.
After implementation,117 focused tests across five files passed in2.10s:
new graph-evidence, existing skill-page/data/route and shared buyer Graph tests.
Exact six source/test/fixture roots plus the web ambient declaration passed strict
checking with0 diagnostics. These exercised decode/serialization/render paths,
not only source text assertions.

## Native read-only browser evidence

The existing synthetic H8 fixture gained four Graph modes, retaining its previous
modes. One fresh headless Chromium151 shell with a two-renderer limit, one exact
owned harness, real Vite/Start page and an allowlisted loopback mock hub ran six
sequential cases. No wallet, model, key, payment, live indexer or production
endpoint was used. Browser profile/cache/runtime were isolated; telemetry and
recording disabled, non-loopback DNS denied. This is not packet-capture proof.

The first script used the wrong plural route and failed its heading check.
All five owned processes and three listeners were independently confirmed closed.
The corrected run used the actual /skill/diff-triage route and passed all six:
1280px ready/zero/invalid/absent,390px maximum uint256 agent ID and ready. Each
case observed exactly one detail GET and one receipts GET, zero name/other reads.
AX region presence, exact indexed counts, provenance, HTML private-field exclusion,
inert escape probe and unchanged D counts (2 of3 validations,7 feedback) passed.
390px document, section and individual count rows remained contained.

Four images were captured and inspected. The1280-ready image caught the
pre-scroll frame despite the earlier live DOM bounds check; it is not visual
evidence of the Graph section. The1280-zero and both390px images independently
show the full section and qualifications without clipping. All images and raw
runtime logs stay private. Both attempts together:10 exact PIDs checked,5 unique
listener origins closed (the web port was reused),0 remaining profile processes.
No shared process was stopped. No browser remained during the full gate.

## Acceptance scope

Only offline decoder/browser presentation is proved. Existing live G6 records
do not suddenly establish current freshness, whole-market totals or canonical
per-skill ownership. Optional absence is not indexed zero. H10 durable session
authority and H14 owner ranking/real-wallet recording remain separate pending
items. No new spend, approval replay, production ENS/mainnet change or push.
The sole max4 full gate and publication/source audit follow.

## Final gate — 22:36 IST

The sole full gate (61261) exited zero: 4,282 Vitest tests across 191 files
in 58.38s; 872 Bun tests across 58 files with 6,383 assertions in 163.92s;
root/web strict checking and client/SSR builds (345ms/174ms). It includes
G9's final detail-ID guard, which previously had only targeted validation.
The frozen source was unchanged during this gate.

Final audit passed 14 G8/G9/web source pins, 10 scoped paths, 191 local links
and public-addition privacy checks. G7, Store, session/buying logic, D evidence
and styles remain unchanged. The source-command-commit workflow stages only
these ten files for an atomic local commit, then an immediate one-commit
fast-forward to main. No duplicate main gate under the owner's rule.

Web source SHA-256 observations:

```text
a3297eeceaa8dbb714d2a893977bfb4494b2be48f77238698864ea804cb1e045  apps/web/src/lib/hub-decode.ts
2509bb03d521b1ce5753802bc8056be3d906f1b914934eebb0daf6c7e21f01cc  apps/web/src/lib/skill-page-data.ts
2d69e394016ee61060c13d59a97c62c62b2bce20d4402932bd93ed01e7074741  apps/web/src/components/graph-evidence.tsx
544be104d49a02591fdc3b7ee4939e0a980144988ed670ad6ca6019e80e12920  apps/web/src/components/skill-page.tsx
75ee56a3ab135dfdb9a381df6d9bf71aef09e5992bbdc4b20ad627c7a2eea745  apps/web/test/graph-evidence.test.tsx
8df6afa26e51ec095c264b3884daa7b5134ad9fdcf4b7c96ee0b67b782a55729  apps/web/test/fixtures/skill-server.ts
```

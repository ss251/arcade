> Sanitized historical execution artifact. Statements reflect their recorded checkpoint and may be superseded. Historical commands are not current instructions. See the [current runbook](../../../runbook.md) for current behavior, approvals and operator commands. Personal/runtime locations and private artifact links may be redacted; public evidence and test distinctions are preserved.

# G3 parent decisions — local staged manifest and inactive ABIs

September 6, 2026,07:05IST. F implementation fast-forwarded to main68bb915 after
all four branch gates; main gate remains active. This note accepts the bounded
design, not source release. Explicit release follows main gate and G rebase.
G2 is committed9c691d1. G1's historical indexed-runbook proof remains unchanged;
no replay of its consumed one-shot deployment or new spending is authorized.

Parent fully read the original G3 task, G2 decisions, complete216-line G3
readiness f073e5d848689d1183d7b94ea198e2540078834a63d0465a63429eaa7d6c1e17,
current schema/scaffold/package/manifest and the independent ownership recheck.
Accept staged buildability instead of the literal missing-handler manifest.

## Ownership and interface

Renderer author owns build-manifest.ts, subgraph.template.yaml, splitters.json,
checks/manifest.bun.test.ts, narrowly related package scripts and current-manifest
assertions in scaffold/schema checks. ABI author owns four planned inactive JSON
ABIs and checks/abis.bun.test.ts. Parent owns README staging/provenance note,
mirrored ignore entries and tracked-to-generated manifest transition. Preserve
schema, smoke.ts, v1 ABI, historical fixture/evidence and toolchain lockfile.

Pure interface: renderManifest(template:string, chainConfig:unknown,
splitters:unknown):string. Separate explicit-path local orchestration and guarded
CLI; imports do not write. Fixed public Arc testnet config, no ambient network,
dotenv, credentials, subprocess discovery, deployment or remote dependency fetch.

The active result has precisely one FeeSplitterSmoke source, FeeSplitter ABI,
smoke.ts mapping, Settlement/Splitter entities and existing Settled handler.
Zero registry sources and zero dynamic templates. Pilot block zero is an explicit
historical smoke exception, never a verified creation height. No listing context,
metadata authority, binding flags or pretend three-registry coverage. Include
prune:never explicitly. This is local staging, not full G3 live verification.

Strict unknown-input validation rejects extra keys, accessors, wrong network/
registry pins, unsupported emitters, malformed/zero addresses, duplicates,
controls and unsafe block numbers. Support integers0..2147483647 explicitly;
the active pilot remains exactly its accepted zero. No silent coercion or first-
duplicate wins. Initial list contains only address/startBlock. Template markers
are exact and resolved once; parse final YAML and validate exact shape/paths
before output. Invalid input leaves prior output unchanged with fixed diagnostics.
Do not broaden into arbitrary templates or hostile-filesystem guarantees.

Adapt existing scaffold/schema assertions to pure rendering so a fresh checkout
needs no ignored YAML. Retain all historical hashes and money/event/schema
assertions. Actual owned-temp CLI tests must run from another cwd and show
import/invalid-input nonmutation. Codegen/build scripts render with no-env nested
Bun and pinned installed tooling, never execute deploy. Parent removes only the
validated generated manifest from tracking, leaving generated local bytes.

## ABIs, trust and later activation

Stage the four event subsets with truthful provenance; exact local contracts/
core subset and pinned plan candidates are not explorer deployment proof.
Identity includes Transfer as well as Registered/URIUpdated/MetadataSet.
Directly parse all four via installed Graph ABI tooling and compare complete
event layouts, including indexed flags, signedness, widths and argument order;
negative mutations must fail. Active pilot codegen does not validate inactive ABI
use. No new dependency or local core ABI change is needed.

G4 activates settlement sources with real mappings and occurrence/collision tests.
G5 activates registries only with real handlers and resolved source provenance.
No Marketplace zero counters before coverage; no canonical listing assignment or
dynamic trusted template promotion from arbitrary metadata. Keep bounded immutable
agent-scoped claims distinct. Later independently reviewed bindings must establish
actual deployment/code/USDC, current owner and hub listing assignment, not booleans
or reciprocal strings. Registry creation blocks/proxy history and pilot creation
remain separate bounded keyless evidence work; no fake completion if unavailable.

G3 author gates: genuine failing collected tests before implementation, focused
Bun checks, exact nested strict program, actual pinned codegen/WASM compilation.
Parent reviews and runs the sole full root gate before atomic commit, then stages
sanitized historical copies. No Matchstick/runtime-indexing/live claim from static
checks. Parent retains every author/reviewer checkpoint and literal-code deviations.

## Explicit local source release —07:07IST

All four main gates passed on68bb915:2748Vitest/124files,523Bun/38files/
4650assertions,root/webstrict,clientSSRbuild and16Forge. Main/worktree Bun totals
differ because retained private fixtures differ; do not infer public-only totals.
G's ten commits rebased without conflict; range-diff all ten exact equals.
G2 now0d79dbd17a05fe1764e9e31c5129724a0e3a6758. Source release is active for
the two bounded G3 slices above. Focused tests/exact nested strict permitted;
coordinate codegen/build through parent. Root alone owns full gates/Git/publication.
No keys, live write, dependency change, G4/G5 implementation or push released.

# G14 parent integration — September 5, 2026

All four original worker reports are retained unchanged and copied with a public
banner. Parent read the production code, manifest, both complete tests and every
report. Independent review reproduced accepted parent/reused child job IDs; the
actual regression and correction are recorded in the review follow-up. The final
consumer rejects both parent reuse and repeated child handles, including the
otherwise permissible definite Graph nonsettlement form.

The independent reviewer also found observer-timeout leaks in the new fixtures.
Two genuine failures demonstrated a scoped child surviving its observer and a
direct TERM-resistant child surviving cleanup. The fixture-only correction now
interrupts the actual scoped Effect and awaits exact owned handles with bounded
TERM/KILL escalation. It does not change shared execSkill or claim downstream
purchase cancellation. The final independent run passed 13 Vitest tests plus 11
Bun tests / 45 assertions in 38.15 seconds. Its real Unix deadline case took
32.048 seconds and made one attempt. Runtime source/lifecycle review is CLEAN.

An exact strict compiler run including both nested tests then exposed 13 diagnostics
in the existing verdict fixture, which earlier root-only checks did not cover:
IncomingHttpHeaders was incorrectly queried as a value, createServer's overload
was used in place of RequestListener, and the fake Bun fetch lacked preconnect.
Parent changed only those fixture types and added a refusing preconnect member,
without changing production behavior or assertions. At 17:42:33 IST, the 13 focused
Vitest tests passed and exact root-options compilation of run.ts plus both nested
tests reported zero diagnostics. Independent read-only review of that correction
is CLEAN. The runtime test and all earlier reports remain unchanged.

The first report's import-time RUN-window observation is retained as its historical
claim; the later actual subprocess import test independently demonstrates zero IO
and clean exit. Neither an import nor the simulated broker constitutes a paid
Graph/Arc result. G12 query costs remain provenance, not duplicated child billing.

The current .08 budget check is source-backed: daemon opens a distinct broker ledger
for this job using its manifest ceiling. The separate root lineage ceiling does not
replace that ledger. Sibling pure G12 imports resolve in the normal repository-based
script runner; no isolated packaging guarantee is claimed. The unchanged loop-probe
60-second profile cannot establish the new 135-second wallet's worst-case nested
behavior. Historical A9 proof remains immutable and is not reinterpreted as G14.

Independent public-copy review is CLEAN: four exact bannered copies, all 27 local
links and a targeted privacy scan passed. The separate full precommit test/type gate
follows this frozen checkpoint. F1's later accepted-transfer gate enables F2–12 code work, but G merges
still wait for the full F dependency. Studio, future Base-mainnet spending and any
new live nested profile remain separately gated. No live action or push occurred.

> Sanitized historical execution artifact. Statements reflect their recorded checkpoint and may be superseded. Historical commands are not current instructions. See the [current runbook](../../../runbook.md) for current behavior, approvals and operator commands. Personal/runtime locations and private artifact links may be redacted; public evidence and test distinctions are preserved.

# G15 / G12 paid-evidence readiness — local read-only review

Status: preparation only; no request, key lookup, signature, payment, test, Git command or source edit performed. This note is not release to implement or execute G15.

## Authority and evidence boundary

- Read the full actual Task 15, current Graph client/run and all four Graph test files, wallet-risk-note composition/tests/manifest, the retained paid authorization, and relevant installed Graph/x402 implementation source.
- `[retained private authorization reference]` is the current exception: Base mainnet Graph queries only, exact 10,000 atomic native USDC each; at most five for recorded evidence and ten GLOBAL across evidence plus video. Preserve at least five for video; neither quota is a target or a diagnostic/retry allowance.
- Reported initial funding is 1.000000 USDC, not a fresh balance observation. Expected payer is `[EXPECTED_BASE_PAYER]`; key selection is service `[APPROVED_PAYER_KEYCHAIN_SERVICE]`, account `[APPROVED_PAYER_KEYCHAIN_ACCOUNT]`. No key value belongs in argv, environment forwarded to another process, files, logs or public artifacts.
- Literal G15 additionally buys a new $0.05 Arc job plus gas. That is NOT authorized by this Base exception; prior A9/C10/D13 permissions are consumed. A new Arc-settled demonstration needs separate owner approval. Actual Base purchases plus unpaid local TestRail composition can be labeled partial evidence, never full G15 cross-chain settlement PASS.
- Missing-key refusal must be tested in the actual consuming process, not by changing only the buyer's environment. Do not delete the owner's Keychain item. Use an isolated, explicitly absent key configuration and a forced cache miss; cached replay is a separate no-new-payment case.

## Exact existing seams and behavior

`skills/counterparty-graph/graph-client.ts` exports:

```ts
type PaidQuery = (args: QueryArgs) => Promise<PaidResult>
interface QueryArgs { readonly subgraphId: string; readonly document: string; readonly variables: Record<string, unknown> }
interface PaidResult { readonly data: Record<string, unknown>; readonly paymentTx: string | null; readonly costAtomic: string | null }
interface QueryOptions { readonly fetch?: typeof globalThis.fetch; readonly signal?: AbortSignal; readonly timeoutMs?: number; readonly now?: () => number }
makePaidQuery(privateKey: string, options?: QueryOptions): PaidQuery
paidQuery(args: QueryArgs & { readonly privateKey: string }, options?: QueryOptions): Promise<PaidResult>
readPayerKey(env: Record<string, string | undefined>, run?: Runner): Promise<string>
```

- `runGraphJob(job, env, {readKey?, makeQuery?, signal?})` is the actual consumer seam; `assess({address}, {payerKey, query})` performs one identities query and only conditionally a second attestations query pinned to the first block hash. A valid empty identity result costs one query, not automatically two. `assess` currently requires a valid key even for an injected cached query.
- The client directly uses installed `@x402/fetch` 2.25.0's core classes and `ExactEvmScheme`, NOT `@graphprotocol/client-x402`'s fetch wrapper. It pins Base 8453, USDC `0x833589fcd6edb6e08f4c7c32d4f71b54bda02913`, merchant `0x79dc34e41b2b591078d3de222c43ecaabd52fccb`, Agent0 subgraph `43s9hQRurMGjuYnC1r2ZwS6xSQktbFyXMPMqGKUFJojb`, $0.01, EIP-3009 and exact USD Coin/version 2 domain. Advertised internal HTTP resource is metadata, never a request target.
- One unsigned POST, one validated challenge, one signature, one paid POST; paid 402/500 does not retry. The signer restricts exact authorization fields/nonce/time/domain. Receipt proof correlates the payment transaction with pinned-token Transfer and AuthorizationUsed events and its block. Read-only receipt polling is bounded to 15 seconds and is not a second paid dispatch.
- Factory concurrency, two attempted queries, two signatures and signed-uncertainty refusal are IN MEMORY. A new factory/process resets all; `paidQuery()` creates a new factory per invocation. No disk result cache, global ten/five counter, expected-owner-account pin or balance-floor check exists. `runGraphJob` eagerly reads the key before constructing its query.
- Current key reader chooses exactly one explicit key mechanism, no fallback; Keychain argv selects service but not `-a` account. A harness can select the exact service AND account with existing bounded `runKeyCommand`, validate the returned key only in its consumer, derive and compare the expected address before creating a payer. No credential lookup was done for this review.
- Installed Graph client 1.0.0 caches only a wrapped fetch/private-key identity in module memory, not query results. Its `createGraphQuery` uses ambient fallbacks and `wrapFetchWithPayment`. Installed wrapper has hook-driven recovery which can create a fresh payload and paid request; no recovery hook is registered in the current direct production path. Do not replace it with that wrapper or claim an unconditional retry currently occurs.
- Existing tests cover actual synthetic signer recovery, strict challenge/receipt validation, one-shot paid errors, local uncertainty, deadlines, and key/refusal boundaries. They do not establish durable cache, cross-process caps, owner identity or balance-floor compliance. These are source-inspection gaps, not newly executed Reds.

## Smallest safe harness: one approval owner, one consumer, explicit replay

Prefer a bounded harness around `runGraphJob`'s existing injected `readKey`/`makeQuery` and `QueryOptions.fetch`, not a new payment implementation or shared SDK change. A reviewed recorder must remain transparent to existing client validation; if that cannot be demonstrated, stop for a narrow source-hook decision rather than bypassing validation.

1. Capture one fixed approval/payer namespace outside disposable run directories. Hold an exclusive cross-process claim. Per-run directories are fresh 0700; journal/cache files exclusive 0600, no symlink/hardlink aliasing. Validate existing approval, records, digests and pending intents BEFORE key lookup. Missing/corrupt expected state or uncertain ownership refuses; changing run ID must not reset the global budget.
2. Count a durable 10,000-atomic reservation for each cache-miss query before any signable work. Retain every unresolved reservation across restarts; do not refund uncertainty, process death, timeout, bad response or an unsigned-looking missing row. Safest initial harness never automatically reclaims even a definitively unsigned reserved slot. Report reserved/may-have-signed, forwarded, receipt-proven paid and cache-hit counts separately. Global reservations <=10; recorded-evidence reservations <=5; video allocation separate.
3. Validate fixed origin/subgraph/document/variables, payer/token/merchant/price and overall deadline. Probe/query RPC reads have fixed method/target and byte/time limits, no endpoint override or redirects. Validate chain ID and exact 32-byte uint256 balanceOf result, matching response IDs; retain block identity/time. Read before paid work and immediately before paid forwarding; require balance >=910,000 atomic to admit another 10,000 debit while preserving the 900,000 floor. Read again after every dispatch, including non-200/uncertain responses. A failed read, unexpected delta or below-floor observation stops; below 900,000 is MAIN OWNER-NEEDED. No balance-only claim of payment correlation or protection against unrelated external wallet spending.
4. Fsync an intent (query/policy/source digests, amount, quota allocation and balance evidence) before the first signable query. A bounded transport recorder fsyncs the exact validated-size 402 bytes/header and body digest before releasing that response to the client. Before forwarding a paid request, correlate its authorization to the same intent, persist nonce/authorization digest and a once-only forward intent, then recheck cancellation/authority and send at most once. The earlier reservation covers a crash between signing and forwarding; do not invent an observed signer-entry event from a fetch callback.
5. Preserve every bounded paid response privately BEFORE client parsing/receipt checks can fail: status, relevant headers, exact bytes and digest, then correlated receipt/block evidence from the read-only proof requests. No unbounded clone/tee, duplicate response consumption or reconstructed Response that hides original URL/redirect/header failures. Bound recorder IO and preserve signal/deadline checks before any delayed send. Partial/truncated response or fsync/close uncertainty stays unresolved; it is not a reusable success cache entry.
6. Cache EACH validated query result immediately, not just the final assessment: first-query success survives second-query failure. Cache key covers canonical document+variables, endpoint/subgraph, payer/token/merchant/price, policy/source versions and pinned block semantics. Store original result, receipt proof, response hash and acquisition time; second query uses the exact first-query block. A corrupted/incomplete cache refuses, never silently refreshes with a new payment.
7. On a full prior-success replay, return the immutable validated assessment artifact without entering `runGraphJob` or reading a key. Label historical block/acquisition and `cacheHits`/`newPaidQueries:0`; retain original source paymentTx/cost, not a fabricated new payment or null cost. For partial cache reuse, preflight the known state and reserve possible misses before entering the real consumer; its injected PaidQuery returns exact cached PaidResult on hits and delegates misses to the one captured factory. No fake key to satisfy `assess` and no claim that cached replay is a fresh runner execution.
8. Unknown outcome stops the sequence immediately. Retained nonce/transaction evidence permits a separately bounded read-only reconciliation, never fresh-signature retry, resend, automatic replacement query or filling the five-query allowance. Close/release does not remove unresolved reservations. Journals/caches need cooperative local-filesystem durability, not a claim to resist arbitrary storage faults or malicious rewrites.

## Composition and literal-plan corrections

- `wallet-risk-note` calls the actual Unix broker once for `usdc-flow-check` (Arc child ceiling $0.01), then once for `counterparty-graph` ($0.05). No automatic hire retry; ambiguous transport fails closed. Its $0.15 parent and child Arc accounting are distinct from Base Graph query costs. Unpaid TestRail integration may reuse the real cached Graph assessment, but must label cached Base provenance and zero new Base dispatches.
- Literal `arcade-buy ... --json` is not a current JSON API: `packages/buyer/src/cli.ts` prints human-readable status/result/receipt and does not implement that flag or the stated poll-URL output fallback. Do not parse its text as a proof or invoke it under the Base-only approval. A later authorized Arc harness should use an actual typed buyer API and authenticated result contract, with separate scope review.
- `output.sources.length` counts evidence sources, not new paid dispatches when caching exists; use the durable query journal. A balance delta alone is not transaction-correlated payment proof; keep existing receipt/nonce/log verification plus the independent balance observations. Do not turn failed command output/missing JSON fields into `settled:false` or “buyer balance untouched.”
- Missing key plus cache hit is expected replay, not a negative signing test. Force only an isolated miss in the controlled consumer, observe zero signer/paid sends and an actual refusal/no output. Do not spend a new paid query merely to exercise failure, and do not claim unknown paid acknowledgements revoke authorization.
- Public evidence is an explicit whitelist: mode, hashes, fixed chain/token, query/reservation/cache counts, atomic balances, verified transaction links and conservative verdict. No raw paid request headers/signatures, grants, private file locators, provider diagnostics or unsanitized full response dumps. Query-payment proof does not make an ERC-8004 attester's service settled.

## Proposed ownership and three next steps

- Harness-only release: NEW `scripts/e2e-graph-cogs.sh`, `scripts/e2e-graph-cogs.ts`, `scripts/e2e-graph-cogs.bun.test.ts`; keep bounded recorder/journal/cache implementation in the TS harness initially. No hub/runner/buyer/payment/dependency or wallet-risk-note edits required for this controlled consumer route. Publish reviewed evidence later in the plan-owned `docs/runbook.md` and `README.md`.
- Normal installed-skill cache/global policy is a DIFFERENT integration requirement. If required for runner/video execution rather than explicit artifact replay, release `skills/counterparty-graph/run.ts` + `test/run.test.ts`/`test/run.bun.test.ts` for lazy, cache-aware consumer wiring; release `graph-client.ts` + `test/graph-client.test.ts`/`test/graph-client.bun.test.ts` only if exact awaited journal/signing hooks or account-qualified key selection cannot safely remain in the harness. Do not silently make injected cache data production authority or add arbitrary cache-path environment overrides.
1. Root settles the precise Base-only mode, recorded subject, fixed approval namespace and artifact-replay versus actual runner-cache requirement; Arc-settled G15 stays OWNER-NEEDED separately. Then release exact paths and fixed recorder/cache interfaces.
2. Implement offline TDD: actual owned-temp cross-process/restart counter races, partial cache/response loss, crash between reservation and send, journal IO failure, no late signer/send, 402/amount/response capture, malformed balance/low floor, expected account mismatch, and no-network/no-key complete replay. Preserve meaningful Reds; use only explicit public synthetic keys in isolated fixtures. Independently review before any real consumer run.
3. Only after release and fresh balance/state gates, run one bounded Base evidence sequence (normally one or two new queries, not five by default), cache and independently reconcile every result; stop on first uncertainty. Reserve video capacity and reuse disk artifacts. Record Base/live and TestRail/local separately; leave full new Arc G15 NOT_RUN until separately approved.

## Local source pins (SHA-256; no execution claims)

| Input | SHA-256 |
|---|---|
| Current paid authorization | `292ac5c60cc803f705cb2d375b6845757d83da8b8aba9b8fde15f456d8ebd74f` |
| Actual Plan G | `43182866504db5119d8e3d24248b96fba6ea7d7291cc08bdd6c6c9d3406cb656` |
| Graph client | `bb79eca1715ba71ee42b7e43019e66215e5e50b7a3963dd139a84604e0e9d315` |
| Graph consumer | `bd05fa37d870f9749c87ff7a3ea863895ee152582dccf23d99ac2928435e4193` |
| Wallet composition | `5317a6606120a84e25b9dc49d2a6239f27c4e8a683c09d3f6e84d4230823fa81` |
| Installed Graph createGraphQuery / fetch cache | `08c16645e3898a065a079d3b41af3c399c619243d14394f17bc7041251f47263` / `2265f1e2215b5b06b547b42d6325f1aa9d23917750ee7d0d3b1d3a7955a861b5` |
| Installed x402 fetch / exact signer chunk | `df19cfe324288677b4a3bdac79e902c5205891683093f516c11bb4e273a75681` / `0c2fcd002471b6fc3ed69d580b71324862ed25eecf2bd094c23af838d0a7c610` |

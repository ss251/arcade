> Sanitized historical execution artifact. Statements reflect their recorded checkpoint and may be superseded. Historical commands are not current instructions. See the [current runbook](../../../runbook.md) for current behavior, approvals and operator commands. Personal/runtime locations and private artifact links may be redacted; public evidence and test distinctions are preserved.

# G6 parent decisions — September 6, 2026

Preparation only while G5 is in flight; no implementation release yet. Parent
read full Task6 and the302-line readiness, reviewed exact splitter provenance
and the two current public-hub GET observations. The readiness's candidate-only
join, append-only known profiles, fail-closed decoding and owned atomic write
contract are accepted. Actual tests must cover the implemented boundaries, not
merely repeat this description. Current empty hub is availability, not owner work.

## Reviewed source inventory

Retain pilot0xf95c8afefae677fdcfc7bd5b8aaaf3702db99206 at the explicitly named
historical startBlock0 exception. Approve the A9 V2 static emitter
0x9e304ec13dd862c81ee8caa8fd262dac426fbedf at verified creation block60460646.
Use its actual Settled and SettledTree mappings; no metadata template creation.
The approved immutable seller/treasury is0xcf821769ed3c0e55e152745377bb833d7155a78a;
pilot seller/treasury is0x3b2bbb840a9570223adbf2172a33bb77fe8d21af. Both observed
USDC0x3600000000000000000000000000000000000000 and fee500bps. Full runtime outside
declared immutable slots, including metadata trailer, matched the corresponding
local compiler artifacts at fixed block60670577. Independent local reconstruction
matched the observed hashes. See task6-splitter-code-evidence.json and
task6-splitter-provenance-review.md. This is a dated projection/hash proof, not
raw-RPC retention, an independent fresh compile or historical skill assignment.

Renderer may narrowly export its immutable known-source profile lookup for the
generator. No caller-supplied trust flag, explorer discovery, zero-height fallback,
unknown profile or conflicting seller may widen that policy. Explicitly seed A9
in reviewed splitters.json even with no current hub announcements. Preserve every
historical pin; the generator is not a catalogue-pruning tool. Final renderer
must deterministically support exact selected known pins and reject duplicates,
changed heights, unknown fields and unknown addresses.

## Coverage and deployment scope

Keep the three G5 registry templates inactive: current implementation ABI checks
at60670577 do not prove creation-to-head proxy history. No arbitrary activation
epoch is selected just to obtain registry counts. Keep Marketplace and canonical
Listing/Agent/Splitter links absent. In particular, a per-seller emitter is not a
per-skill payment counter, and metadata claims are not settlement attribution.
Retain the four templates as inactive compile/test artifacts. Evidence must say
two reviewed static settlement emitters, not complete ARCADE/ERC-8004 coverage.

The owner's explicit resumed G2–G6 execution includes Task6's ordinary fresh
Studio deployment. It does not authorize replaying G1's consumed smoke operation
or any payment. After local tests, independent review and build, parent may make
one fresh no-payment deployment of reviewed v0.1.0 to the existing owner-approved
slug, through a finite credential-in-process runtime with a fresh owned journal.
Record exact CID/version/acknowledgment and preserve G1 untouched. Never graph
auth/global credential cache, argv secrets or automatic redeploy on failure.
Any changed version/redeployment must be a separately reviewed implementation
step, not an unbounded loop. No remote action is released to G5 authors here.

Live acceptance queries exact already-settled A9 root/child/grandchild and tree,
using retained occurrence/nonce/money evidence, matching deployment identity,
index height and no indexing errors. Marketplace null and absent canonical links
remain explicitly expected limitations, not fabricated zeros or a global total.
Do not claim Studio Synced/current-head finality from an indexed historical proof.
Do not rerun A9 or send a new Arc purchase. Preserve every failed observation.

## Files and verification after release

Generator owner: scripts/graph-splitters.ts and its Bun suite. Integration owner:
renderer/template/splitters.json plus narrowly needed manifest/scaffold checks.
Parent: root scripts, subgraph README, runbook, task reports/publication and Git.
Do not edit G4/G5 mapping/schema behavior or G7/hub/MCP as part of G6. One full
repository gate per commit; focused actual renderer/TypeScript tests and actual
Graph codegen/build remain separately labeled. Imports/help/default dry run must
not mutate files, read credentials or issue payments. Default dry run requires
explicit --hub; --write alone permits only the validated local list replacement.

H-before-G8 remains the recorded integration exception. Later stats values and
their source label must come from the same selected read. No emitter subset can
be relabeled complete hub call/catalogue totals. These are technical scope choices,
not owner requests. Newly approved Base10/5 paid-query authority remains separately
queued and unused; it does not authorize an Arc-settled G15 purchase.

## Deployment command boundary and source split

Inspection of installedgraph-cli0.98.1 confirmed deploy reads only a key flag or
global ~/.graph-cli.json, not a secure deploy-key environment route. Do not add
a root deploy alias that encourages flag secrets or globalauth storage. After
G5commit release a separate bounded scripts/graph-deploy.ts plus its Bun suite,
reusing the reviewed G1 one-attempt JSON-RPC/journal transport design without
editing G1's retained runtime or consumed journal. It may support the fixed
Studio endpoint/slug and explicit reviewed CID/version plus a fresh owned journal;
help/import must perform no I/O or key lookup. The consuming process selects exact
Keychain service arcade-graph-deploy-key and accountGRAPH_DEPLOY_KEY with bounded
absolute/security child lifetime, output capture and fixed diagnostics. No key
value in argv, output, persistentauth config, files or child environment. Validate
CID/version/journal/source intent before key lookup; preserve unknown outcomes,
never automatic retries/uploads/builds/redeploys. Root separately uploads reviewed
public build artifacts using the pinned CLI's keyless build/IPFS mechanism and
pins the returned CID before the single deployment. Any public CLI remains an
operator-invoked action, not standing approval or permission to replay G1.

Disjoint G6 ownership after release: generator twofiles; renderer/template/list
and narrow checks; deploycommand/two-file scope and focused offline tests. Parent
owns root aliases, README/runbook/evidence and the sole fullgate. No dependency,
G4/G5 mapping/schema or existingG1runtime change is needed. Cross-file interface
is approvedSplitterPin(address) returning only immutableaddress/seller/startBlock,
or null. Preserve requiredpilot while optionally accepting the exactA9V2profile;
the reviewed committed list will contain both, deterministically address-sorted.

## Explicit post-G5 release — 03:14 UTC

G5 committed42769befd5df157d7aa70c0d251fe8f59dbd7c21 after its one complete
gate/build and independent source/publication acceptance. TrackedGROOTclean
rechecked (only ignored-by-policy .superpowers/ remainsuntracked). Parent now
releases the three disjoint implementation slices above; each author must read
these complete decisions and Task6/readiness before edits. No G6author receives
network/Keychain/upload/deployment/payment/Git authority. Use synthetic keys and
owned local fixtures only; root owns any actual reviewed remote step later.
No codegen or nativeAS concurrently; integrator may run one coordinated local
codegen when source paths/interfaces are ready, but parent owns finalbuild and
solefullgate. No new dependency, source-order or shared-hub integration change.

## Atomic local/live checkpoints — 03:40 UTC

Commit the reviewed generator, fixed deployment command, selected manifest and
local evidence first, after one complete gate/build. Keep Task6 live acceptance
open. Then inspect the actual public build inventory, deliberately upload it
once, pin the CID and use one fresh owned journal for the authorized v0.1.0
deployment. A separate evidence commit records actual acknowledgments and
read-only indexed proof, with its own single full gate. This preserves incremental
history without mixing an unperformed deployment into the implementation claim.
The separate private query helper must be quiescent before any complete gate
because the root test command also collects retained private fixtures.

# G1 partial live publication — parent review

September 5, 2026, after the publisher's 14:00 UTC freeze. CLEAN within this
selected documentation/source-contract scope. This is not a completed live G1
gate, a new deployment, or permission to advance G2–6.

Read every changed document and the complete scaffold test, all three historical
reports and the private publication audit. Independently checked that each public
copy equals the common banner plus its documented privacy substitutions; original
report SHA256 values remain unchanged. All 43 then-present local links resolve.
The six unchanged smoke source/dependency fingerprints match the frozen deployed
checkpoint. Targeted selected-file checks found no personal paths, private journal
identifier, credential material or private clickable targets. Public chain/source
hashes retain context; this is not a whole-repository secret-scan claim.

Independently reran all six scaffold tests / 36 assertions and applied the exact
root compiler options to the nested test: zero TypeScript diagnostics. The test
retains the original five source contracts and checks that the dated deployment
and data observations are explicitly partial. The historical runtime report's old
sanitizer claim remains visibly superseded by its independent security correction.

## Later bounded keyless read

One additional unauthenticated read of the existing returned endpoint completed
at `2026-09-05T14:10:24.097Z`: HTTP 200, 250 bytes, valid GraphQL data, exact CID
`QmePuPnHraVaV9TmxaAwCCMKwTD8iFW96eoEUfSA1BoCW8`, indexed block **42,441,110**,
hash `0x3600a989a2412632a0277a7b08bcb9a04dbdb09d38d489420696aa07cc3e0717`,
and `hasIndexingErrors: false`. Latest settlements and the exact historical
`9a706d57…` filter were again empty. The request had no credentials or cookies,
no redirects or retries, a 15-second deadline and 64 KiB actual response cap.
It made no payment, upload or deployment. This shows further indexing progress,
not full synchronization or a known-settlement match.

## Full local gate

The separate full `bun --no-env-file run test` and
`bun --no-env-file run typecheck` command completed with exit 0. Vitest passed;
its numeric summary was omitted by bounded tool output and is not reconstructed.
Bun reported **347 tests / 3,683 assertions across 32 files**, zero failures.
That discovery includes the 19 preserved private runtime/security regression
tests; it is not a claim that all discovered files are publicly committed.
Both root build-mode strict TypeScript and web strict TypeScript passed.

The first additional subgraph build invocation placed `--cwd` incorrectly and
printed Bun help with exit 0; that is NOT build evidence. The corrected
`bun --no-env-file run build`, executed from `subgraph/`, actually compiled the
WASM and wrote `build/subgraph.yaml`, then exited 0. Generated outputs remain
ignored. No manifest, ABI, schema, mapping, dependency or lockfile was changed.

After adding this parent checkpoint and its cross-links, the selected eight
documents passed the same targeted privacy check and all **47** local links
resolved. The final scaffold rerun remained six tests / 36 assertions and
`git diff --check` was clean.

Only this reviewed documentation checkpoint and its scaffold status assertion
are to be committed. No main merge, push, paid query, new settlement or live G1
completion is claimed. Canonical F-before-G dependencies remain in force.

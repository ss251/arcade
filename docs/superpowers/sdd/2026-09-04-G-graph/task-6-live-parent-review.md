> Sanitized historical execution artifact. Statements reflect their recorded checkpoint and may be superseded. Historical commands are not current instructions. See the [current runbook](../../../runbook.md) for current behavior, approvals and operator commands. Personal/runtime locations and private artifact links may be redacted; public evidence and test distinctions are preserved.

# G6 acknowledged deployment — parent evidence checkpoint

September 6, 2026, 04:16 UTC gate/source checkpoint. **Partial evidence accepted;
Task6 indexed A9 acceptance remains open.** Implementation commit
8532df81d965c7ebf675cc4a40462a89d9412c2f is unchanged.

## Review and evidence

The parent fully read the frozen indexed-query helper/tests, author report,
independent query review and independent retained upload/deployment audit.
The private query helper passed14 Bun tests/135 assertions plus exact strict;
independent review repeated them, checked actual persisted GraphQL fields with
modeled query facilities and18 supplemental boundary assertions. These are
offline checks, not a successful live result.

One reviewed public-build upload returned CID
QmWL6jCCNvRkmB3mvPaxvMH7931AvQ5Y7jmBzCJ2gdpjHF at03:59:52.062UTC.
One Studio deployment was acknowledged04:01:17.711UTC. The owned three-row
journal and public build inventory match the selected operation. **Deployment
is consumed and must not be replayed.** Local record consistency is not packet
capture, authenticated journal evidence or independent IPFS retrieval.

The first exact A9 query at04:03:58.766UTC was refused; the helper did not expose
its body/hash/meta. Separate04:05 and04:08 diagnostics reported startup absence.
A later metadata read at04:14:55.681UTC observes the exact deployed CID,
hasIndexingErrors:false and block10795110. This establishes that indexing
started, not that A9 block60523612 was reached. Its212-byte body SHA256 is
1327aaebc532e5302fb4ee2f76b518e26b480c0a750c5180da458a6b4c0eef47.
Metadata-only reads do not observe Marketplace or Tree values. Raw response
bodies are not retained; bounded selected facts and hashes are retained.
No automatic request loop, new purchase, payer-key access or redeployment ran.

## Sole full gate for this documentation checkpoint

The one complete test/typecheck invocation exited0:

- Vitest:2941 tests /131 files.
- Bun:829 tests /51 files /5770 expect calls.
- Root project-reference strict build and web noEmit strict:0 diagnostics.

The14 private indexed-query tests are now collected, unlike the previous local
implementation gate. Counts above are actual output, not arithmetic inference.
No unchanged full-suite rerun, new Graph build or native AssemblyScript repeat
was performed. All11 non-document implementation/configuration file hashes
match the local implementation checkpoint after the gate.

## Publication and remaining acceptance

The public checkpoint includes the live brief, three independently preserved
historical source/review reports, this parent record and later operator/index
notes. Historical bodies remain under the standard warning banner. Only two
literal personal Bun-bin directory occurrences in the query author report are
substituted. Runtime helpers, raw journals, databases, keys and owner handoffs
remain private. Final publication audit is separate; this record does not claim
its result in advance.

Task6 still needs a separately selected exact-CID indexed A9 occurrence/tree
read once indexing reaches the required height. Never replay deployment or buy
another event to force progress. Even successful selected historical readback
does not prove complete marketplace/ERC-8004 coverage, canonical per-skill
assignment, current-head synchronization or off-chain lineage contents.


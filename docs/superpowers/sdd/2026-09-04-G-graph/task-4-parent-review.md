> Sanitized historical execution artifact. Statements reflect their recorded checkpoint and may be superseded. Historical commands are not current instructions. See the [current runbook](../../../runbook.md) for current behavior, approvals and operator commands. Personal/runtime locations and private artifact links may be redacted; public evidence and test distinctions are preserved.

# G4 parent review — September 6, 2026

G3 committed e00cd89 after its single full gate and clean publication audit.
Parent then explicitly released G4. Thirteen final source paths were captured
at the author's freeze:11 author paths plus paired root ignore additions.

## Source review and contract adaptations

Parent read full Task4/readiness/decisions/provisioning, complete ids and mapping,
all realAS tests including the later null negative controls, full renderer with
its current delta, template/config/package/README and every existing-check delta.
No material source issue identified. Independent source/runtime review follows.

The static source remains the historical v1 pilot, now pointing to the actual
settlement mapping; the inactive V2 template compiles real SettledTree handling.
No template is instantiated and no new emitter or registry is activated. Existing
smoke source/G1 fixture/schema/v1ABI remain unchanged. Events consume explicit
common fields, not a tree-class reinterpretation. Generated artifacts remain
ignored, including actual Matchstick tests/.bin output discovered during runs.

Occurrence identity checks the log range before narrowing or any writes, and
Settlement deduplication precedes splitter counters/tree changes. Immutable tree
occurrences retain exact fulluint32 counts. The mutable hash summary preserves
first scalar facts, becomes permanently ambiguous with rootnull on collision,
and never turns replay into another occurrence. Summary count overflow stays
storednull, not zero; event money/nonce/time/emitter remain exact facts.

No Marketplace factory, Agent, Listing or arbitrary metadata binding is added.
Unknown aggregate coverage remains unavailable. Existing splitter observation
metadata and nullable reference are preserved without asserting their authority.
No fee recalculation or restriction that child spend must be below root price.

## Actual author checks and failure chronology

Initial real native Matchstick AS/WASM/store execution ran against unchanged
smoke:6collected/4pass/2behavioral failures (negative and2147483648 log indices
accepted instead of refusal). New handlers corrected both; the same6 passed.
Three separately collected renderer-transition failures tracked the approved
newmapping/template contract and passed after implementation. A pre-child launcher
hash-call ArrayBuffer type error was setup-only, not a behavioral regression.

Expanded25AS cases initially gave20pass/5fail. Those five were exclusively the
test helper expecting a missing pointer when the real host materialized Graph's
ValueKind.NULL for unset fields. Mapping source was unchanged for that correction.
The helper now accepts only missing or explicitNull, and two genuine negative
controls reject measuredzero and a non-nullroot; it never relies on getterzero.

Final author27AS cases passed with exact child reaped,112focusedBun/240expect
across4files (including unchanged21ABI cases), exact4rootstrict0. These are
separate runtime kinds, not one summed count. Pinned codegen passed; author did
not run a final Graph build or full repository gate. The native runtime hash is
cd05611b588649e629e42e4ea0915d811d1ddbb73e8edd392a718c81b4361dbd;
download/version/cache/provenance limits are retained in the provisioning note.

## Remaining acceptance at this checkpoint

Independent narrow runtime/focused checks and report, parent sole frozen full
test/type gate and actual final Graph build, scrubbed public copies and commit
remain pending. Local Matchstick store behavior is not live graph-node/Studio
indexing proof. No G4 network/key/signature/payment/deployment/mainnet/push or
consumedG1approval replay occurred. Separate G6 read-only provenance work did not
activate G4 sources or provide a canonical listing assignment.

## Later independent runtime checkpoint

Independent fresh Matchstick compilation/execution passed27/27 at02:29:25.702UTC,
exactnativechild84516 exit0/reaped in1822ms;112Bun/240expect and exact4rootstrict0
also passed. This repetition is not extra unique coverage. The reviewer found no
material issue in the11authorpaths; final13fingerprint comparison/report follows.
Parent fully read the frozen187-line author report and corroborated all13 source
hashes against its independent capture. No product change followed that freeze.
Parent's sole full repository test/type plus final actualGraphbuild command is
now running against this frozen tree; final results remain pending.

## Final parent full gate and build

The single frozen full command completed exit0, collected02:35:29UTC:
2941Vitest/131files,675Bun/47files/5148expect, root+web strict0, and actual pinned
manifest regeneration/codegen/GraphWASM build0. The Graph build compiled the
shared mapping for the static pilot and reused that compiled WASM for the
inactive V2 template. No upload/deploy ran and no repeat full gate is planned.
These repository totals include retained private review fixtures;27 actualAS
cases are separate evidence, not automatically part of the root test script.
Independent report is fully read, CLEAN with all13source+4preservation pins.
Final public-copy audit and commit remain; no source correction is needed.

# G14 brief — bounded Graph evidence in Wallet Risk Note

Extend the current object-shaped wallet output, preserving the original flow
source and adding a required nullable counterparty plus nullable nested Graph
source. Publish price $0.15 and sub-spend ceiling $0.08. Actual child prices are
$0.01 for flow and $0.05 for Graph: $0.06 maximum child settlement, not the stale
plan's $0.07. At a 5% fee, $0.0825 pre-inference margin is arithmetic only, not
measured billing, live profitability or another settlement.

Use exactly those two sequential broker targets/caps and the current job's Unix
grant, never a spending key. Bind fixed child skill, distinct child/parent job IDs,
settled state, result, exact cost and per-job remaining-budget arithmetic. Validate
flow address/chain/fresh facts and the current closed G12 assessment/source contract.
Reject clean allow and positive attester counts because no production service-payment
verifier is configured. Unknown Graph evidence imposes at least caution.

Only a correlated HTTP-200 Graph envelope with settled=false, null result, zero cost
and unchanged budget can continue as definite nonsettlement. Post-dispatch transport
uncertainty, timeout, malformed/oversized evidence and inconsistent amounts refuse
the parent; no retry or payment claim is invented. Output contains fixed findings,
not provider diagnostics, broker fences or private job handles.

Make imports inert and CLI input/output/time limits explicit. The two published
child bounds require a safe sequential 135-second parent bound with cleanup; do not
assume spare runner capacity. Preserve loop-probe's existing 60-second profile and
all immutable A9 live guards: a new four-job/time/capacity profile and separate live
authority are required before claiming a later nested proof.

Require failure-first behavior/correlation tests, real owned Unix and execSkill/CLI
fixtures, observed child reaping after observer failures, exact strict compilation
including both nested tests and independent review. Socket cleanup is not proof that
the broker canceled a downstream purchase. Preserve earlier reports as dated records.
No shared source/dependencies, real key, provider/chain purchase, live profile or push.

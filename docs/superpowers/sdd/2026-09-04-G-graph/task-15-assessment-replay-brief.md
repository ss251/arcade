# G15S — complete assessment artifact and no-key replay

Release the Graph harness/native test, one inert original result-decoder export
and its focused consumer test, plus this plan-owned brief/report/index/progress.
No existing consumer/key/payment behavior changes. Reuse the original result
decoder, identity selector, pure synthesis and output schema for retained data.

Require complete globally qualified, unclaimed source-bound history. Reconstruct
the fixed identity query and, only when its original selector requires it, the
exact pinned attestations query. Validate each immutable query cache against
the qualified ledger proof. Unknown/partial/corrupt evidence refuses, never
refreshes. Keep original cost/transaction/block/acquisition provenance and all
quota counts. Supplied synthetic consistency remains explicitly distinct from
live acquisition or an actual original consumer acknowledgement.

Store an exclusive private bounded assessment result and commit marker only
when the supplied output equals the retained-data reconstruction. Revalidate
history, bytes, claims, source and monotonic five-second local IO acknowledgement.
Partial/late/uncertain writes retain artifacts; no overwrite, refund or takeover.
Read-only full replay returns a deeply immutable historical artifact with cache
hits and zero new paid queries. Never enter runGraphJob, assess, Keychain, signer,
transport or a fake-key path. Tests exercise real owned files and an actual
empty-PATH/no-network replay child; no operational root or real payment.

One sequential four-worker full gate, one atomic local commit and exact-one
mainFF. Keep fixed owner-root live orchestration/operational initialization and
all existing owner checkpoints separate; no spending, validity/cap/replay change,
approval replay or push.

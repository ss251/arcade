# G1 indexed-match parent review

September 5, 2026. Parent review of the final evidence checkpoint is clean. The
independent observation, its brief, publication audit, current README changes
and seventh scaffold test were read in full. The parent checked the filtered
transaction, exact 10,000 atomic amount and case-insensitive buyer against the
retained runbook. This accepts the previously missing indexed-runbook requirement;
it is not another live query or payment.

The public [observation](task-1-later-indexing-review.md) is exactly its unchanged
original plus the two-line banner, verified independently by byte comparison.
Original SHA256:
`8596598e2b59e3eaef6a5845d3c5dbdeea931a5ea2d937ad79b687e58cebafe1`.
The manifest, schema, smoke mapping, ABI, package and lock bytes match HEAD and
the six deployment-era fingerprints retained in the earlier records. Current
documentation preserves the partial checkpoint and its original empty results.
No credential, private journal or personal path is included in this checkpoint.

## Verification performed

- Parent repeated the seven local scaffold checks: 7 passed, 45 assertions.
- Exact nested TypeScript program for the changed scaffold test: zero diagnostics.
- Full repository gate: 2,338 Vitest tests in 115 files; 348 Bun tests in 32 files,
  3,692 assertions, zero failures. The Bun count includes 19 retained private
  runtime/security checks; it is not a count of public tests alone.
- Root and web strict TypeScript both passed after that full suite.
- Actual pinned `graph build subgraph.yaml` compiled the smoke WASM and wrote
  its build manifest successfully, with no upload or deployment.
- Scoped diff check passed. Author link audit found 48 valid local links/anchors
  before this parent review document was added; no later larger count is inferred.

The observed six-block difference, 134 ms separation and lack of direct fresh
receipt/log or seller/fee split proof remain explicit. The G1 requirement is met,
not a fully-synced/dashboard assertion. The exact acknowledged deployment is
not repeated. The checkpoint is ready for its atomic commit; G2–6 local source
release follows a separate parent decision. Full F still precedes the G merge.
No new live deployment, payment, Base payer use or other owner authority follows
from passing these local gates.

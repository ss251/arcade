> Sanitized historical execution artifact. Statements reflect their recorded checkpoint and may be superseded. Historical commands are not current instructions. See the [current runbook](../../../runbook.md) for current behavior, approvals and operator commands. Personal/runtime locations and private artifact links may be redacted; public evidence and test distinctions are preserved.

# G6 generator — independent review

September 6, 2026, 03:40 UTC. **CLEAN within the released generator scope.**
No actionable defect found and no correction requested. This is not acceptance
of the separately owned deployment command, a full repository gate or live G6
indexing proof. The reviewer authored the renderer integration, not this generator.

## Read scope and contract

Read all 289 generator source lines, all 299 test lines and the full frozen author
report; complete Task6, the 302-line readiness and current 115-line parent decisions;
and actual catalog/discovery producers plus public listing and money contracts.
The current `/listings` projection lacks a splitter and filters both delisted and
ENS-expired records. Actual `buildWellKnownX402` advertises the default rail and
does not filter ENS expiry. The implementation correctly joins these two surfaces
without promoting discovery-only resources or advertised nondefault EIP support.

The strict own-JSON capture, exact inner/outer resource equality, canonical seller
identity, actual public price grammar and bounded uint256 arithmetic are consistent
with this producer. Mixed-case seller spelling is normalized for the join without
relaxing the resource path. Description/output-schema drift and duplicate/missing
matches refuse; discovery-only resources remain excluded. Required public fields
are bounded JSON with payment-relevant validation, not a replacement for every
constraint in the full PublicListing schema.

Only the immutable approved profile lookup supplies address, seller and activation
height. Both pure entry points independently reject extra authority fields,
unknown/conflicting sources and changed inventory heights. Existing pins are
retained even when discovery is empty. The pilot remains mandatory; an A9 addition
uses exactly 60460646. Output is sorted address/startBlock only, with no historical
skill binding, metadata context, global totals or registry activation.

The local operation captures write permission, fetch, signal and cloned file URLs
before awaiting transport. It reads regular non-symlink files with bounded buffers,
performs two GETs with no credentials/body/authentication headers and no retries,
and checks cancellation before a local replacement. Fatal UTF-8 includes EOF;
response bytes, captured JSON structure and monotonic operation time are bounded.
Late uncooperative fetch acquisition is canceled and cannot reach the write path.
Fixed failures are owned through a WeakMap rather than inspecting thrown provider
objects. Real manifest validation and all twelve required asset checks precede
temporary-file creation; cooperative output changes prevent rename.

## Independently executed checks

Using the repository's Bun suite and exact TypeScript options, as directed by
`ts-testing`:

```text
bun --no-env-file test scripts/graph-splitters.bun.test.ts
58 pass, 0 fail, 117 expect() calls, 1 file, 382ms; exit0
```

The suite uses the actual public-listing transformation and discovery producer,
actual renderer/file prerequisites, owned temporary files and three actual inert
Bun import/help/invalid-argument children. I reran those fixtures, not a remote hub
or an independent live CLI write. Their normal exits were awaited; this does not
prove arbitrary child-process fault handling.

Exact strict check: read root tsconfig with installed TypeScript; parse with the
absolute config filename and current GROOT base; createProgram with only
`scripts/graph-splitters.ts` and `scripts/graph-splitters.bun.test.ts` as roots and
the unmodified parsed options; include parsed/pre-emit diagnostics. Result:
**2 roots, 0 diagnostics, exit0**, with actual transitive dependencies. No full
parsed file list or relaxed test compiler settings were used.

## Chronology and limits

The author's initial 44-pass/2-fail run records fixture problems, not behavioral
Reds. The report separately records genuine mixed-case producer, hostile thrown
value and mutable dry-run-permission failures and their subsequent unchanged
regression passes. The final suite contains those regressions; this review
repeated final Green only and did not reconstruct or rerun earlier source bytes.
Additional fixed-size file hardening is explicitly supplemental, not a FIFO Red.

Two GETs are not an atomic catalog snapshot. Matching announcements prove no
signed-Hello, historical listing allocation, current code identity or complete
Marketplace coverage. Transport assertions use injected responses; no actual
HTTP framing/server integration or ten-second timeout wall-clock experiment was
added here. HTTP framing/header validation is not claimed by this review. Local
operations are awaited and cancellation-checked, not hard-kernel deadlines;
single-file rename is not hostile-filesystem CAS, fsync/crash durability or a
two-file transaction. Current missing-rail production observations still refuse.

No source/test edits, private regression addition, Graph codegen/build, native AS,
full suite, Git command, external request, environment inspection, credential
lookup, deployment or payment was performed. Only this private report was added.
The frozen generator source, tests and author-report pins matched before and after
checks. Other context pins below record the read checkpoint; no reviewer process
remains active.

## SHA-256 inventory

```text
2507b8241c9ed373ef5d9e4f239c8e649cf0e784a9f510e631c2dd299555e766  scripts/graph-splitters.ts
4894bd0037005bf7c07aba2da66aa50d39618518e8e793d9fdcab4e102b7844f  scripts/graph-splitters.bun.test.ts
f6a1d38913592f41e572004165e8fc1b9c29fd30f98efc08a47ead56c6b4fc55  internal/task6-generator-report.md
fabe63b7f2dfecdc70073e04cf2484b0d70e799d2c63ecf1d998386eee7af9a1  internal/task6-readiness.md
1ec698e5f2e6850d140c95975ecd245f77073547ea8c01185ede06d852dd30ba  internal/task6-parent-decisions.md
1ca5df6f5008d886d4008d02e5aaf9dc2c64ef9778f87773e138cec870c33240  apps/hub/src/openapi.ts
533f80fe8a6af49214bad0c778b2cd93188dae1f24d208ed2abfffba9aedc6f2  apps/hub/src/server.ts
3eb758b690b3e71d93819a0ddfefd122a8a2c08afa2fe4532ae28daa7caa3309  packages/core/src/manifest.ts
d37519af5b63ca0cbbe5370d778f2d6771506f0e79cc16da1639c49166e46427  packages/core/src/money.ts
853f1f1a5133e0c643f2a030e355cfe0d21a89d9ac8a04cc7f7fbb6c26efcc9d  subgraph/build-manifest.ts
2bb81f7ba113799c9204a85c30cad13279826c4763708b3d835adb7d2a7dd2c9  subgraph/splitters.json
```

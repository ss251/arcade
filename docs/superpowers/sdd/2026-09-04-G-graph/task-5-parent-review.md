> Sanitized historical execution artifact. Statements reflect their recorded checkpoint and may be superseded. Historical commands are not current instructions. See the [current runbook](../../../runbook.md) for current behavior, approvals and operator commands. Personal/runtime locations and private artifact links may be redacted; public evidence and test distinctions are preserved.

# G5 parent review — September 6, 2026

Parent read full Task5/readiness/decisions, all four registry source modules and
three real AS suites, complete integrator delta/template/schema/checks/README,
generated type findings and both final author reports. Fourteen source paths
captured at final freeze; no G4 helper/mapping/test/config change. Independent
review reports final focused acceptance CLEAN; its frozen report is forthcoming.

## Contract and source acceptance

The immutable RegistryEvent stores exact tx/log coordinates with closed event
kind/disposition. Replay returns before mutation; supported prerequisite skips
are remembered. Canonical Graph order is assumed for first delivery; this is not
a reorder/backfill buffer or complete-registry-history claim. Counter validation
precedes saves. No host crash/rollback experiment is inferred from expected traps.

Agent creation uses actual Registered owner from the pinned emitter. Known-owner
transfers clear wallet authority; unknown agents remain absent. URI operator
does not become owner, and invalid replacement URI clears the old value. Known
burn refuses before writes under the nonnullable-owner schema, rather than keep
a stale owner or create dangling relations. Four typed metadata keys create
bounded immutable claims only, without canonical Listing/Splitter assignment.

Feedback preserves full signed/index/decimal values, observes revocation once
and cannot resurrect old facts. Invalid bounded indexed-tag text is remembered
skip; other optional text is independently omitted. The final logical-duplicate
comparison includes all retained normalized fields. Validation preserves first
request identity and updates its latest local50..100 pass-classifier count by
delta; no trusted validator/customer payment is implied by any count.

One active pilot and its exact inactive V2 template are preserved; three real
registry templates are additive but inactive. No template instantiation,
Marketplace row, unknown emitter or canonical link is introduced. G6 separately
selects proven source coverage; current proxy ABI is not pre-upgrade-history proof.

## Actual verification chronology

Integrator captured five genuine schema/manifest behavioral failures before the
corresponding additions. Subsequent source-mutating refusal cases are passing
supplemental coverage. A schema fixture's first-field replacement hit the newly
added RegistryEvent rather than Agent; targeting the actual Agent prefix retained
its original expected failure. A test-only strict-type correction kept the exact
historical snapshot assertion. Final focused135Bun/289expect/four-rootstrict0 and
one coordinated codegen passed; no placeholder mapping was used for generation.

The mapping author wrote suites before modules, then supplied real drafts. Two
native attempts failed compilation before collection: nullable overloaded equality
and explicit old-AS typing. These are not behavioral Reds. The first compiled
28G5 tests passed. Supplemental integer/scalar/byte/coordinate boundary tests were
added. Review found an actual optional-feedback diagnostic issue:34cases gave
33pass/1fail against unchanged comparison. A narrow normalization/comparison fix
made the unchanged34 pass. First stored facts/counters were already preserved.

Final author61AS/store cases passed:27unchangedG4+34G5 (identity16/reputation11/
validation7). Independent fresh compilation/execution repeated61pass, exact
nativechild54758 exit0/reaped at03:04:08.085UTC/6963ms, no fuse or output overflow;
independent135Bun/289expect/four-rootstrict0 also passed. Repeats are not additive
unique coverage or invented AS assertion counts. Pinned runtime source/provenance
limits remain in historical G4 records. No native process remains active.

Parent captured all14 final source hashes and released its sole complete test/
rootwebtypecheck/actualGraphWASM command in63685. Results are still pending at
this checkpoint; no second complete gate is planned for unchanged source.
The gate includes retained private test fixtures, separately from actualAS cases.

No credential, signing, payment, registry write, upload or deployment occurred for
G5. Separate G6 read-only A9 receipt/provenance preparation is explicitly distinct,
not new transactions or a G5 live indexing proof. Base paid-query approval remains
queued and unused; prior G1 deployment and A9 spending authority are not replayed.
Final public-copy audit, complete gate result and atomic commit remain pending.

## Final complete gate and build — 03:09 UTC observation

The single frozen command63685 exited0. Its actual Vitest phase succeeded
before the &&-chained Bun phase; the captured output chunk was truncated across
the Vitest aggregate summary, so no new exact Vitest total is asserted here.
Bun reported698pass/0fail across47files/5197expect calls, followed by successful
root/web strict checking and actual manifest/codegen/GraphWASM build. No full
gate was rerun to recover a display count. These totals include retained private
fixtures; the61nativeAS cases remain separately attributed runtime evidence.

Graph compiled the active pilot mapping, reused itsWASM for the inactiveV2
template, and separately compiled each of the three real registry mappings.
This is local compilation, not template instantiation or deployment. The full
independent review is read and CLEAN, SHA
1c9143043a41eb2be70b7287ab9056e0c758cbb0ec0dd3636c299ac96aeac5b2.
All14source hashes rechecked unchanged after gate; diffcheck0. Six historical
reports/copies and brief/index/progress publication audit and atomiccommit are
the only remaining G5 acceptance steps. No source correction is required.

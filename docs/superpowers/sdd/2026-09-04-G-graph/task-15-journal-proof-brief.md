# G15P — readonly balance-journal/query correlation

Release only the Graph harness/test and plan-owned brief/report/index/progress.
Read complete private balance journals without a live key, transport or write.
Require original current-source query binding, exact canonical bounded files,
private modes/no aliases, absent journal claim, complete hash chain/marker and
unchanged double reads. Bind historical handoff fields to the actual canonical
reservation ledger prefix and immutable head. Later reservations may exist;
reading a historical row must not mint a new process-local handoff or clear it.

Validate original creation/observation/intent temporal bounds at their retained
times, preserving low after observations as facts. Join journal intent exactly
to retained forward intent, then reuse the existing cache/protocol/receipt and
balance-delta verifier. Distinguish retained consistency from acquisition,
consensus, acknowledged close, successful payment or fresh spending authority.
No reservation writes/reconciliation, cache refresh, altered existing payment
validity/cap/replay rules or live release in this commit.

Use owned synthetic records/children, genuine Reds, exact TypeScript checks,
one sequential four-worker full gate and one atomic local commit/mainFF. No
operational namespace, actual key/endpoint/RPC/payment, approval replay or push.

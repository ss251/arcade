> Sanitized historical execution artifact. Statements reflect their recorded checkpoint and may be superseded. Historical commands are not current instructions. See the [current runbook](../../../runbook.md) for current behavior, approvals and operator commands. Personal/runtime locations and private artifact links may be redacted; public evidence and test distinctions are preserved.

# G3 parent checkpoint — September 6, 2026

Parent resumed canonical G after F14 commit68bb915 and the full-F fast-forward.
All four main gates passed2748Vitest/124files,523Bun/38files/4650assertions,
root/web strict,clientSSRbuild and16Forge. FROOT's529Bun/41files/4681 differed
because retained private fixtures differ; neither total is public-only coverage.
G's ten commits rebased unchanged, range-diff all equals, G2 now0d79dbd.

## Complete source review

Read full G3 task/readiness/parent decisions, complete renderer/test/template/list,
all four ABI JSON files and tests, both prior scaffold/schema deltas and package
scripts. Read complete author and independent reports for both slices, plus the
complete retained-provenance review and all relevant projected source records.
Both independent source reviews are CLEAN; their fingerprints match the author
freezes. Source scope is15 files including parent README/ignore updates; generated
subgraph.yaml is removed from tracking but retained as a local generated artifact.

The closed pure renderer accepts only the explicit current Arc config and pilot,
without listing/context authority. Its full shape and markers are validated
before output; only active files are prerequisites. CLI imports do not write or
load malformed JSON outside the fixed-error boundary. Same-directory exclusive
temporary output is renamed only after validation. This is cooperative local IO,
not crash-durable or hostile-filesystem protection. Fresh-checkout tests do not
depend on generated YAML. Every old historical schema/money/event assertion is
preserved. Direct independent reverse-delta hashes corroborate that preservation.

The four staged event subsets parse through actual Graph ABI.load; complete
ordered names/types/indexed flags match local Solidity/core/explicit candidates.
Transfer is intentionally added. Active codegen does not activate or prove the
four ABIs. No missing-handler manifest, no-op registry mapping, Marketplace zero,
canonical binding or arbitrary metadata template authority has been introduced.

Parent authored the dated README staging section and mirrored ignore pattern.
The pending tracking transition was completed with git rm --cached on only the
generated manifest, followed by an existence check: local file remains. Its
serializer trailing spaces are generated bytes, not newly tracked source;
git diff --check is clean after untracking. No source was altered to hide a test.

## Tests, genuine failures and actual build

ABI author and reviewer each passed21Bun/38 expect calls plus Node deep comparisons,
exact one-root strict0. Four missing-file collected Reds are distinct from the
three corrected plan-extraction fixture errors; eight mutation checks were
already-correct supplementary Greens, not manufactured product regressions.
Renderer author and reviewer each passed89Bun/192 assertions, exact four-root
strict0. Preserve initial two collected Reds, seven first-implementation YAML
flow-style failures and the later actual malformed-chain CLI diagnostic Red.
The latter was corrected with an inert public pin and guarded parsing, not a
relaxed assertion. Repeated author/reviewer runs are not distinct added cases.

Parent's sole actual subgraph build began07:21:49IST and was collected complete
by07:23:55IST, exit0: nested no-env manifest generation, pinned Graph codegen and
FeeSplitterSmoke WASM compilation. No deploy ran. This proves local compilation,
not AS mapping save/load behavior, new Studio indexing or full registry coverage.
All15 source hashes were frozen before the single full repository test/type gate.
That one gate completed exit0, collected07:35IST:2941Vitest/131files,
673Bun/47files/5138expect calls, root+web strict0. These include this worktree's
retained private review fixtures, not solely public test files. No repeat full
gate is planned for this frozen commit. Final publication audit remains pending.

## Keyless public provenance, with precise limits

Direct Arc explorer reads and RPC reads used no key or mutation. The current
eight selected registry event layouts match the implementation ABIs returned by
the explorer. At explicit block60670577, each proxy's ERC1967 slot matches the
last of its two explorer-reported Upgraded entries. The shared initial address
is not assumed to be a dummy implementation: its behavior is not proven here.
The current ABI must not be applied to pre-upgrade history on this evidence alone.

Explorer creation blocks: Identity29241340, Reputation29241344, Validation29241349,
pilot53890408, V2splitter60460646. Later registry upgrades are30069662/30069670/
30069677. Old registry and pilot creation receipts returned null from public RPC;
these are unavailable observations, not failed/nonexistent deployments. Only
V2's available receipt independently matches its exact runbook creation hash,
address, creator, status1 and block. No settlement follows from a creation receipt.

The retained projection was independently correlated without another network run.
It has provider response hashes but not complete raw bodies, archival receipts,
a pinned block header/state proof or a completeness proof for explorer logs.
Later separate source metadata names IdentityRegistryUpgradeable and
ReputationRegistryUpgradeable; the Validation source request returned429 and was
not immediately retried. Nonempty runtime bytes/hashes for current implementations
and both splitters are recorded at the same block. Neither nonempty bytecode nor
explorer source metadata proves bytecode equality with local source or a binding
to a canonical hub listing. These remain prerequisites for later activation work.

## Budget instruction interruption

At07:22IST the user requested conserve/save/handoff/stop. Parent immediately
stopped all three completed delegates, started no new gate and only collected the
already-running successful build plus read-only status. G code was retained;
H had no uncommitted source, only excluded private ledgers. Before any WIP commit
or completed handoff, the user lifted conserve and explicitly resumed canonical
G/H/I with one full gate per commit. No usage-reset credit was consumed by a tool.
This is not a completed stop/handoff claim. No new payment, key lookup, consumed
approval replay, production re-point, mainnet action or push occurred.

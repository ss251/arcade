# G13 brief — public skill guide matching actual authority and evidence

Write the open-source counterparty-graph SKILL.md from the implemented G10–12
contracts, not the historical plan's unsupported claims. Explain separate Arc
buyer revenue and Base seller query costs, fixed source/queries/snapshot and
bounded per-run attempts. Show a strict schema-valid illustrative output with
explicit unknown values, completeness flags and no invented proof.

State that production has no counterparty service-payment verifier or trusted
validator policy: no allow verdict and zero attester settled count. A Graph query
payment receipt is not service-settlement proof. Explain bounded pages, revoked
feedback, no-identity refusal versus incomplete evidence, and signed uncertainty
that survives a job's failure. Do not imply process restart revokes authorization
or grants a fresh durable spending budget.

Require exactly one explicit private configuration route, no default Keychain
item or fallback. Never give a key-printing/deletion command, reinitialize a live
runner, claim development MCP use not observed, or describe a network switch as
one constant. Directory publish is preview; serving is a separate owner action.
Live Base purchase and Studio evidence remain owner-gated. Pin only the actual
unsigned challenge checkpoint, and distinguish advertised price from paid cost.

Add genuine missing-document Reds for the critical guide/manifest/code contract,
strictly decode its illustrative output and check its local links/shell syntax.
Independent read-only review precedes a separate conventional documentation
commit after G12. No real key, provider call, payment or Git push.

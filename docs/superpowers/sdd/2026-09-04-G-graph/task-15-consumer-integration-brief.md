# G15U — controlled original-consumer integration

Release the Graph harness/native test and plan-owned brief/report/index/progress.
Use the fixed OS-account state parent and current local source manifest inside
the entry point, without root/source argv/env overrides or operational creation.
Require explicitly supplied command and transport capabilities for misses; no
ambient defaults or CLI live mode. Existing complete artifacts replay without
consumer, key or transport. Corrupt/unknown state refuses; complete retained
query history may explicitly materialize its missing assessment artifact without
buying again, labeled historical rather than a fresh consumer run.

For a real miss, establish known state, claim the qualified writer, observe a
fresh balance, reserve and create original handoff/admission/response journals
before entering runGraphJob or key lookup. Use one original makePaidQuery factory
and existing awaited response/pre-forward hooks. Cache hits return exact freshly
validated retained results; every miss preserves original query/parent binding.
Recheck fresh balance and persist its pre-forward/intent records under the
original callback deadline. Cache each original validated success immediately.

After any potential forwarding, including failure/abort/non-200, make one bounded
cleanup-only after-balance acquisition and retain its facts. Qualify exact
receipt/delta/history before admitting the next query. Failure keeps reservations
and claims; never retry, refund, repair or take over. Track outstanding consumer/
key work so an early runGraphJob cancellation does not skip bounded cleanup.
Only conservative successful consumer output and complete qualified evidence
may close the global claim and write the final assessment artifact.

Snapshot configuration and capability references before asynchronous work;
reject malformed/getter-backed options without exposing diagnostics. Report
reservation attempts separately from acknowledged reservations. Bounded cleanup
reports tracked-work uncertainty explicitly; it is not a universal OS/transport
shutdown guarantee or a replacement for a future owned-process hard deadline.

Tests run only in isolated owned-temporary processes with OS-home, command and
transport fixtures. Positive owner-shaped runs must label simulated account
identity and synthetic protocol/receipt data; exercise the actual original
consumer/client and real public-fixture signing where feasible, never claim an
owner signature or authenticated payment. Real wrong-account rejection remains.
No operational state, real Keychain/key/RPC/payment, existing validity/cap/replay
change, approval replay or push. One sequential four-worker full gate and one
atomic local commit/mainFF; operational/live and all owner checkpoints stay separate.

> Sanitized historical execution artifact. Statements reflect their recorded checkpoint and may be superseded. Historical commands are not current instructions. See the [current runbook](../../../runbook.md) for current behavior, approvals and operator commands. Personal/runtime locations and private artifact links may be redacted; public evidence and test distinctions are preserved.

# G2 parent release and bounded schema adaptations

2026-09-05. G1 indexed-runbook requirement accepted; final evidence checkpoint
eccbbc8 follows parent review, full2338Vitest/348Bun/strict and actual WASM build.
Release LOCAL G2 only. No deploy, upload, registry write, key, payment, G merge or
G3–6 source is authorized by this release. Full F precedes H1/G/H merges.

Parent read the complete task2-readiness, actual G2 plan, current smoke/schema,
scaffold contracts and G7 query/decoder. Accept the bounded corrections below.

## Buildable transition and ownership

Own schema.graphql; temporary smoke.ts compatibility adapter; new schema AST Bun
checks and their small fixtures; minimal existing scaffold changes; dated README
local-transition note; ignored implementation report. Add Splitter to the sole
manifest's mapping.entities declaration only; do not alter source/address/network,
ABI, handler, startBlock or toolchain. No other source ownership.

The plan's expected codegen failure is not genuine validation, and deleting the
only manifest handler would violate every-commit buildability. Capture genuine
AST contract Reds against the old schema first, then implement the schema and
required Splitter adapter. Use installed isolated GraphQL tooling, no dependency
change. Include negative AST mutations and validate actual three G7 query fields.

Preserve the original smoke schema as a tiny immutable fixture with its frozen
SHA256 in the scaffold check; move only the historical exact-schema assertion to
that fixture. New AST checks must validate the actual current schema independently.
Retain existing actual mapping money/nonce/event-identity checks and exact pilot
manifest assertions, changing only expected entities to include Splitter. Preserve
all G1 public original reports and dated partial/indexed observations; a new dated
README note explains local schema evolution does not mutate the acknowledged CID.

Temporary smoke adapter loads/creates Splitter from actual event.address, source
static, actual firstSeenBlock and event-derived counters. Set Settlement.splitter;
keep listing unknown/null and preserve every original event field. Guard existing
immutable Settlement ID before counter updates. Do not create Marketplace or
registry counts from this partial manifest; absent aggregate means unavailable,
not a fabricated zero. Local compilation does not prove graph-node runtime saves.

## Entity identity and attribution

Approve the eight planned entities plus immutable TreeOccurrence and ListingClaim.
All emitted money and full uint32 child counts use BigInt. TreeOccurrence identity
is actual transaction plus log index, with treeHash and exact event facts. Tree
remains a hash lookup summary compatible with G7: nullable root and childCount,
occurrenceCount BigInt, ambiguous Boolean; documented remaining scalar fields are
first observed. Later mappings clear root on distinct collision, retain full
occurrences, and null signed-Int overflow rather than overwrite/wrap. No mapping
collision runtime coverage is claimed in G2.

Agent.listing and Splitter.listing stay nullable. Canonical Listing is created
only with an independently trusted binding; arbitrary metadata may produce only
bounded agent-scoped immutable ListingClaim evidence, never assignment authority.
Do not infer seller, version, agent, current owner or globally allocated listing
ID from metadata strings/getter duck typing. Keep actual registry bytes explicit;
chain+agent ID assumes one pinned registry per chain. Unknown agent owner means
no canonical Agent yet, not a zero/empty placeholder. Retain required owner.

Future G3–5 must separately address trusted static context, untrusted dynamic
emitters, current-owner Transfer events, idempotent revocation/response/counters,
event de-duplication, timestamps versus block units and immutable collisions. G8
cannot present reciprocal strings alone as independently verified hub ownership.
These are recorded follow-on constraints, not permission to implement them now.

Finish focused scaffold/schema/query compatibility checks, exact nested strict,
actual pinned codegen and WASM build; distinguish schema/source assertions from
runtime save/load evidence. No Matchstick binary download or runtime/network
expansion without a separate parent decision. Freeze exact source/report hashes
for parent independent review, public preparation, full gates and atomic commit.

# G2 parent source and publication review

September 5, 2026. Parent review is clean for the seven frozen schema/adapter/
manifest/check/fixture/README paths. The complete implementation and publication
reports, readiness decisions, actual schema, both tests and complete source diff
were read. The historical fixture exactly equals the previous G1 schema; the
manifest adds only the emitter entity. Original G1 evidence remains historical.

The temporary adapter initializes all required Splitter fields from actual event
facts, guards immutable settlement identity before counters, preserves original
money/nonce/transaction fields and assigns the required emitter. It does not
create a Marketplace or infer listing/registry authority. The occurrence/claim
schema permits honest later collision and unknown-attribution handling; it does
not implement those mapping rules in G2.

## Parent verification

- Focused scaffold/schema repeat: 37 Bun checks, 109 assertions, zero failures.
- Exact nested TypeScript for both checks: zero diagnostics.
- Full gate: 2,338 Vitest tests /115 files; 378 Bun tests /3,756 assertions /33
  files, including 19 retained private G1 runtime/security checks. Root and web
  strict TypeScript passed.
- Parent separately repeated actual pinned codegen and WASM build from the
  subgraph package. Both exited zero; no upload, deployment or network query.
- Ten source/toolchain fingerprints match the original report. The public report
  is independently verified as exactly its original plus the historical banner,
  with no body substitutions; original SHA256 remains
  `2bfe9a927e508729e20ef06935aba2b9bf27ac194cc3bb45bf590910134d3bed`.
- Parent repeated the four-document local-file-link/privacy check; 46 links
  resolve before this review document was added. The author's separate audit also
  checked anchors. No personal/temp path appeared; scoped diff check passed.

AST/query/source validation and compilation are not graph-node runtime saves,
duplicate event delivery, tree-collision execution or registry-handler proof.
The acknowledged G1 CID is unchanged; these local schema bytes have not been
deployed. Ready for the atomic G2 commit. G3–6 require separate source release,
and full F still precedes G's main merge. No new owner live authority is implied.

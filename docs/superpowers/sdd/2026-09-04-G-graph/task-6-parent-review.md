> Sanitized historical execution artifact. Statements reflect their recorded checkpoint and may be superseded. Historical commands are not current instructions. See the [current runbook](../../../runbook.md) for current behavior, approvals and operator commands. Personal/runtime locations and private artifact links may be redacted; public evidence and test distinctions are preserved.

# G6 parent review — September 6, 2026

Parent read the complete Task6, readiness and decisions; generator, deployment
consumer and both complete test files; renderer/list and all changed contract
checks; the operator README/runbook/aliases; author and independent reports.
The source review accepts the two known-profile boundary, absence of canonical
attribution and the narrow deployment child-cleanup correction. This is a local
implementation checkpoint, not live Task6 completion.

## Contract and actual review

Current catalogue/discovery is only a candidate join. The generator uses the
actual public producer's six-decimal price grammar, canonical seller identity
and exact payment coordinates, with bounded own-JSON capture and fixed errors.
Captured write permission cannot be changed by a later awaited transport. Two
read-only requests are not an atomic snapshot or historical signed-Hello proof.
Required known pins survive empty data; arbitrary profiles, changed heights and
seller conflicts fail. Final renderer/file checks precede atomic list replacement.

The renderer selects exactly the reviewed A9 and pilot sources in address order.
A9 uses its creation block60460646; pilot0 remains the explicit historical
exception. Four templates remain inactive. G4/G5 schema, mapping/helper modules,
AS/store suites and original ABI fixtures remain byte-identical. No Marketplace,
per-skill counters, canonical ownership or registry-history activation is created.

The deployment interface captures fixed policy and trusted dependencies, validates
an explicit CIDv0 and fresh private journal before Keychain, then durably records
intent before its sole POST. Exact endpoint/slug/version/query URL, framed bounded
response and closed diagnostics exclude provider prose. Unknown dispatch outcomes
are retained, never retried. A fixed Keychain child uses empty env and no argv key;
the key is held by the consumer only. This is not standing permission for arbitrary
CIDs or new journals, global cross-journal deduplication, RAM erasure or defense
against a compromised same-user filesystem.

Independent review reproduced an enclosing-operation cancellation returning
before native child close (zero sends). The corrected consumer retains and joins
the key-reader promise with bounded cleanup. The same independent probe now
observed closedAtReturn:true, SIGKILL and zero sends. The original report prefix
and narrow original source/test preimages were retained, not rewritten to erase
the failure. A stuck injected reader remains finite without claiming reaping.

The actual root help aliases were run with an empty environment except PATH.
Both exited0, emitted only fixed help and did not request any key or network.
The four separate configuration tests prove exact alias values, not deployment.
No real Keychain invocation, upload, Studio POST or payment occurred.

## Focused evidence and limitations

- Integration:161 Bun/364 assertions, exact four-root strict0 and coordinated
  codegen0; the generated A9 event module equals the existing V2 template module.
- Generator author and independent reviewer:58 Bun/117 assertions and exact
  two-root strict0 each. These repeated checks are not summed.
- Deployment author:29 Bun/242 assertions and exact two-root strict0 after
  correction. Independent review ran original27/235 and later two regression
  cases/7 assertions plus exact strict and the same fileless child probe.
- Parent:four aliases failed before the package change and passed unchanged
  afterward; actual help aliases both passed; full source/report/diff review.
- Existing A9 receipt/provenance checks are read-only dated evidence, not Graph
  indexing or a repeated purchase. Selected projections and hashes do not imply
  raw RPC body retention or independently reconstructed complete chain history.

Parent's one complete repository test/type gate and actual Graph build are
running at this checkpoint. The separately drafted private query suite is
preserved outside test collection until this gate completes. No second gate,
native-AS repeat, paid query or upload is inferred from these focused results.

## Frozen thirteen-path source inventory

```text
64d88df42b286fa2d9cc59351a8b11c7e01005451fbd25fde8fbe61c55d898a0  package.json
f2112233c2988c27660708dc56df7330d9a07c1e68ea1b916f501f465c55a3c3  scripts/graph-commands.bun.test.ts
6e1397318b7f30e47842dc8fbd25994034fa33cd6cb084ba440fe3d1aca14de2  scripts/graph-deploy.ts
8ce1071a0c4c5c80a2d730d85ad2d8ced5ecf2b678c10ffed1283736333d7907  scripts/graph-deploy.bun.test.ts
2507b8241c9ed373ef5d9e4f239c8e649cf0e784a9f510e631c2dd299555e766  scripts/graph-splitters.ts
4894bd0037005bf7c07aba2da66aa50d39618518e8e793d9fdcab4e102b7844f  scripts/graph-splitters.bun.test.ts
853f1f1a5133e0c643f2a030e355cfe0d21a89d9ac8a04cc7f7fbb6c26efcc9d  subgraph/build-manifest.ts
2bb81f7ba113799c9204a85c30cad13279826c4763708b3d835adb7d2a7dd2c9  subgraph/splitters.json
29f00a64dfa57223916cf5d5479edef2a99b34f8cd7b5b0d30f421e148510b89  subgraph/checks/manifest.bun.test.ts
bca223ab29b681b0884a9a38bead48a57d2ed5962144db4ff2bbc5161c6fe1a2  subgraph/checks/scaffold.bun.test.ts
77b0fd9a5e2c2ac91fccb33220e5460b768e3cbf928591ebc87740157e3075ec  subgraph/checks/schema.bun.test.ts
7457263f9eec50e75c0762b7605c6d645a2274a89e2061d78e47bd120d9f6eb0  subgraph/README.md
512cadd9bb7272943fa1ad5f1c0ce57be5c7a5e0e76c8db985420ddfe0624b13  docs/runbook.md
```

Public copies preserve preparation, failures and limitations under a historical
banner. Local implementation will commit before deliberate reviewed upload and
one fresh v0.1.0 deployment. Exact indexed A9 proof and any final Task6 acceptance
follow in a separately gated evidence commit. H-before-G8 remains the recorded
shared-server integration exception. The Base10/5 allowance is still unused;
it does not authorize a new Arc G15 purchase. No GitHub push.

## Later sole full gate and local build — 03:48:35 UTC checkpoint

The same frozen complete command exited0:2941 Vitest tests/131 files;
815 Bun tests/50 files/5637 expect calls (157.64s for Bun); root/web strict;
and actual manifest regeneration/codegen/Graph WASM build. These totals include
retained private fixtures, not only new public G6 tests. Full output was captured
without truncating the aggregate counts. No unchanged-gate rerun occurred.

The build compiled FeeSplitterA9 and reused its exact mapping WASM for pilot/V2
template, plus all three inactive registry mappings. The old unreferenced pilot
WASM remains in the build directory from a prior build; it is not a manifest
source or automatic upload input. Public upload will follow only exact current
manifest references, separately audited. All thirteen frozen source hashes
matched after the gate; diff whitespace check passed. No native-AS repeat was
required because its source/schema/test inputs were unchanged.

This supersedes the gate-pending checkpoint, not the still-pending upload,
deployment or indexed proof. Final public-artifact audit and atomic local commit
follow. The private query helper may now resume for the later live-evidence step.

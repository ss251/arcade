# G10 brief — bounded Graph evidence listing and pinned query documents

Create the $0.05 script listing and two fixed, bounded Base ERC-8004 queries. Add
actual skill-test collection before tests; preserve no-hire authority and private
credential/egress projection. Match the installed core decoder, public canary input
contract and G11 output types. Explicit zero subspend is invalid in the current
positive-only optional Bounds field, so omit it and test the effective zero grant.

Do not describe raw off-chain payment claims as verified settlement or policy
eligibility as financial safety. Include source block/hash and nullable observed
cost; bind identity aliases, validation agent and feedback envelope fields. Treat
25/100 page limits as incomplete evidence, not exhaustive results. The strict
runtime must enforce properties the generic hub JSON validator does not support.

Pin client-x402 1.0.0 at the root with a minimal reviewed lock change, lifecycle
scripts disabled and a frozen repeat. Inspect actual exports/types/transport before
G12. No global fetch/environment mutation, key access, paid request, RPC, Studio
account/deployment or live-evidence claim. Root owns independent review/full gates,
ordered G10-before-G11 commits, and the canonical F-before-G merge.

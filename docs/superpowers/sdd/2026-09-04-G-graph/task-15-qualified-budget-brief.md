# G15Q — readonly global retained-evidence qualification

Release only the Graph harness/test and plan-owned brief/report/index/progress.
Derive a bounded readonly view of the existing fixed-namespace reservation
ledger and every immutable head under an original current-source manifest.
Preserve all reservation/evidence/video/atomic counts and existing caps. No
ledger write, operational root, new claim, reset, refund or live authority.

Only complete private unclaimed balance journals and query caches may qualify
through the existing journal/protocol/receipt/balance verifier. Reconstruct each
canonical binding from bounded saved data and the original source manifest;
attestations require an earlier qualified identity query in this same ledger.
Require globally distinct transaction/nonce and chained balances/time/block
observations. Missing/partial evidence remains unresolved; malformed complete
evidence refuses. An unresolved row stops qualification of all later rows.

Existing reservation decoder/writer behavior stays unchanged. This view does
not yet admit another reserve; a later opt-in writer must revalidate it under
its own exclusive claim. Existing complete cache/journal files already provide
durable evidence markers, so no redundant mutable paid/refund flag is added.
Use synthetic owned tests and genuine Red, one sequential four-worker full gate,
one atomic local commit/mainFF. No real key/RPC/endpoint/payment/spend or push.

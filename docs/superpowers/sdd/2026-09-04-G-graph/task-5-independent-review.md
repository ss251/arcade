> Sanitized historical execution artifact. Statements reflect their recorded checkpoint and may be superseded. Historical commands are not current instructions. See the [current runbook](../../../runbook.md) for current behavior, approvals and operator commands. Personal/runtime locations and private artifact links may be redacted; public evidence and test distinctions are preserved.

# G5 independent review — September 6, 2026

**CLEAN at the final fourteen-path freeze.** Independent acceptance: **61 actual Matchstick AS/store tests** (unchanged G4 27 plus G5 34: identity16/reputation11/validation7), **135 focused Bun tests /289 expect calls**, and **exact four-root TypeScript strict: 0 diagnostics**. No additional source correction requested. Parent owns the actual Graph build, complete repository gate, publication and commit.

## Scope and method

Read full actual Task5, readiness, parent decisions including both later field clarifications, all four mapping modules, all three complete AS suites, all seven integration files, generated event getters, and both final author reports. The fully read ts-testing skill guided actual handler/store coverage and separation of runtime versus source/typing checks. Initial source review waited for the author's compiler work; no independent AS execution overlapped an author run or codegen.

Only the focused native run, focused Bun/strict commands and local read-only hash/reconstruction checks were performed. No source/test edits, Git, network, key lookup, query, signature, payment, dependency installation, upload or deployment. No TypeScript mirror was substituted for real AssemblyScript execution. The only authored file is this ignored review.

## Finding, clarification and correction

- Source review identified that logical feedback duplicate classification omitted retained normalized tag2/endpoint/feedbackURI. Changing those fields was labeled ignored_duplicate instead of ignored_conflict. First stored facts, revoked state and counters were already preserved; this was truthful disposition, not resurrection or double counting.
- Parent approved the narrow correction. Author captured the actual regression on original reputation SHA `f568d31124e02cfbdd8ea6d2fdf59baedf2cd1af90a28f1b50a4609db9fd64a3`: 33 pass/1 fail at 03:00:43.470 UTC, then unchanged34 passed at 03:01:14.608 UTC. These Red/Green executions are author-attributed, not independently repeated on intentionally reverted production.
- Final code normalizes those optional fields once, compares all three, and reuses their values on first insertion. I independently reversed only that delta in memory and reconstructed the exact original Red hash. The collected regression independently passed on the frozen final source.
- The apparent invalid-tag1/optional-text mismatch was a parent-policy clarification, not a defect: tag1 must be valid scalar text <=2048 UTF8 bytes (empty allowed) before topic hashing; otherwise a remembered invalid_tag skip creates no Feedback/count. Other optional text can be omitted independently. The appended decisions, README, implementation and tests now agree.

## Final source verdict

- RegistryEvent is the only schema addition. Required occurrence coordinates, actual emitter and closed helper kind/disposition values are preserved. Markers precede replay mutations; supported prerequisite skips remain remembered. Known occurrence coordinate conflicts refuse. This supports canonical Graph delivery order plus arbitrary exact replay, not arbitrary first-delivery reordering or an adversarial replacement of an already canonical event payload.
- Registered alone creates a nonzero-owner Agent with full-width ID. Distinct registrations do not reset its facts/counters. Actual nonzero Transfer recipient becomes owner and clears wallet; known zero-recipient transfer refuses before saves under the required-owner schema. URI operator is not promoted to owner; invalid URI clears the optional prior value. Unknown prerequisites produce no placeholders.
- Four metadata keys create separate immutable claims only. Indexed key hashes match supported keys; UTF8 bytes are validated before conversion, and Unicode scalar/text byte bounds are explicit. Address/listing-ID/uint256 grammar is closed and does not coerce. Endpoints stay opaque. Unsupported agentWallet metadata is ignored; no canonical Listing/Splitter relationship or dynamic source is invented.
- Feedback preserves signed int128, uint64 index, exact raw uint8 decimals and independently normalized text. Full decoded integer validation precedes generated i32 narrowing. Logical duplicates/conflicts keep first facts; revocation decrements once and does not resurrect. Validation keeps its first request Agent/validator identity, binds each response, preserves raw0..255, and applies local PASSED50..100 latest-state deltas. Impossible counters refuse before saves rather than clamp to zero.
- Active production source remains exactly the G4 pilot. V2 plus the three registry templates are inactive, with real mapping paths and staged ABI layouts. No source address/startBlock/context is introduced for a registry. Tests compare signatures/order/indexing/widths against actual ABIs and verify all four dynamic-source instance counts stay zero.
- Registry counts describe accepted/observed bounded records, not all emissions, trusted validators, actual service payments or complete marketplace coverage. No Marketplace is created; G7's unavailable/canonical-binding requirements are not weakened. No current implementation ABI observation is promoted into pre-upgrade indexing compatibility or activation authority.
- Import/CLI input validation and fixed diagnostics remain inside the renderer boundary. Missing required real mapping/ABI assets preserve prior output. The historical source snapshots, actual schema/query contract and README chronology are retained.

## Independently executed checks

Native command from subgraph/: `<owned-runtime-directory>/binary-macos-12-m1 -r fee-splitter identity reputation validation`.

The exact executable SHA-256 was `cd05611b588649e629e42e4ea0915d811d1ddbb73e8edd392a718c81b4361dbd` before and after execution. One owned native child received only `PATH=<verified-node-bin>:/usr/bin:/bin`, ignored stdin, separately capped524288-byte stdout/stderr, and a60000ms SIGKILL fuse. All four selected suites visibly compiled fresh. PID54758 exited0 and was awaited/reaped at **2026-09-06T03:04:08.085Z**, elapsed6963ms; no fuse or overflow fired, stderr empty. The two unchanged G4 negative-control assertion diagnostics remain expected PASS. All fourteen G5 paths plus G4 ids/mapping/test/config hashes matched before/after.

Focused Bun command from subgraph/, using the installed Bun with env-i/PATH-only and no env file:

```sh
bun --no-env-file test ./checks/manifest.bun.test.ts ./checks/abis.bun.test.ts ./checks/scaffold.bun.test.ts ./checks/schema.bun.test.ts
```

Result: **135 pass, 0 fail, 289 expect calls /4 files**, reported549ms, exit0. The unchanged ABI21 checks are included, not additional new G5 tests. Owned temporary CLI fixtures cleaned their layouts and awaited children. No Graph codegen/build or external service was invoked by this selection.

Exact strict at **03:04:37.628 UTC**, exit0: root tsconfig read and parsed with its absolute filename/current GROOT base; ts.createProgram used only subgraph/build-manifest.ts and checks/schema.bun.test.ts, manifest.bun.test.ts, scaffold.bun.test.ts, retaining actual transitive dependencies and parsed options with noEmit. Config plus pre-emit diagnostics0; strict:true. This is not the repository-wide type gate or a claim that TypeScript checks AssemblyScript.

Expected-failure AS cases demonstrate refusal; source inspection verifies validation occurs before writes. No separate transaction rollback/crash experiment, arbitrary storage-fault experiment, host descendant fault injection or live graph-node/Studio persistence claim is made. All owned review processes ended. Generated/compiled files are ignored local artifacts.

## Chronology and preservation

Full mapping author report SHA `d949dec24b23c6ca49e61241389831c0d50790a6e3a5f4563f76ec6b51bb1623` and integrator report SHA `90e0d86c54f3002086b61cb806665aa0fe9693eb0146dd79016a583c2481bce3` were read completely. Parent decisions at read SHA `98bf335951fe09602ca3c3af4b793961da08dbed296620f7a67c50542dd1bc5e` include both clarifications.

Integrator's initial five genuine missing-entity/template failures are distinct from its one type-annotation issue and later historical Agent.registry mutation targeting the newly added RegistryEvent field. Mapping author's initial two native compiler failures collected no behavior: overloaded nullable Bytes/BigInt comparisons, then u8/i32 and nullable-string typing. First actual mapping runtime was28 Green; later boundary coverage was supplemental Green, and only the optional-text classifier regression was33/1 Red. No nonexistent missing-module run or compiler failure is relabeled as a product Red.

All eleven integrator preservation hashes matched: G4 ids, fee-splitter mapping/suite, Matchstick config, historical smoke/G1 schema fixture/v1 ABI, splitter list, package and paired ignores. In-memory removal of only RegistryEvent reconstructed exact G4 schema SHA `913780f0d521079f2cb97670a0bd542a087ac27cbc2e0ae93d58d8e75defb483`. README prefix before the G5 heading reconstructed exact G4 SHA `3b5163d4a18dcf09236cc7616a1b67f68ef75fb86e4a50d17eff1f0151430c11`.

## Frozen fourteen-path SHA-256 inventory

```text
c9686c6c284ecd2a8905a4f55fa1ff43fa1d5c8a759bdb62e811d37ff1ddc810  subgraph/src/identity.ts
ba448181295f4a2c87e8846d7828ce85266c4cb9c4b62ceeecb4e710ff9657fe  subgraph/src/reputation.ts
049347854a9ad81b1c6d07435b313de88d48c63f2d90367bec83adbb61a83d65  subgraph/src/validation.ts
72fecf0bc69da5342e5f3809d4e9a224109a13ef87df82571876259c87e757a9  subgraph/src/registry.ts
09c8aa39b9dbd6cbfaabc80f8de10787b7d1a60ccaa62de2a609e460bd09727c  subgraph/tests/identity.test.ts
a629b8b49c3f7c68d554eb75cc55bdf97bfe5876e8194aa4bbcc75dc075afac4  subgraph/tests/reputation.test.ts
b33922173c861e04103c851aea7990b4fdfa39d0a9bd89e3cd6536d8dc134c97  subgraph/tests/validation.test.ts
eb08f3767b2916cbe0b4e66f73f81f6b23156b362b20f58cf925f6fae66e97db  subgraph/schema.graphql
c0396bec23c5356fddb5329b99ace622bed2d00de3ce5d95861056dcabb0bd35  subgraph/checks/schema.bun.test.ts
c8d73d2bdfd0e8a8f8692b007c7609e68cebbed86ba7c3a6b15222abef722a71  subgraph/build-manifest.ts
85a05e6002647d2f1bff3aca984354764c6b2876464d1cc4059d88b2d1d2a39f  subgraph/subgraph.template.yaml
3b30433f1677f441c9198ea469e226f1846c01cb1e71627bd47f49786a56bda6  subgraph/checks/manifest.bun.test.ts
6d47fd9979321f9dbb2fd95b4e8134bea2794607dd2778c371af57f5a3280a74  subgraph/checks/scaffold.bun.test.ts
fba9ff44ec61067e8b150fe53f118d604e30c7204bd91b4e04de426ef16f7bb7  subgraph/README.md
```

All fourteen matched again after the focused Bun/strict checks. No deployment, registry history, canonical relationship, paid-query or new Arc-spend authority follows from this local acceptance.

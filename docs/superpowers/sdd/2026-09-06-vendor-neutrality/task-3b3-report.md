# Task 3B3 — pinned Circle fixture and metadata evidence

Base72b40a0. No production source, dependency, engine, public schema or payment
behavior changes. Added the pinned two-skill Circle compatibility fixture,
source attribution/hash records and four offline regression cases. The public
[evidence record](../../../evidence/B13-agent-plugins.md) distinguishes the fixture,
native CLI offline proof and one live read-only handler discovery session.

The plugin-creator skill guided compatibility validation only. Upstream manifest
and reference contents remain inert data, not active agent instructions; no
personal marketplace or plugin installation was performed. The full upstream
plugin has18 skills; this excerpt deliberately includes two and all17 references.
Apache license/NOTICE accompany each generated skill. Source records verify25
mapped copies:21 byte-exact and four explicitly normalized final-LF copies.

The first focused invocation's outcome was lost at a context boundary; no test
process remained when checked. One observed focused run then passed110 tests/
four files/3.00s, including real native CLI generation/read-back, reference and
attribution preservation, no frontmatter authority and selected-skill no-network
behavior. Exact test-root strict checking passed with zero diagnostics. These
are fixture regression tests, not a new behavioral Red or a full-gate replay.

One credential-free live metadata session returned four read-only MCP listing
candidates with no skipped components or tool execution. HTTP405 on the optional
GET stream did not prevent successful initialize/tools-list. The actual existing
ingestion handler and SDK transport were used with a finite endpoint/method/header
boundary. No model/wallet API, key read, payment or files generation. A distinct
post-exit observation verified process absence, private journal permissions and
no output. Raw operational evidence remains private.

Root self-review, pinned Git-blob/size audit, inactive-SVG scan and heuristic
secret/personal-path scans passed. One sequential four-worker full gate follows;
no agents, independent parallel reviews, paid retries or push. After the fixture
commit/fast-forward, vendor Task3 ingestion is complete for the documented subset;
PlanJ full checkout/live listing work and the H10/H14/G15 gaps remain separate.

## Sole gate, isolated follow-up and publication audit

The sole full invocation66846 exited1:4,494 Vitest tests/201 files/65.23s passed;
838 Bun tests passed and one existing relay cleanup hook timed out (55 files,
6,114 assertions,168.01s). Its deliberately permanently pending fake upstream
outlived a5-second afterEach hook. The relay and its test are unchanged since
c6f6676; the investigation skill required tracing that cleanup before any fix.

The one affected case then passed unchanged in137ms with two assertions; no
other case was run. The exact intermittent root cause is unproved, so no
speculative source fix or timeout increase was applied. Only the previously
unreached type/build stages followed:root/web strict0,client392ms/SSR180ms.
This is **split coverage with a recorded flaky cleanup failure**, not a clean
monolithic gate. No full sweep or live metadata probe was repeated.

Thirty frozen test/fixture hashes,35 scoped paths,19 local links,empty index,
privacy/diff checks and unchanged runtime/dependencies audited before commit.
Status:DONE_WITH_CONCERNS for the existing intermittent cleanup hook. The
fixture/metadata claim passed; escrow/payment execution remains outside it.
